# Permanent railway construction boost across the Burmese protectorate
IPB_railway_construction = {
	enable = { always = yes }
	icon = GFX_IPB_railway_modifier_icon

	state_production_speed_rail_way_factor = 0.5
}

# Philippine guerrilla system (port of the Yugoslav partisan mechanics)
PHI_organized_guerrilla_activity = { #STATE MODIFIER
	enable = { always = yes }

	icon = GFX_modifiers_SOV_invaders_must_die

	resistance_damage_to_garrison = 0.5
	compliance_gain = -0.05
	resistance_growth = 0.2
	resistance_target = 0.8
	resistance_decay = -0.2
}

PHI_dispersed_guerrilla_activity = { #STATE MODIFIER
	enable = { always = yes }

	icon = GFX_modifiers_SOV_invaders_must_die

	resistance_growth = 0.1
	resistance_target = 0.3
	resistance_decay = -0.10
}

# Tokyo Express (Bismarck Sea prototype): stacking supply bonus while the run is active,
# banded by how many destroyers IJN currently has assigned to it.
# local_supplies_for_controller is engine-guaranteed to feed the state controller only,
# and the enable check restricts it to JAP-controlled states, so the bonus is JAP-only.
# NOTE: cross-scope check_variable = { var = IJN.num_ships_with_type@destroyer ... } is
# untested syntax, see implementation report.
JAP_te_supply_1 = { #STATE MODIFIER
	enable = {
		CONTROLLER = { tag = JAP }
		JAP = { has_country_flag = JAP_te_active }
		check_variable = { var = IJN.num_ships_with_type@destroyer value = 0 compare = greater_than }
		check_variable = { var = IJN.num_ships_with_type@destroyer value = 2 compare = less_than }
	}
	icon = GFX_JAP_te_supply_icon

	local_supplies_for_controller = 0.05
}

JAP_te_supply_2 = { #STATE MODIFIER
	enable = {
		CONTROLLER = { tag = JAP }
		JAP = { has_country_flag = JAP_te_active }
		check_variable = { var = IJN.num_ships_with_type@destroyer value = 1 compare = greater_than }
		check_variable = { var = IJN.num_ships_with_type@destroyer value = 3 compare = less_than }
	}
	icon = GFX_JAP_te_supply_icon

	local_supplies_for_controller = 0.10
}

JAP_te_supply_3 = { #STATE MODIFIER
	enable = {
		CONTROLLER = { tag = JAP }
		JAP = { has_country_flag = JAP_te_active }
		check_variable = { var = IJN.num_ships_with_type@destroyer value = 2 compare = greater_than }
		check_variable = { var = IJN.num_ships_with_type@destroyer value = 4 compare = less_than }
	}
	icon = GFX_JAP_te_supply_icon

	local_supplies_for_controller = 0.15
}

JAP_te_supply_4 = { #STATE MODIFIER
	enable = {
		CONTROLLER = { tag = JAP }
		JAP = { has_country_flag = JAP_te_active }
		check_variable = { var = IJN.num_ships_with_type@destroyer value = 3 compare = greater_than }
	}
	icon = GFX_JAP_te_supply_icon

	local_supplies_for_controller = 0.20
}
