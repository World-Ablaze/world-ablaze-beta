scripted_gui = {
    resource_usage_window = {
        context_type = player_context

		window_name = "facilities_resource_usage_box"
		parent_window_name = facilities_overview_bottom_window
		visible = {
			always = yes
		}
    }
}