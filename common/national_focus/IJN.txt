# Blank focus tree for the IJN Tokyo Express subject: it exists purely as a fleet
# administration, so it gets no focuses at all (user ruling 2026-07-14). The single
# dummy focus sits behind allow_branch = never and is required only so the tree parses.
focus_tree = {
	id = IJN_blank

	country = {
		factor = 0
		modifier = {
			add = 100
			tag = IJN
		}
	}

	default = no
	reset_on_civilwar = no

	focus = {
		id = IJN_blank_focus
		icon = GFX_goal_generic_build_navy
		x = 0
		y = 0
		cost = 1
		allow_branch = { always = no }
		ai_will_do = { factor = 0 }
		completion_reward = { }
	}
}
