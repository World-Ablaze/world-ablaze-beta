############################################################################################################
#	World Ablaze AI mod - Construction System - Queue Functions Module
#	Building queue functions for standard construction AI
############################################################################################################

### functions for AI construction

# [pc-queue] breadth-first placement in the shared-slot queues.
# Engine fact this rests on: the engine merges same-(state, building) adds into ONE construction line
# (amount = N); a line takes at most MAX_CIV_FACTORIES_PER_LINE (WA: 20) civilian factories and builds
# its levels SERIALLY. Breaking on the first eligible state of the score-sorted array therefore turned
# a burst of N calls into one slow line instead of N parallel ones.
# Each queue function makes ONE walk over WA_AI_shared_slot_scores and classifies every eligible state
# (WA_AI_bf_consider) into four tiers, best first:
#   A  rank < K, under the per-line depth cap, not yet picked this pulse   -> the normal spread
#   B  rank < K, under depth, already picked this pulse                    -> every top-K state has
#      one; wrap around (picked set is cleared, round-robin restarts)
#   C  rank < K, over depth, not yet picked                                -> the depth cap is a
#      PREFERENCE, not a wall: a burst bigger than K x depth keeps spreading
#   D  first eligible state in score order, whatever its depth/picked state -> the plain first-fit
#      pick, so no country can ever build LESS than before (picked set cleared)
# The first tier-A hit ends the walk (the array is score-sorted, so it is the best). K =
# ceil((civs - civs already assigned to priority construction) / @WA_AI_BF_CIVS_PER_LINE), clamped
# to [1, @WA_AI_BF_MAX_K] = the lines the country can actually feed. WA_AI_bf_k is kept FRACTIONAL
# (9.15, 12.8 ...): the walk compares `_wa_bf_rank_ < WA_AI_bf_k` with an integer rank, which IS the
# ceiling - and the WA_TLM_sq_k / _k_max gauges copy it as-is, so read them as ceil(). Score order
# still decides WHICH K states are candidates; breadth only decides how a burst spreads among them.
# All nine shared-slot types (CIC/MIC/NIC + the five refineries + SILO) carry the [refineries] sum
# WA_AI_committed_<X> and a depth trigger WA_AI_queue_depth_ok_<X> (WA_AI_CONSTRUCTION_triggers.txt).
# Not applied to the non-shared queues (RADAR/AIR/AA/INF): different arrays, small caps.
# WA_AI_bf_k, WA_AI_bf_picked_<X> and every _wa_bf_* are TEMP (execution-scoped = one event fire =
# one burst). The selection loop breaks on its own _wa_bf_break_ and never reads the plain `break` on
# entry; the plain `break = 1` is still set on every add so the downstream contract (queue_INF
# fallback, MIC-replacement legs) is unchanged whatever the engine does with `break` on loop entry.
# Cadence: C.10 is gated on idle civs > 1 AND re-arms for 75-1000 days after a firing, so absorbing
# more idle civs per firing does not lower the firing rate. Priority construction keeps its floored
# share ([refineries] stable base) and K is computed net of the civs PC already holds, so even a bulk
# fire_only_once burst never takes the whole pool.
# num_of_civilian_factories (TOTAL) minus WA_AI_PC_assigned_factories_total on purpose - the idle
# reading *_available_for_projects sits at ~0 once the vanilla queue saturates (see lessons-log).
# Telemetry: WA_TLM_sq_* (standing, family "sq" = standard queue) - see WA_TLM_core.txt / doc 6d.
# MUST MATCH NDefines.NProduction.MAX_CIV_FACTORIES_PER_LINE in common/defines/05_defines.lua (WA = 20,
# vanilla 15) - it is the divisor that turns "civs" into "lines the country can feed".
@WA_AI_BF_CIVS_PER_LINE = 20
# Hard ceiling on breadth, independent of civ count. Rank K+1.. of the score order is never used by
# tiers A-C even for a 600-civ USA; a burst larger than K wraps around inside the top K. It bounds
# two costs of breadth: how far down the score order (infra, supply, exposure) a burst may reach,
# and how many civs one burst may take from the engine's own queue at once.
@WA_AI_BF_MAX_K = 20

# ROOT = THIS = country. Computes WA_AI_bf_k once per pulse (same "compute if 0" idiom as
# WA_AI_has_shared_slot_scores).
### PC constants read by the queue functions in this file - the budgets
### (constant:wa_ai_pc.qmax.inf_resource / .mic_to_cic), the scoped type-1 strategy tag
### (constant:wa_ai_pc.type_id.inf_resource, one numbering space with strategies.txt and the railway
### files) and the [pc-queue] priority bands (constant:wa_ai_pc.prio.strategic / .default) - are HOI4
### script constants in common/script_constants/wa_ai_pc.txt.

WA_AI_queue_breadth_prepare = {
	if = { limit = { check_variable = { WA_AI_bf_k = 0 } }
		set_temp_variable = { WA_AI_bf_k = num_of_civilian_factories }
		subtract_from_temp_variable = { WA_AI_bf_k = WA_AI_PC_assigned_factories_total }
		divide_temp_variable = { WA_AI_bf_k = @WA_AI_BF_CIVS_PER_LINE }
		clamp_temp_variable = { var = WA_AI_bf_k min = 1 max = @WA_AI_BF_MAX_K }
	}
}

# Resets the per-selection temporaries. Country scope, called right before the walk.
WA_AI_bf_select_begin = {
	set_temp_variable = { _wa_bf_break_ = 0 }
	set_temp_variable = { _wa_bf_rank_ = 0 }
	set_temp_variable = { _wa_bf_a_ = 0 }
	set_temp_variable = { _wa_bf_b_ = 0 }
	set_temp_variable = { _wa_bf_c_ = 0 }
	set_temp_variable = { _wa_bf_d_ = 0 }
	# found flags: THIS.id is a scope-encoded reference and can be negative, so the id
	# temporaries above are only ever used with var: / is_in_array, never compared numerically.
	set_temp_variable = { _wa_bf_a_found_ = 0 }
	set_temp_variable = { _wa_bf_b_found_ = 0 }
	set_temp_variable = { _wa_bf_c_found_ = 0 }
	set_temp_variable = { _wa_bf_d_found_ = 0 }
}

# THIS = an ELIGIBLE state (the caller has already applied the type's availability limit).
# Inputs (temp): _wa_bf_depth_ok_ (0/1), _wa_bf_is_picked_ (0/1), _wa_bf_rank_ (eligible states seen
# so far), WA_AI_bf_k. Records the first state of each tier (A/B/C/D above) by id; a tier-A hit
# also raises _wa_bf_break_ so the walk stops. Increments the rank.
WA_AI_bf_consider = {
	if = { limit = { check_variable = { _wa_bf_rank_ < WA_AI_bf_k } }
		if = { limit = { check_variable = { _wa_bf_depth_ok_ = 1 } }
			if = { limit = { check_variable = { _wa_bf_is_picked_ = 0 } check_variable = { _wa_bf_a_found_ = 0 } }
				set_temp_variable = { _wa_bf_a_ = THIS.id }
				set_temp_variable = { _wa_bf_a_found_ = 1 }
				set_temp_variable = { _wa_bf_break_ = 1 }
			}
			if = { limit = { check_variable = { _wa_bf_b_found_ = 0 } } set_temp_variable = { _wa_bf_b_ = THIS.id } set_temp_variable = { _wa_bf_b_found_ = 1 } }
		}
		if = { limit = { check_variable = { _wa_bf_is_picked_ = 0 } check_variable = { _wa_bf_c_found_ = 0 } } set_temp_variable = { _wa_bf_c_ = THIS.id } set_temp_variable = { _wa_bf_c_found_ = 1 } }
	}
	if = { limit = { check_variable = { _wa_bf_d_found_ = 0 } } set_temp_variable = { _wa_bf_d_ = THIS.id } set_temp_variable = { _wa_bf_d_found_ = 1 } }
	add_to_temp_variable = { _wa_bf_rank_ = 1 }
}

# Country scope, after the walk. Picks the best tier into _wa_bf_target_ (scope-encoded state id, only
# ever used with var:) and _wa_bf_target_found_ (0 = nothing eligible), sets _wa_bf_clear_ = 1 when the picked set must restart (tiers B and D), and
# _wa_bf_tier_ (1-4) for telemetry.
WA_AI_bf_choose = {
	set_temp_variable = { _wa_bf_target_ = 0 }
	set_temp_variable = { _wa_bf_target_found_ = 0 }
	set_temp_variable = { _wa_bf_clear_ = 0 }
	set_temp_variable = { _wa_bf_tier_ = 0 }
	if = { limit = { check_variable = { _wa_bf_a_found_ = 1 } }
		set_temp_variable = { _wa_bf_target_ = _wa_bf_a_ }
		set_temp_variable = { _wa_bf_target_found_ = 1 }
		set_temp_variable = { _wa_bf_tier_ = 1 }
	}
	else_if = { limit = { check_variable = { _wa_bf_b_found_ = 1 } }
		set_temp_variable = { _wa_bf_target_ = _wa_bf_b_ }
		set_temp_variable = { _wa_bf_target_found_ = 1 }
		set_temp_variable = { _wa_bf_clear_ = 1 }
		set_temp_variable = { _wa_bf_tier_ = 2 }
	}
	else_if = { limit = { check_variable = { _wa_bf_c_found_ = 1 } }
		set_temp_variable = { _wa_bf_target_ = _wa_bf_c_ }
		set_temp_variable = { _wa_bf_target_found_ = 1 }
		set_temp_variable = { _wa_bf_tier_ = 3 }
	}
	else_if = { limit = { check_variable = { _wa_bf_d_found_ = 1 } }
		set_temp_variable = { _wa_bf_target_ = _wa_bf_d_ }
		set_temp_variable = { _wa_bf_target_found_ = 1 }
		set_temp_variable = { _wa_bf_clear_ = 1 }
		set_temp_variable = { _wa_bf_tier_ = 4 }
	}
}

# Country scope, after a successful selection add. Telemetry only.
# tlm: WA_TLM_sq_tier_a_n / _b_n / _c_n / _d_n (standing, family sq) - which tier placed the add;
# d_n > 0 means breadth was exhausted (every top-K candidate over depth AND already picked, or
# nothing eligible inside the top K). Consumers: checklist R53, R49.
WA_AI_bf_record = {
	if = { limit = { check_variable = { _wa_bf_tier_ = 1 } } add_to_variable = { WA_TLM_sq_tier_a_n = 1 } }
	else_if = { limit = { check_variable = { _wa_bf_tier_ = 2 } } add_to_variable = { WA_TLM_sq_tier_b_n = 1 } }
	else_if = { limit = { check_variable = { _wa_bf_tier_ = 3 } } add_to_variable = { WA_TLM_sq_tier_c_n = 1 } }
	else_if = { limit = { check_variable = { _wa_bf_tier_ = 4 } } add_to_variable = { WA_TLM_sq_tier_d_n = 1 } }
	if = { limit = { check_variable = { WA_AI_bf_k > WA_TLM_sq_k_max } } set_variable = { WA_TLM_sq_k_max = WA_AI_bf_k } }
}

# civilian factories
WA_AI_queue_CIC = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	# [pc-queue] breadth-first placement - one selection walk, four tiers (see WA_AI_bf_consider).
	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				WA_AI_available_SHARED = yes
				infrastructure > 3
				WA_AI_available_CIC = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_CIC = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_CIC value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_CIC }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_CIC = yes }
		add_to_temp_array = { WA_AI_bf_picked_CIC = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}

	if = { limit = { check_variable = { break = 0 } }

		WA_AI_queue_INF = yes
	}
}
WA_AI_queue_CIC_2 = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				WA_AI_available_SHARED = yes
				WA_AI_available_CIC = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_CIC = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_CIC value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_CIC }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_CIC = yes }
		add_to_temp_array = { WA_AI_bf_picked_CIC = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}
}

# military factories
# [overextension-brake] when the country is industrially overextended (WA_AI_industry_overextended,
# the hysteresis flag maintained monthly by WA_AI_update_industry_overextension in
# WA_AI_misc_effects.txt) additional MICs buy ~zero throughput, so substitute instead of queueing the
# MIC - see WA_AI_overext_substitute_MIC below. All callers are safe to substitute at this funnel:
# the WA_AI_C.0 startup bootstrap (the flag cannot be set before the first monthly evaluation, and a
# mid-game re-fire under overextension SHOULD substitute), the WA_AI_C.10 recurring construction
# branches, and the WA_AI_queue_NIC no-coastal-slot fallback below.
WA_AI_queue_MIC = {

	if = { limit = { WA_AI_industry_overextended = yes }

		WA_AI_overext_substitute_MIC = yes
	}
	else = {

		if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

			WA_AI_get_shared_slot_scores = yes
		}

		WA_AI_queue_breadth_prepare = yes
		WA_AI_bf_select_begin = yes
		for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
			if = {
				limit = {
					WA_AI_available_SHARED = yes
					infrastructure > 3
					WA_AI_available_MIC = yes # [pc-queue] state cap incl. queued
				}
				set_temp_variable = { _wa_bf_depth_ok_ = 0 }
				if = { limit = { WA_AI_queue_depth_ok_MIC = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
				set_temp_variable = { _wa_bf_is_picked_ = 0 }
				if = { limit = { is_in_array = { array = WA_AI_bf_picked_MIC value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
				WA_AI_bf_consider = yes
			}
		}
		WA_AI_bf_choose = yes
		if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_MIC }
		if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
			var:_wa_bf_target_ = { WA_AI_add_MIC = yes }
			add_to_temp_array = { WA_AI_bf_picked_MIC = _wa_bf_target_ }
			WA_AI_bf_record = yes
			set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
		}

		if = { limit = { check_variable = { break = 0 } }

			WA_AI_queue_INF = yes
		}
	}
}

# [overextension-brake] substitution cascade for WA_AI_queue_MIC under industry overextension.
# THIS = ROOT = country (same scope contract as WA_AI_queue_MIC).
# Refinery leg gates: synth tech researched; coal self-sufficiency (same form as
# WA_AI_build_refinery_resource_shortage_rubber - refineries eat 60 coal/level, so only build on
# domestic coal); economy law at war_economy or above (WA ladder in common/ideas/_economic.txt).
# Dedup: the refinery leg is skipped while a type-8 (synthetic refinery) project already sits in
# WA_AI_PC_queue, so the cascade cannot pile on top of
# WA_AI_build_refinery_resource_shortage_rubber's own type-8 queueing; the per-state same-type check
# inside WA_AI_PC_can_build_project still applies below. If the refinery leg starts nothing (queue
# unchanged - no eligible state) it falls through to the CIC leg, so a substituted call never builds
# less than WA_AI_queue_CIC itself guarantees (which has its own INF fallback).
# Debug counters (persistent country variables, savegame-visible):
#   WA_AI_overext_dbg_mic_redirects_refinery, WA_AI_overext_dbg_mic_redirects_cic
WA_AI_overext_substitute_MIC = {

	set_temp_variable = { _overext_queued_ = 0 }

	if = {
		limit = {
			has_tech = synth_oil_experiments

			# input self-sufficiency (same test as WA_AI_build_refinery_resource_shortage_rubber)
			check_variable = { resource@coal > 50 }
			check_variable = { resource_imported@coal < 20 }

			# economy law at or above war economy
			OR = {
				has_idea = war_economy
				has_idea = tot_economic_mobilisation
				has_idea = over_mobilisation
			}

			# dedup: no synthetic refinery (type 8) already in the PC queue
			NOT = {
				any_of = { array = WA_AI_PC_queue value = v_overext_
					check_variable = { WA_AI_PC_building_type^v_overext_ = 8 }
				}
			}
		}

		set_temp_variable = { _overext_q_before_ = WA_AI_PC_queue^num }
		WA_AI_priority_queue_synthetic_refinery = yes

		if = { limit = { check_variable = { WA_AI_PC_queue^num > _overext_q_before_ } }

			set_temp_variable = { _overext_queued_ = 1 }
			add_to_variable = { WA_AI_overext_dbg_mic_redirects_refinery = 1 }
			if = { limit = { has_country_flag = WA_AI_construction_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSTRUCTION: overextended - MIC redirected to synthetic refinery" }
		}
	}

	if = { limit = { check_variable = { _overext_queued_ = 0 } }

		WA_AI_queue_CIC = yes
		add_to_variable = { WA_AI_overext_dbg_mic_redirects_cic = 1 }
		if = { limit = { has_country_flag = WA_AI_construction_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSTRUCTION: overextended - MIC redirected to CIC" }
	}
}

WA_AI_queue_MIC_2 = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				WA_AI_available_SHARED = yes
				WA_AI_available_MIC = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_MIC = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_MIC value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_MIC }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_MIC = yes }
		add_to_temp_array = { WA_AI_bf_picked_MIC = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}
}

# dockayrds
WA_AI_queue_NIC = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				is_coastal = yes
				WA_AI_available_SHARED = yes
				WA_AI_available_NIC = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_NIC = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_NIC value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_NIC }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_NIC = yes }
		add_to_temp_array = { WA_AI_bf_picked_NIC = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}

	if = { limit = { check_variable = { break = 0 } }

		WA_AI_queue_MIC = yes
	}
}

# refineries
WA_AI_queue_REF = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				# is_core_of = ROOT
				WA_AI_available_REF = yes
				WA_AI_available_SHARED = yes
				WA_AI_available_REF = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_REF = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_REF value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_REF }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_REF = yes }
		add_to_temp_array = { WA_AI_bf_picked_REF = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}

	if = { # replace MIC if slots are maxed out
		limit = {
			check_variable = { break = 0 }
			has_tech = expanded_industry6
		}

		for_each_scope_loop = { array = WA_AI_shared_slot_scores

			if = {
				limit = {
					# is_core_of = ROOT
					arms_factory > 0
					synthetic_refinery < 3
					NOT = { has_state_flag = WA_AI_mic_replaced_with_ref }
					ROOT = { has_full_control_of_state = PREV }
				}

				remove_building = { type = arms_factory level = 1 }
				if = { limit = { WA_AI_available_REF = yes }
					set_state_flag = { flag = WA_AI_mic_replaced_with_ref value = 1 days = 180 }
					WA_AI_add_REF = yes
					set_temp_variable = { break = 1 }

					if = { limit = { ROOT = { has_country_flag = WA_AI_logging } } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSTRUCTION: replacing MIC with REF in [This.GetName]" }
				}
				else = { add_building_construction = { type = arms_factory level = 1 instant_build = yes } }
			}
		}
	}
}

# silos
WA_AI_queue_SILO = {

	if = { limit = { has_tech = fuel_silos }

		if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

			WA_AI_get_shared_slot_scores = yes
		}

		# [silo-breadth] breadth-first placement - one selection walk, four tiers (see WA_AI_bf_consider).
		WA_AI_queue_breadth_prepare = yes
		WA_AI_bf_select_begin = yes
		for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
			if = {
				limit = {
					# is_core_of = ROOT
					WA_AI_available_SILO = yes
					WA_AI_available_SHARED = yes
				}
				set_temp_variable = { _wa_bf_depth_ok_ = 0 }
				if = { limit = { WA_AI_queue_depth_ok_SILO = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
				set_temp_variable = { _wa_bf_is_picked_ = 0 }
				if = { limit = { is_in_array = { array = WA_AI_bf_picked_SILO value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
				WA_AI_bf_consider = yes
			}
		}
		WA_AI_bf_choose = yes
		if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_SILO }
		if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
			var:_wa_bf_target_ = { WA_AI_add_SILO = yes }
			add_to_temp_array = { WA_AI_bf_picked_SILO = _wa_bf_target_ }
			WA_AI_bf_record = yes
			set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
		}
	}
}

# infrastructure
WA_AI_queue_INF = {

	if = { limit = { check_variable = { WA_AI_has_infrastructure_slot_scores = 0 } }

		WA_AI_get_infrastructure_slot_scores = yes
	}

	for_each_scope_loop = { array = WA_AI_infrastructure_slot_scores

		if = {
			limit = {
				WA_AI_available_INF = yes
				NOT = { has_state_flag = WA_AI_inf_queued }
			}

			WA_AI_add_INF = yes
			set_temp_variable = { break = 1 }
			set_state_flag = { flag = WA_AI_inf_queued value = 1 days = 1 }
		}
	}
}

# infrastructure for resources
WA_AI_queue_INF_resource = {

	if = { limit = { check_variable = { WA_AI_has_resource_slot_scores = 0 } }

		WA_AI_get_resource_slot_scores = yes
	}

	# [refineries] one vanilla infrastructure add per call, never a PC project. The PC route already
	# has an owner: the dispatcher strategy WA_AI_resource_extraction (every 2 days, floored civ gate,
	# needs = 3) calls WA_AI_priority_queue_INF_resource itself, and double-admitting here meant this
	# function skipped the vanilla add its caller asked for. WA_AI_C.10 fires only with spare vanilla
	# civs, which is exactly the regime a vanilla add is for.
	for_each_scope_loop = { array = WA_AI_resource_slot_scores

		if = {
			limit = {
				WA_AI_available_INF = yes
			}

			WA_AI_add_INF = yes
			set_temp_variable = { break = 1 }
		}
	}
}
# PC resource-state infrastructure. Sole caller: WA_AI_resource_extraction (dispatcher).
WA_AI_priority_queue_INF_resource = {


	if = { limit = { check_variable = { WA_AI_has_resource_slot_scores = 0 } }

		WA_AI_get_resource_slot_scores = yes
	}

	set_temp_variable = { _project_building_type = 1 }
	set_temp_variable = { _project_province_id = 0 }
	set_temp_variable = { _project_priority = constant:wa_ai_pc.prio.default }
	set_temp_variable = { _project_queue_num = 1 }
	# [refineries] explicit budget, scoped to its own type_id: WA_AI_build_supply_line also creates
	# building type 1 (type_id 24), and unscoped the two budgets counted each other's projects.
	# _project_queue_max_scoped is clear-on-read in WA_AI_PC_start_project (armed right before the
	# single call below); _project_type_id persists and is retired at the end of this function.
	set_temp_variable = { _project_queue_max = constant:wa_ai_pc.qmax.inf_resource }
	set_temp_variable = { _project_type_id = constant:wa_ai_pc.type_id.inf_resource }
	set_temp_variable = { _project_target_state = 0 }
	set_temp_variable = { _project_target_level = 0 }

	for_each_scope_loop = { array = WA_AI_resource_slot_scores

		if = { limit = { WA_AI_PC_can_build_project = yes }

			set_temp_variable = { _project_queue_max_scoped = 1 }
			WA_AI_PC_start_project = yes
			set_temp_variable = { break = 1 }
		}
	}

	set_temp_variable = { _project_type_id = 0 }
}

#WA refineries

WA_AI_queue_HSR = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				# is_core_of = ROOT
				WA_AI_available_HSR = yes
				WA_AI_available_SHARED = yes
				WA_AI_available_HSR = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_HSR = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_HSR value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_HSR }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_HSR = yes }
		add_to_temp_array = { WA_AI_bf_picked_HSR = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}

	if = { # replace MIC if slots are maxed out
		limit = {
			check_variable = { break = 0 }
			has_tech = expanded_industry6
		}

		for_each_scope_loop = { array = WA_AI_shared_slot_scores

			if = {
				limit = {
					# is_core_of = ROOT
					arms_factory > 0
					hydro_steel_refinery < 3
					NOT = { has_state_flag = WA_AI_mic_replaced_with_hsr }
					ROOT = { has_full_control_of_state = PREV }
				}

				remove_building = { type = arms_factory level = 1 }
				if = { limit = { WA_AI_available_HSR = yes }
					set_state_flag = { flag = WA_AI_mic_replaced_with_hsr value = 1 days = 180 }
					WA_AI_add_HSR = yes
					set_temp_variable = { break = 1 }

					if = { limit = { ROOT = { has_country_flag = WA_AI_logging } } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSTRUCTION: replacing MIC with HSR in [This.GetName]" }
				}
				else = { add_building_construction = { type = arms_factory level = 1 instant_build = yes } }
			}
		}
	}
}

WA_AI_queue_SR = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				# is_core_of = ROOT
				WA_AI_available_SR = yes
				WA_AI_available_SHARED = yes
				WA_AI_available_SR = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_SR = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_SR value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_SR }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_SR = yes }
		add_to_temp_array = { WA_AI_bf_picked_SR = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}

	if = { # replace MIC if slots are maxed out
		limit = {
			check_variable = { break = 0 }
			has_tech = expanded_industry6
		}

		for_each_scope_loop = { array = WA_AI_shared_slot_scores

			if = {
				limit = {
					# is_core_of = ROOT
					arms_factory > 0
					steel_refinery < 3
					NOT = { has_state_flag = WA_AI_mic_replaced_with_SR }
					ROOT = { has_full_control_of_state = PREV }
				}

				remove_building = { type = arms_factory level = 1 }
				if = { limit = { WA_AI_available_SR = yes }
					set_state_flag = { flag = WA_AI_mic_replaced_with_SR value = 1 days = 180 }
					WA_AI_add_SR = yes
					set_temp_variable = { break = 1 }

					if = { limit = { ROOT = { has_country_flag = WA_AI_logging } } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSTRUCTION: replacing MIC with SR in [This.GetName]" }
				}
				else = { add_building_construction = { type = arms_factory level = 1 instant_build = yes } }
			}
		}
	}
}

WA_AI_queue_HAR = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				# is_core_of = ROOT
				WA_AI_available_HAR = yes # [refineries] HAR's own slot test, not REF
				WA_AI_available_SHARED = yes
				WA_AI_available_HAR = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_HAR = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_HAR value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_HAR }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_HAR = yes }
		add_to_temp_array = { WA_AI_bf_picked_HAR = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}

	if = { # replace MIC if slots are maxed out
		limit = {
			check_variable = { break = 0 }
			has_tech = expanded_industry6
		}

		for_each_scope_loop = { array = WA_AI_shared_slot_scores

			if = {
				limit = {
					# is_core_of = ROOT
					arms_factory > 0
					hydro_aluminium_refinery < 3
					NOT = { has_state_flag = WA_AI_mic_replaced_with_har }
					ROOT = { has_full_control_of_state = PREV }
				}

				remove_building = { type = arms_factory level = 1 }
				if = { limit = { WA_AI_available_HAR = yes }
					set_state_flag = { flag = WA_AI_mic_replaced_with_HAR value = 1 days = 180 }
					WA_AI_add_HAR = yes
					set_temp_variable = { break = 1 }

					if = { limit = { ROOT = { has_country_flag = WA_AI_logging } } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSTRUCTION: replacing MIC with HAR in [This.GetName]" }
				}
				else = { add_building_construction = { type = arms_factory level = 1 instant_build = yes } }
			}
		}
	}
}

WA_AI_queue_AR = {

	if = { limit = { check_variable = { WA_AI_has_shared_slot_scores = 0 } }

		WA_AI_get_shared_slot_scores = yes
	}

	WA_AI_queue_breadth_prepare = yes
	WA_AI_bf_select_begin = yes
	for_each_scope_loop = { array = WA_AI_shared_slot_scores break = _wa_bf_break_
		if = {
			limit = {
				# is_core_of = ROOT
				WA_AI_available_AR = yes
				WA_AI_available_SHARED = yes
				WA_AI_available_AR = yes # [pc-queue] state cap incl. queued
			}
			set_temp_variable = { _wa_bf_depth_ok_ = 0 }
			if = { limit = { WA_AI_queue_depth_ok_AR = yes } set_temp_variable = { _wa_bf_depth_ok_ = 1 } }
			set_temp_variable = { _wa_bf_is_picked_ = 0 }
			if = { limit = { is_in_array = { array = WA_AI_bf_picked_AR value = THIS.id } } set_temp_variable = { _wa_bf_is_picked_ = 1 } }
			WA_AI_bf_consider = yes
		}
	}
	WA_AI_bf_choose = yes
	if = { limit = { check_variable = { _wa_bf_clear_ = 1 } } clear_temp_array = WA_AI_bf_picked_AR }
	if = { limit = { check_variable = { _wa_bf_target_found_ = 1 } }
		var:_wa_bf_target_ = { WA_AI_add_AR = yes }
		add_to_temp_array = { WA_AI_bf_picked_AR = _wa_bf_target_ }
		WA_AI_bf_record = yes
		set_temp_variable = { break = 1 } # downstream contract (queue_INF fallback, MIC-replacement legs)
	}

	if = { # replace MIC if slots are maxed out
		limit = {
			check_variable = { break = 0 }
			has_tech = expanded_industry6
		}

		for_each_scope_loop = { array = WA_AI_shared_slot_scores

			if = {
				limit = {
					# is_core_of = ROOT
					arms_factory > 0
					aluminium_refinery < 3
					NOT = { has_state_flag = WA_AI_mic_replaced_with_AR }
					ROOT = { has_full_control_of_state = PREV }
				}

				remove_building = { type = arms_factory level = 1 }
				if = { limit = { WA_AI_available_AR = yes }
					set_state_flag = { flag = WA_AI_mic_replaced_with_AR value = 1 days = 180 }
					WA_AI_add_AR = yes
					set_temp_variable = { break = 1 }

					if = { limit = { ROOT = { has_country_flag = WA_AI_logging } } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSTRUCTION: replacing MIC with AR in [This.GetName]" }
				}
				else = { add_building_construction = { type = arms_factory level = 1 instant_build = yes } }
			}
		}
	}
}

# radar
WA_AI_queue_RADAR = {

	if = { limit = { has_tech = radio_detection }

		if = { limit = { check_variable = { WA_AI_has_radar_slot_scores = 0 } }

			WA_AI_get_radar_slot_scores = yes
		}

		for_each_scope_loop = { array = WA_AI_radar_slot_scores

			if = {
				limit = {
					WA_AI_available_RADAR = yes
					NOT = { has_state_flag = WA_AI_radar_queued }
				}

				WA_AI_add_RADAR = yes
				set_temp_variable = { break = 1 }
				set_state_flag = { flag = WA_AI_radar_queued value = 1 days = 1 }
			}
		}
	}
}

# airbase
WA_AI_queue_AIR = {

	if = { limit = { check_variable = { WA_AI_has_airbase_slot_scores = 0 } }

		WA_AI_get_airbase_slot_scores = yes
	}

	for_each_scope_loop = { array = WA_AI_airbase_slot_scores

		if = {
			limit = {
				WA_AI_available_AIR = yes
			}

			WA_AI_add_AIR = yes
			set_temp_variable = { break = 1 }
		}
	}
}

# antiair
WA_AI_queue_AA = {

	if = { limit = { check_variable = { WA_AI_has_antiair_slot_scores = 0 } }

		WA_AI_get_antiair_slot_scores = yes
	}

	for_each_scope_loop = { array = WA_AI_antiair_slot_scores

		if = {
			limit = {
				WA_AI_available_AA = yes
				NOT = { has_state_flag = WA_AI_aa_queued }
			}

			WA_AI_add_AA = yes
			set_temp_variable = { break = 1 }
			set_state_flag = { flag = WA_AI_aa_queued value = 1 days = 1 }
		}
	}
}

# mil to civ conversion
WA_AI_priority_convert_MIC_to_CIC = {

	# [refineries] the brake on industry conversion is the explicit budget below, nothing else.
	# Callers: the MIC->CIC else_if legs of WA_AI_build_refinery_resource_shortage_steel / _aluminium
	# (WA_AI_CONSTRUCTION_PRIORITY_strategies.txt), reached only with shortage_months = 3, mil share
	# > 0.4 and no free shared slot anywhere. What bounds conversion:
	#   - constant:wa_ai_pc.qmax.mic_to_cic = 1 : ONE type-9 project in flight per country, across both
	#     callers (unscoped budget over building type 9, whose only creator is this function);
	#   - the MIC is removed at COMPLETION (WA_AI_PC_complete_project), never at admission - a queued
	#     project that the fill never funds removes nothing;
	#   - band 100 (constant:wa_ai_pc.prio.default): on a saturated major (pool eaten by rail/air) it
	#     never funds - it sits 30 weeks, the stall sweep drops it, the leg re-queues it 2 days later,
	#     forever: a permanent 1-slot clutter, MIC count unchanged. Do NOT read this leg as the
	#     industry-overextension lever - it is inert exactly where overextension lives.
	#   - on a slack country: funded at <= 20 civs, ~80 days per 8000-IC conversion, so at most one
	#     MIC -> CIC per ~3 months while the three caller conditions persist; each conversion lowers the
	#     mil share toward the 0.4 floor, which ends it.
	# Sets _project_queue_max and zeroes target_state / target_level / type_id explicitly: these temps
	# are shared with every other PC caller, so an inherited value silently rebudgets or redirects the
	# project - same leak hygiene as the refinery functions below.
	if = { limit = { check_variable = { WA_AI_has_mil_to_civ_conversion_slot_scores = 0 } }

		WA_AI_get_mil_to_civ_conversion_slot_scores = yes
	}

	set_temp_variable = { _project_building_type = 9 }
	set_temp_variable = { _project_province_id = 0 }
	set_temp_variable = { _project_priority = constant:wa_ai_pc.prio.default }
	set_temp_variable = { _project_queue_num = 1 }
	set_temp_variable = { _project_queue_max = constant:wa_ai_pc.qmax.mic_to_cic }
	set_temp_variable = { _project_target_state = 0 }
	set_temp_variable = { _project_target_level = 0 }
	set_temp_variable = { _project_type_id = 0 }

	for_each_scope_loop = { array = WA_AI_mil_to_civ_conversion_slot_scores

		if = { limit = { WA_AI_PC_can_build_project = yes }

			WA_AI_PC_start_project = yes
			set_temp_variable = { break = 1 }
		}
	}
}

# [refineries] shared by all five refinery queue functions below (types 8 synthetic / 11 hydro steel /
# 12 hydro aluminium / 15 steel / 16 aluminium; mapping per WA_AI_PC_get_build_speed and
# WA_AI_PC_get_building_cost in WA_AI_CONSTRUCTION_PRIORITY_core.txt).
# THIS = ROOT = country for all five (called from WA_AI_build_refinery_resource_shortage_* and, for
# the synthetic one, from WA_AI_overext_substitute_MIC).
#
# 1. FILTERED DRAW. The eligibility tests live in `limit`, the engine's own filtered form (same shape
#    as the radar strategy in WA_AI_CONSTRUCTION_PRIORITY_strategies.txt), never in an inner `if`: a
#    bare random_scope_in_array over controlled_states draws ONE state per pulse and then usually
#    discards it, so the hit rate is (eligible states / controlled states) - under 9% for a major
#    holding tens of controlled states with a handful of buildable ones.
#
# 2. EXPLICIT _project_queue_max = 1, plus zeroed _project_target_state / _project_target_level.
#    These temps are shared with every other PC caller: without the explicit set,
#    WA_AI_PC_start_project inherits whatever ran last (3 from the two air strategies, 5 from the
#    railway core, 0 = unlimited from the railway helpers), and a stale non-zero target_state
#    redirects the project to an entirely unrelated state. One refinery project of a given type in
#    flight per country is the intended budget.
#
# 3. TWO-PASS SITE SELECTION. Pass 1 keeps the home-area criterion (WA_AI_refinery_region_priority - a
#    thin wrapper over WA_AI_region_priority, so SOV/CHI keep their central-Russia / western-China
#    rule). If pass 1 queued nothing - detected by queue growth, the same idiom
#    [overextension-brake] uses in WA_AI_overext_substitute_MIC above - pass 2 retries over the
#    widened set (WA_AI_refinery_region_priority_wide: ROOT-owned and not on a hostile border). A
#    saturated home area otherwise means a country never places a refinery at all.
#
# Contract preserved for [overextension-brake] a successful queue is still visible as growth of
# WA_AI_PC_queue within the same call, so WA_AI_overext_substitute_MIC's before/after delta around
# WA_AI_priority_queue_synthetic_refinery still detects success (both passes end in
# WA_AI_PC_start_project, which is what grows it).
#
# There is deliberately NO admission-count gate here or in the three strategies that call these
# functions. What bounds them: _project_queue_max = 1 per type (at most five refinery projects of any
# kind in flight per country, ~100 civs), the shortage_months = 3 requirement, the coal guard,
# WA_AI_PC_has_project_civs_20, and the sorted fill (band 250 sits below air 300 and rail 500/1000,
# above the default band). Every default-band strategy carries its own explicit _project_queue_max.

WA_AI_priority_queue_synthetic_refinery = {


	set_temp_variable = { _project_building_type = 8 }
	set_temp_variable = { _project_province_id = 0 }
	# [pc-queue] strategic band (250) - a shortage-driven refinery outranks default-100
	# projects but never a war railway; band table in WA_AI_CONSTRUCTION_PRIORITY_core.txt
	set_temp_variable = { _project_priority = constant:wa_ai_pc.prio.strategic }
	set_temp_variable = { _project_queue_num = 1 }
	# [refineries] explicit bounds / leak hygiene - see the header note above
	set_temp_variable = { _project_queue_max = 1 }
	set_temp_variable = { _project_target_state = 0 }
	set_temp_variable = { _project_target_level = 0 }

	set_temp_variable = { _refinery_q_before_ = WA_AI_PC_queue^num }

	# [refineries] pass 1 - preferred sites (filtered draw)
	random_scope_in_array = { array = controlled_states
		limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority = yes }

		WA_AI_PC_start_project = yes
		set_temp_variable = { break = 1 }
	}

	# [refineries] pass 2 - widened sites, only when pass 1 queued nothing
	if = { limit = { NOT = { check_variable = { WA_AI_PC_queue^num > _refinery_q_before_ } } }

		random_scope_in_array = { array = controlled_states
			limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority_wide = yes }

			WA_AI_PC_start_project = yes
			set_temp_variable = { break = 1 }
		}
	}
}

WA_AI_priority_queue_hydro_steel_refinery = {


	set_temp_variable = { _project_building_type = 11 }
	set_temp_variable = { _project_province_id = 0 }
	# [pc-queue] strategic band (250) - see the synthetic-refinery note above
	set_temp_variable = { _project_priority = constant:wa_ai_pc.prio.strategic }
	set_temp_variable = { _project_queue_num = 1 }
	# [refineries] explicit bounds / leak hygiene - see the header note above
	set_temp_variable = { _project_queue_max = 1 }
	set_temp_variable = { _project_target_state = 0 }
	set_temp_variable = { _project_target_level = 0 }

	set_temp_variable = { _refinery_q_before_ = WA_AI_PC_queue^num }

	# [refineries] pass 1 - preferred sites (filtered draw)
	random_scope_in_array = { array = controlled_states
		limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority = yes }

		WA_AI_PC_start_project = yes
		set_temp_variable = { break = 1 }
	}

	# [refineries] pass 2 - widened sites, only when pass 1 queued nothing
	if = { limit = { NOT = { check_variable = { WA_AI_PC_queue^num > _refinery_q_before_ } } }

		random_scope_in_array = { array = controlled_states
			limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority_wide = yes }

			WA_AI_PC_start_project = yes
			set_temp_variable = { break = 1 }
		}
	}
}

WA_AI_priority_queue_hydro_aluminium_refinery = {


	set_temp_variable = { _project_building_type = 12 }
	set_temp_variable = { _project_province_id = 0 }
	# [pc-queue] strategic band (250) - see the synthetic-refinery note above
	set_temp_variable = { _project_priority = constant:wa_ai_pc.prio.strategic }
	set_temp_variable = { _project_queue_num = 1 }
	# [refineries] explicit bounds / leak hygiene - see the header note above
	set_temp_variable = { _project_queue_max = 1 }
	set_temp_variable = { _project_target_state = 0 }
	set_temp_variable = { _project_target_level = 0 }

	set_temp_variable = { _refinery_q_before_ = WA_AI_PC_queue^num }

	# [refineries] pass 1 - preferred sites (filtered draw)
	random_scope_in_array = { array = controlled_states
		limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority = yes }

		WA_AI_PC_start_project = yes
		set_temp_variable = { break = 1 }
	}

	# [refineries] pass 2 - widened sites, only when pass 1 queued nothing
	if = { limit = { NOT = { check_variable = { WA_AI_PC_queue^num > _refinery_q_before_ } } }

		random_scope_in_array = { array = controlled_states
			limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority_wide = yes }

			WA_AI_PC_start_project = yes
			set_temp_variable = { break = 1 }
		}
	}
}

WA_AI_priority_queue_steel_refinery = {


	set_temp_variable = { _project_building_type = 15 }
	set_temp_variable = { _project_province_id = 0 }
	# [pc-queue] strategic band (250) - see the synthetic-refinery note above
	set_temp_variable = { _project_priority = constant:wa_ai_pc.prio.strategic }
	set_temp_variable = { _project_queue_num = 1 }
	# [refineries] explicit bounds / leak hygiene - see the header note above
	set_temp_variable = { _project_queue_max = 1 }
	set_temp_variable = { _project_target_state = 0 }
	set_temp_variable = { _project_target_level = 0 }

	set_temp_variable = { _refinery_q_before_ = WA_AI_PC_queue^num }

	# [refineries] pass 1 - preferred sites (filtered draw)
	random_scope_in_array = { array = controlled_states
		limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority = yes }

		WA_AI_PC_start_project = yes
		set_temp_variable = { break = 1 }
	}

	# [refineries] pass 2 - widened sites, only when pass 1 queued nothing
	if = { limit = { NOT = { check_variable = { WA_AI_PC_queue^num > _refinery_q_before_ } } }

		random_scope_in_array = { array = controlled_states
			limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority_wide = yes }

			WA_AI_PC_start_project = yes
			set_temp_variable = { break = 1 }
		}
	}
}

WA_AI_priority_queue_aluminium_refinery = {


	set_temp_variable = { _project_building_type = 16 }
	set_temp_variable = { _project_province_id = 0 }
	# [pc-queue] strategic band (250) - see the synthetic-refinery note above
	set_temp_variable = { _project_priority = constant:wa_ai_pc.prio.strategic }
	set_temp_variable = { _project_queue_num = 1 }
	# [refineries] explicit bounds / leak hygiene - see the header note above
	set_temp_variable = { _project_queue_max = 1 }
	set_temp_variable = { _project_target_state = 0 }
	set_temp_variable = { _project_target_level = 0 }

	set_temp_variable = { _refinery_q_before_ = WA_AI_PC_queue^num }

	# [refineries] pass 1 - preferred sites (filtered draw)
	random_scope_in_array = { array = controlled_states
		limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority = yes }

		WA_AI_PC_start_project = yes
		set_temp_variable = { break = 1 }
	}

	# [refineries] pass 2 - widened sites, only when pass 1 queued nothing
	if = { limit = { NOT = { check_variable = { WA_AI_PC_queue^num > _refinery_q_before_ } } }

		random_scope_in_array = { array = controlled_states
			limit = { WA_AI_PC_can_build_project = yes WA_AI_refinery_region_priority_wide = yes }

			WA_AI_PC_start_project = yes
			set_temp_variable = { break = 1 }
		}
	}
}


######################################################

# for building INF to increase supply
WA_AI_queue_INF_supply = {
	if = {
		limit = {
			any_of_scopes = { array = controlled_states
				WA_AI_available_INF = yes
				free_building_slots = { building = infrastructure size > 5 include_locked = no }
				ROOT = { divisions_in_state = { state = PREV size > 5 } }
				any_neighbor_state = { CONTROLLER = { has_war_with = ROOT strength_ratio = { tag = ROOT ratio > 0.5 } } }
			}
		}

		random_scope_in_array = { array = controlled_states
			limit = {
				WA_AI_available_INF = yes
				free_building_slots = { building = infrastructure size > 5 include_locked = no }
				ROOT = { divisions_in_state = { state = PREV size > 5 } }
				any_neighbor_state = { CONTROLLER = { has_war_with = ROOT strength_ratio = { tag = ROOT ratio > 0.5 } } }
			}
			WA_AI_add_INF = yes
		}
	}
	else_if = {
		limit = {
			any_of_scopes = { array = controlled_states
				WA_AI_available_INF = yes
				free_building_slots = { building = infrastructure size > 5 include_locked = no }
				ROOT = { divisions_in_state = { state = PREV size > 5 } }
				any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } }
			}
		}
		random_scope_in_array = { array = controlled_states
			limit = {
				WA_AI_available_INF = yes
				free_building_slots = { building = infrastructure size > 5 include_locked = no }
				ROOT = { divisions_in_state = { state = PREV size > 5 } }
				any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } }
			}
			WA_AI_add_INF = yes
		}
	}
}

# build forts on victory points in a state
WA_AI_queue_FORT_CITIES = {

	random_scope_in_array = { array = controlled_states
		limit = {
			is_demilitarized_zone = no # [dmz-build-loss] bunker is disabled_in_dmz (see WA_AI_available_MIC)
			is_in_home_area = yes
			WA_AI_has_city = yes
			check_variable = { WA_AI_forts_constructed_on_cities_@ROOT < 3 }
		}

		WA_AI_add_FORT_CITY = yes
	}
}

# build forts on every border province of a state
WA_AI_queue_FORT_BORDER = {

	random_scope_in_array = { array = controlled_states
		limit = {
			is_controlled_by = ROOT
			is_demilitarized_zone = no # [dmz-build-loss] bunker is disabled_in_dmz (see WA_AI_available_MIC)
			NOT = { WA_AI_no_border = yes }
			is_in_home_area = yes
			check_variable = { WA_AI_forts_constructed_on_border_@ROOT < 3 }
		}

		WA_AI_add_FORT_BORDER = yes
	}
}

# build coastal forts on naval bases in a state
WA_AI_queue_CFORT_BASES = {

	random_scope_in_array = { array = controlled_states
		limit = {
			is_controlled_by = ROOT
			is_demilitarized_zone = no # [dmz-build-loss] coastal_bunker is disabled_in_dmz (see WA_AI_available_MIC)
			is_coastal = yes
			is_in_home_area = yes
			WA_AI_has_naval_base = yes
			check_variable = { WA_AI_forts_constructed_on_bases_@ROOT < 3 }
		}

		WA_AI_add_CFORT_BASE = yes
	}
}

# build coastal forts on every coastal province in a state
WA_AI_queue_CFORT_COAST = {

	random_scope_in_array = { array = controlled_states
		limit = {
			is_controlled_by = ROOT
			is_demilitarized_zone = no # [dmz-build-loss] coastal_bunker is disabled_in_dmz (see WA_AI_available_MIC)
			is_coastal = yes
			is_in_home_area = yes
			check_variable = { WA_AI_forts_constructed_on_coast_@ROOT < 3 }
		}

		WA_AI_add_CFORT_COAST = yes
	}
}

############################################################################
# Country-specific scripted effects for AI construction queueing
############################################################################

# Original build order (including infra) is in WA_AI_construction.txt, WA_AI_C.0
# Refineries-onward must be separated out because the USA does not start with synth tech
WA_AI_queue_USA_post_infra_build_order = {
	# Roughly Sept/Oct 1939, after infra and first alu mills finish

	# 30 refs - Trade law swaps to export focus
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes

	# 100 mils - Limited rearmament national spirit
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes
	WA_AI_queue_MIC = yes

	# 30 refs - For synthetic rubber program
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
	WA_AI_queue_REF = yes
}
