####################################################################################################
#	World Ablaze AI - repeatable-decision spending facts.
#	Read with `constant:wa_ai_decisions.<group>.<key>` from WA_AI_DECISIONS_has_spare_pp
#	(common/scripted_triggers/WA_AI_DECISIONS_triggers.txt), the one reader.
#	Full restart (not `reload`).
####################################################################################################

wa_ai_decisions = {
	schema = {
		any_key = yes
		data = {
			{
				any_key = yes
				data = fixed_point
			}
		}
	}

	# [repeatable-pp-decisions] political power an AI keeps in hand before it may spend on a
	# repeatable decision: below the floor, one-off choices (laws, advisors) get the PP first.
	pp = {
		spare_floor = 350		# PP
	}
}
