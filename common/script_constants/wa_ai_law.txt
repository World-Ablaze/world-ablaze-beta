####################################################################################################
#	World Ablaze AI - conscription law facts.
#	Read with `constant:wa_ai_law.<group>.<key>` from WA_AI_LAW_update_pool_floor
#	(common/scripted_effects/WA_AI_law_effects.txt), the one writer of WA_AI_LAW_pool_floor.
#	Full restart (not `reload`).
####################################################################################################

wa_ai_law = {
	schema = {
		any_key = yes
		data = {
			{
				any_key = yes
				data = fixed_point
			}
		}
	}

	# [sov-conscription-oscillation] the free manpower an AI keeps before climbing one conscription
	# rung: max(floor_min, army_share x deployed army manpower) - max, so a small army keeps the 250k
	# floor and a 5M army waits for 500k (was literal 500000 / 750000 / 1000000 per rung).
	pool = {
		floor_min  = 250000		# men
		army_share = 0.10		# share of deployed_army_manpower_k (engine variable, thousands)
	}
}
