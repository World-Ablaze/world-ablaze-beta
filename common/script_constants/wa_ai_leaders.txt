####################################################################################################
#	World Ablaze AI - scripted leader recruitment constants.
#	Read with `constant:wa_ai_leaders.<group>.<key>` from WA_AI_leader_recruitment_effects.
#	The same number is the last declared index in history/general/WA_AI_leader_pool.txt; the pair is
#	held equal by tools/constants_registry.json, not by a comment.
####################################################################################################

wa_ai_leaders = {
	schema = {
		any_key = yes
		data = {
			{
				any_key = yes
				data = fixed_point
			}
		}
	}

	# Declared character-template pool. Only a token declared in a parsed file resolves after a
	# reload; a leader given a token past the last declared one loses its template and its portrait.
	pool = {
		last_index = 40	# highest WA_AI_gen_<role>_N declared in history/general/WA_AI_leader_pool.txt
	}

}
