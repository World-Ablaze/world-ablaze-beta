

political_actions = {

	BUL_surrender_to_SOV = {

		icon = generic_form_nation

		allowed = {
			original_tag = BUL
		}

		available = {
			has_war_with = SOV
			is_subject = no
			has_capitulated = no
			threat > 0.75
			surrender_progress > 0.01
			OR = {
				899 = {
					CONTROLLER = {
						has_war_with = ROOT
						OR = {
							is_in_faction_with = SOV
							tag = SOV
						}
					}
				}
				900 = {
					CONTROLLER = {
						has_war_with = ROOT
						OR = {
							is_in_faction_with = SOV
							tag = SOV
						}
					}
				}
				81 = {
					CONTROLLER = {
						has_war_with = ROOT
						OR = {
							is_in_faction_with = SOV
							tag = SOV
						}
					}
				}
				108 = {
					CONTROLLER = {
						has_war_with = ROOT
						OR = {
							is_in_faction_with = SOV
							tag = SOV
						}
					}
				}
				898 = {
					CONTROLLER = {
						has_war_with = ROOT
						OR = {
							is_in_faction_with = SOV
							tag = SOV
						}
					}
				}
				184 = {
					CONTROLLER = {
						has_war_with = ROOT
						OR = {
							is_in_faction_with = SOV
							tag = SOV
						}
					}
				}
			}
		}

		visible = {
			tag = BUL
			has_war_with = SOV
			is_subject = no
			has_capitulated = no
		}

		cost = 0

		fire_only_once = yes

		complete_effect = {
			SOV = { country_event = { id = sov_armor.807 } }
		}

		ai_will_do = {
			factor = 200

			modifier = {
				factor = 0

				is_in_faction_with = GER
				has_war_with = SOV
				OR = {
					202 = {
						CONTROLLER = {
							is_in_faction_with = GER
						}
					}
					206 = {
						CONTROLLER = {
							is_in_faction_with = GER
						}
					}
				}
			}
		}
	}

	BUL_integrate_pirot = {

		icon = generic_political_discourse

		allowed = {
			original_tag = BUL
		}

		available = {
			851 = { is_fully_controlled_by = BUL }
			OR = {
				AND = {
					has_war_with = YUG
					YUG = { has_capitulated = yes }
				}
				NOT = { has_war_with = YUG }
				YUG = { exists = no }
			}
		}

		cost = 50

		fire_only_once = yes

		visible = {
			tag = BUL
			OR = {
				851 = { is_controlled_by = BUL }
				has_war_with = YUG
			}
		}

		complete_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			851 = { add_core_of = BUL }
		}

		ai_will_do = {
			factor = 200
		}
	}

	BUL_integrate_macedonia = {

		icon = generic_political_discourse

		allowed = {
			original_tag = BUL
		}

		available = {
			106 = { is_fully_controlled_by = BUL }
			OR = {
				AND = {
					has_war_with = YUG
					YUG = { has_capitulated = yes }
				}
				NOT = { has_war_with = YUG }
				YUG = { exists = no }
			}
		}

		cost = 50

		fire_only_once = yes

		visible = {
			tag = BUL
			OR = {
				106 = { is_controlled_by = BUL }
				has_war_with = YUG
			}
		}

		complete_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			106 = { add_core_of = BUL }
		}

		ai_will_do = {
			factor = 200
		}
	}

	BUL_integrate_thrace = {

		icon = generic_political_discourse

		allowed = {
			original_tag = BUL
		}

		available = {
			184 = { is_fully_controlled_by = BUL }
			OR = {
				AND = {
					has_war_with = GRE
					GRE = { has_capitulated = yes }
				}
				NOT = { has_war_with = GRE }
				GRE = { exists = no }
			}
		}

		cost = 50

		fire_only_once = yes

		visible = {
			tag = BUL
			OR = {
				184 = { is_controlled_by = BUL }
				has_war_with = GRE
			}
		}

		complete_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			184 = { add_core_of = BUL }
		}

		ai_will_do = {
			factor = 200
		}
	}

	BUL_integrate_central_macedonia = {

		icon = generic_political_discourse

		allowed = {
			original_tag = BUL
		}

		available = {
			731 = { is_fully_controlled_by = BUL }
			OR = {
				AND = {
					has_war_with = GRE
					GRE = { has_capitulated = yes }
				}
				NOT = { has_war_with = GRE }
				GRE = { exists = no }
			}
		}

		cost = 50

		fire_only_once = yes

		visible = {
			tag = BUL
			OR = {
				731 = { is_controlled_by = BUL }
				has_war_with = GRE
			}
		}

		complete_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			731 = { add_core_of = BUL }
		}

		ai_will_do = {
			factor = 200
		}
	}
}