FRA_internal_factions = {

	FRA_fulfill_the_renault_contract = {

		icon = generic_tank

		available = {
			custom_trigger_tooltip = {
				tooltip = FRA_fulfill_the_renault_contract_tt
				check_variable = { num_equipment_in_armies@infantry_support_tank_equipment > 899 }
			}
		}

		visible = {
			tag = FRA
		}

		days_mission_timeout = 1460
		is_good = yes
		fire_only_once = yes
		activation = {
			tag = FRA
		}

		complete_effect = {
			add_political_power = 200
			custom_effect_tooltip = FRA_fulfill_the_renault_contract_tt2
			hidden_effect = {
				add_to_variable = { var = FRA_French_Army_factory_output value = 2 }
				FRA_political_instability_update_effect = yes
			}
		}

		timeout_effect = {
		}

		ai_will_do = {
			factor = 10
		}
	}

	FRA_fulfill_the_hotchkiss_contract = {

		icon = generic_tank

		available = {
			if = {
				limit = {
					has_country_flag = wwi_equipment_2
				}
				custom_trigger_tooltip = {
					tooltip = FRA_fulfill_the_hotchkiss_contract_tt
					check_variable = { num_equipment_in armies@light_tank_equipment > 1899 }
				}
			}
			else_if = {
				limit = {
					has_country_flag = wwi_equipment_1
				}
				custom_trigger_tooltip = {
					tooltip = FRA_fulfill_the_hotchkiss_contract_tt
					check_variable = { num_equipment_in_armies@light_tank_equipment > 1399 }
				}
			}
			else = {
				custom_trigger_tooltip = {
					tooltip = FRA_fulfill_the_hotchkiss_contract_tt
					check_variable = { num_equipment_in_armies@light_tank_equipment > 899 }
				}
			}
		}

		visible = {
			tag = FRA
		}

		days_mission_timeout = 1460
		is_good = yes
		fire_only_once = yes
		activation = {
			tag = FRA
		}

		complete_effect = {
			add_political_power = 400
			custom_effect_tooltip = FRA_fulfill_the_renault_contract_tt2
			hidden_effect = {
				add_to_variable = { var = FRA_French_Army_factory_output value = 2 }
				FRA_political_instability_update_effect = yes
			}
		}

		timeout_effect = {
		}

		ai_will_do = {
			factor = 10
		}
	}
}