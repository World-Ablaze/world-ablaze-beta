political_actions = {
	PHI_dismantle_organized_guerrilla_activity = {
		icon = generic_police_action
		allowed = {
		}

		available = {
			army_manpower_in_state = { state = FROM amount > 19999 }
			divisions_in_state = {
				state = FROM
				size > 1
			}
			FROM = {
				has_dynamic_modifier = { modifier = PHI_organized_guerrilla_activity }
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_in_faction_with = ROOT
					}
				}
				NOT = { has_state_flag = PHI_rooting_out_guerrilla_activity }
			}
		}

		target_trigger = {
			FROM = {
				has_dynamic_modifier = { modifier = PHI_organized_guerrilla_activity }
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_in_faction_with = ROOT
					}
				}
			}
		}

		visible = {
			FROM = {
				has_dynamic_modifier = { modifier = PHI_organized_guerrilla_activity }
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_in_faction_with = ROOT
					}
				}
			}
		}

		cancel_trigger = {
			army_manpower_in_state = { state = FROM amount < 19999 }
			divisions_in_state = {
				state = FROM
				size < 2
			}
		}

		cost = 0

		fire_only_once = no
		custom_cost_trigger = {
			command_power > 4
		}
		custom_cost_text = custom_cost_5_cp

		ai_will_do = {
			factor = 400
		}

		days_remove = 120

		state_target = yes

		cancel_if_not_visible = yes

		on_map_mode = map_and_decisions_view

		complete_effect = {
			hidden_effect = {
				add_command_power = -5
			}
			FROM = {
				set_state_flag = PHI_rooting_out_guerrilla_activity
			}
		}

		cancel_effect = {
			FROM = {
				clr_state_flag = PHI_rooting_out_guerrilla_activity
			}
		}

		remove_effect = {
			army_experience = 10
			damage_units = {
				state = FROM
				damage = 0.2
				ratio = yes
				army = yes
			}
			FROM = {
				clr_state_flag = PHI_rooting_out_guerrilla_activity
				set_state_flag = PHI_dispersed_guerrilla_activity
				remove_dynamic_modifier = { modifier = PHI_organized_guerrilla_activity }
				add_dynamic_modifier = { modifier = PHI_dispersed_guerrilla_activity }
			}
		}
	}
	PHI_counter_guerrilla_activity = {
		icon = generic_police_action
		allowed = {
		}

		available = {
			army_manpower_in_state = { state = FROM amount > 9999 }
			divisions_in_state = {
				state = FROM
				size > 0
			}
			FROM = {
				has_dynamic_modifier = { modifier = PHI_dispersed_guerrilla_activity }
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_in_faction_with = ROOT
					}
				}
				NOT = { has_state_flag = PHI_rooting_out_guerrilla_activity }
			}
			has_war = no
		}

		target_trigger = {
			FROM = {
				has_dynamic_modifier = { modifier = PHI_dispersed_guerrilla_activity }
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_in_faction_with = ROOT
					}
				}
			}
		}

		visible = {
			FROM = {
				has_dynamic_modifier = { modifier = PHI_dispersed_guerrilla_activity }
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_in_faction_with = ROOT
					}
				}
			}
		}

		cancel_trigger = {
			army_manpower_in_state = { state = FROM amount < 9999 }
			divisions_in_state = {
				state = FROM
				size < 1
			}
		}

		cost = 0

		fire_only_once = no
		custom_cost_trigger = {
			command_power > 4
		}
		custom_cost_text = custom_cost_5_cp

		ai_will_do = {
			factor = 400
		}

		days_remove = 120

		state_target = yes

		cancel_if_not_visible = yes

		on_map_mode = map_and_decisions_view

		complete_effect = {
			hidden_effect = {
				add_command_power = -5
			}
			FROM = {
				set_state_flag = PHI_rooting_out_guerrilla_activity
			}
		}

		cancel_effect = {
			FROM = {
				clr_state_flag = PHI_rooting_out_guerrilla_activity
			}
		}

		remove_effect = {
			army_experience = 10
			FROM = {
				clr_state_flag = PHI_rooting_out_guerrilla_activity
				set_state_flag = PHI_guerrilla_activity_destroyed
				remove_dynamic_modifier = { modifier = PHI_dispersed_guerrilla_activity }
			}
		}
	}
}
