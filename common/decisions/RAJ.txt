
RAJ_the_british_raj = {

	RAJ_provincial_elections_mission = {

		icon = generic_political_rally

		available = {
			has_completed_focus = RAJ_provincial_elections
		}

		visible = {
			tag = RAJ
		}

		days_mission_timeout = 365
		is_good = no
		fire_only_once = yes
		activation = {
			tag = RAJ
		}

		complete_effect = {
		}

		timeout_effect = {
			add_stability = -0.3
		}

		ai_will_do = {
			factor = 1
		}
	}

	RAJ_quit_india_movement_mission = {

		icon = generic_political_rally

		available = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_quit_india_movement_mission_failed
			}
		}

		visible = {
			tag = RAJ
			has_completed_focus = RAJ_commit_to_the_war_effort
		}

		days_mission_timeout = 1780

		is_good = no
		fire_only_once = yes
		activation = {
			tag = RAJ
			has_completed_focus = RAJ_commit_to_the_war_effort
		}

		modifier = {
		}

		complete_effect = {
		}

		timeout_effect = {
			country_event = { id = raj_armor.812 }
		}

		ai_will_do = {
			factor = 1
		}
	}

	RAJ_assert_british_authority = {

		icon = generic_arrest

		available = {
			has_completed_focus = RAJ_curb_the_congress
			has_stability < 0.5
		}

		visible = {
			tag = RAJ
			has_completed_focus = RAJ_curb_the_congress
		}

		days_remove = 180
		days_re_enable = 70

		fire_only_once = no

		cost = 50

		ai_will_do = {
			factor = 4000
		}

		modifier = {
			stability_weekly = 0.002
		}

		complete_effect = {
			add_autonomy_ratio = {
				value = -0.05
			}
		}

		timeout_effect = {
		}
	}

	RAJ_clamp_down_on_anti_war_sentiment = {

		icon = eng_blackshirt_speech

		available = {
			has_completed_focus = RAJ_curb_the_congress
			has_war_support < 0.5
		}

		visible = {
			tag = RAJ
			has_completed_focus = RAJ_curb_the_congress
		}

		days_remove = 70
		days_re_enable = 70

		fire_only_once = no

		cost = 50

		ai_will_do = {
			factor = 4000
		}

		modifier = {
			war_support_weekly = 0.004
		}

		complete_effect = {
			add_autonomy_ratio = {
				value = -0.05
			}
		}

		timeout_effect = {
		}
	}
	
	RAJ_collaborate_with_the_hindu_mahasabha_FROM = {

		icon = aluminium

		allowed = {
			#original_tag = TUR
		}

		available = {
			FROM = {
				OR = {
					state = 432
					state = 431
					state = 435
					state = 993
					state = 438
					state = 436
					state = 429
					state = 424
					state = 423
					state = 991
					state = 992
					state = 958
				}
				NOT = { has_dynamic_modifier = { modifier = hm_state } }
			}
		}

		target_trigger = {
			FROM = {
				OR = {
					state = 432
					state = 431
					state = 435
					state = 993
					state = 438
					state = 436
					state = 429
					state = 424
					state = 423
					state = 991
					state = 992
					state = 958
				}
				NOT = { has_dynamic_modifier = { modifier = hm_state } }
			}
		}

		days_remove = 45

		cost = 30

		fire_only_once = no

		ai_will_do = {
			factor = 5
		}

		visible = {
			has_completed_focus = RAJ_collaborate_with_the_hindu_mahasabha
			is_subject = yes
			NOT = { has_autonomy_state = autonomy_dominion }
		}

		complete_effect = {
		}

		remove_effect = {
			FROM = { add_dynamic_modifier = { modifier = hm_state } }
		}
	}

	######### CIVIL UNREST
	
	RAJ_encourage_violence_in_assam = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_assam_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 432 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_assam_countered
				clr_country_flag = RAJ_violence_in_assam
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_assam
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				432 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_west_bengal = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_west_bengal_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 431 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_west_bengal_countered
				clr_country_flag = RAJ_violence_in_west_bengal
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_west_bengal
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				431 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_bihar = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_bihar_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 435 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_bihar_countered
				clr_country_flag = RAJ_violence_in_bihar
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_bihar
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				435 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_jharkhand = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_jharkhand_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 993 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_jharkhand_countered
				clr_country_flag = RAJ_violence_in_jharkhand
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_jharkhand
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				993 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_odisha = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_odisha_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 426 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_odisha_countered
				clr_country_flag = RAJ_violence_in_odisha
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_odisha
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				426 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_uttar_pradesh = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_uttar_pradesh_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 438 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_uttar_pradesh_countered
				clr_country_flag = RAJ_violence_in_uttar_pradesh
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_uttar_pradesh
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				438 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_jabalpur = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_jabalpur_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 436 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_jabalpur_countered
				clr_country_flag = RAJ_violence_in_jabalpur
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_jabalpur
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				436 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_bombay = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_bombay_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 429 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_bombay_countered
				clr_country_flag = RAJ_violence_in_bombay
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_bombay
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				429 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_telangana = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_telangana_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 427 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_telangana_countered
				clr_country_flag = RAJ_violence_in_telangana
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_telangana
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				427 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_mysore = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_mysore_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 425 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_mysore_countered
				clr_country_flag = RAJ_violence_in_mysore
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_mysore
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				425 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_madras = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_madras_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 424 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_madras_countered
				clr_country_flag = RAJ_violence_in_madras
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_madras
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				424 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_tamil_nadu = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_tamil_nadu_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 423 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_tamil_nadu_countered
				clr_country_flag = RAJ_violence_in_tamil_nadu
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_tamil_nadu
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				423 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_rajahsthan = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_rajahsthan_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 433 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_rajahsthan_countered
				clr_country_flag = RAJ_violence_in_rajahsthan
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_rajahsthan
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				433 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_gujarat = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_gujarat_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 428 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_gujarat_countered
				clr_country_flag = RAJ_violence_in_gujarat
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_gujarat
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				428 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_madhya_pradesh = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_madhya_pradesh_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 437 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_madhya_pradesh_countered
				clr_country_flag = RAJ_violence_in_madhya_pradesh
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_madhya_pradesh
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				437 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_haryana = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_haryana_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 991 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_haryana_countered
				clr_country_flag = RAJ_violence_in_haryana
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_haryana
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				991 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_uttarakhand = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_uttarakhand_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 992 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_uttarakhand_countered
				clr_country_flag = RAJ_violence_in_uttarakhand
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_uttarakhand
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				992 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_aksaichin = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_aksaichin_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 958 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_aksaichin_countered
				clr_country_flag = RAJ_violence_in_aksaichin
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_aksaichin
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				958 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}
	
	RAJ_encourage_violence_in_jammu = {

		icon = generic_prepare_civil_war

		allowed = {
			tag = RAJ
		}

		available = {
			hidden_trigger = { always = no }
		}
		
		cancel_trigger = {
			OR = {
				is_subject = no
				has_autonomy_state = autonomy_dominion
				has_country_flag = RAJ_counter_violence_in_jammu_countered
			}
		}

		highlight_states = { highlight_state_targets = { state = 990 } }

		on_map_mode = map_and_decisions_view

		fire_only_once = no

		days_mission_timeout = 30

		is_good = no

		cancel_if_not_visible = yes

		activation = {
			always = no
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_quit_india_movement_mission_flag
		}

		cancel_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_counter_violence_in_jammu_countered
				clr_country_flag = RAJ_violence_in_jammu
			}
		}

		timeout_effect = {
			hidden_effect = {
				clr_country_flag = RAJ_violence_in_jammu
			}
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				add_war_support = -0.05
				add_stability = -0.02
				990 = {
					damage_building = {
						type = infrastructure
						damage = 1.0
					}
					damage_building = {
						type = industrial_complex
						damage = 1.0
					}
				}
				hidden_effect = {
					country_event = { id = raj_armor.891 }
				}
			}
		}
	}

	######### CIVIL UNREST ^

	######### COUNTER CIVIL UNREST
	
	RAJ_counter_violence_in_assam = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_assam
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_assam_countered
		}
	}

    RAJ_counter_violence_in_west_bengal = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_west_bengal
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_west_bengal_countered
		}
	}
	
    RAJ_counter_violence_in_bihar = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_bihar
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_bihar_countered
		}
	}
	
    RAJ_counter_violence_in_jharkhand = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_jharkhand
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_jharkhand_countered
		}
	}
	
    RAJ_counter_violence_in_odisha = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_odisha
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_odisha_countered
		}
	}
	
    RAJ_counter_violence_in_uttar_pradesh = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_uttar_pradesh
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_uttar_pradesh_countered
		}
	}
	
    RAJ_counter_violence_in_jabalpur = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_jabalpur
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_jabalpur_countered
		}
	}
	
    RAJ_counter_violence_in_bombay = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_bombay
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_bombay_countered
		}
	}
	
    RAJ_counter_violence_in_telangana = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_telangana
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_telangana_countered
		}
	}
	
    RAJ_counter_violence_in_mysore = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_mysore
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_mysore_countered
		}
	}
	
    RAJ_counter_violence_in_madras = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_madras
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_madras_countered
		}
	}
	
    RAJ_counter_violence_in_tamil_nadu = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_tamil_nadu
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_tamil_nadu_countered
		}
	}
	
    RAJ_counter_violence_in_rajahsthan = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_rajahsthan
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_rajahsthan_countered
		}
	}
	
    RAJ_counter_violence_in_gujarat = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_gujarat
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_gujarat_countered
		}
	}
	
    RAJ_counter_violence_in_madhya_pradesh = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_madhya_pradesh
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_madhya_pradesh_countered
		}
	}
	
    RAJ_counter_violence_in_haryana = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_haryana
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_haryana_countered
		}
	}
	
    RAJ_counter_violence_in_uttarakhand = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_uttarakhand
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_uttarakhand_countered
		}
	}
	
    RAJ_counter_violence_in_aksaichin = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_aksaichin
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_aksaichin_countered
		}
	}
	
    RAJ_counter_violence_in_jammu = {
		icon = generic_police_action

		available = {
			tag = RAJ
		}

		visible = {
			tag = RAJ
			has_country_flag = RAJ_violence_in_jammu
		}

		cost = 50
		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		complete_effect = {
			if = {
				limit = {
					check_variable = { RAJ_civil_unrest > 10 }
				}
				hidden_effect = {
					set_country_flag = RAJ_quit_india_movement_mission_failed
					country_event = { id = raj_armor.817 }
				}
			}
			else_if = {
				limit = {
					check_variable = { RAJ_civil_unrest < 11 }
				}
				hidden_effect = {
					add_to_variable = { RAJ_civil_unrest = 1.0 }
				}
			}
			set_country_flag = RAJ_counter_violence_in_jammu_countered
		}
	}
	
	######### COUNTER CIVIL UNREST ^
}

political_actions = {
	RAJ_attract_british_arms_investors = {

		icon = gre_investment_decisions

		allowed = {
		}

		available = {
			num_of_controlled_states > 3
			is_subject_of = ENG
			if = {
				limit = {
					ENG = {
						has_war = yes
					}
				}
				NOT = {
					ENG = {
						any_enemy_country = {
							is_major = yes
						}
					}
				}
			}
		}

		days_remove = 60

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 5
		}

		visible = {
			has_completed_focus = RAJ_strengthen_british_investors
		}

		complete_effect = {
		}

		remove_effect = {
			random_owned_controlled_state = {
				prioritize = { 429 }
				limit = {
					free_building_slots = {
						building = arms_factory
						size > 1
						include_locked = yes
					}
				}
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = arms_factory
					level = 2
					instant_build = yes
				}
			}
			random_owned_controlled_state = {
				prioritize = { 439 }
				limit = {
					free_building_slots = {
						building = arms_factory
						size > 1
						include_locked = yes
					}
				}
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = arms_factory
					level = 2
					instant_build = yes
				}
			}
			random_owned_controlled_state = {
				prioritize = { 431 }
				limit = {
					free_building_slots = {
						building = arms_factory
						size > 1
						include_locked = yes
					}
				}
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = arms_factory
					level = 2
					instant_build = yes
				}
			}
		}
	}

	RAJ_attract_british_industrial_investors = {

		icon = gre_investment_decisions

		allowed = {
		}

		available = {
			num_of_controlled_states > 3
			is_subject_of = ENG
			if = {
				limit = {
					ENG = {
						has_war = yes
					}
				}
				NOT = {
					ENG = {
						any_enemy_country = {
							is_major = yes
						}
					}
				}
			}
		}

		days_remove = 60

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 5
		}

		visible = {
			has_completed_focus = RAJ_strengthen_british_investors
		}

		complete_effect = {
		}

		remove_effect = {
			random_owned_controlled_state = {
				prioritize = { 429 }
				limit = {
					free_building_slots = {
						building = industrial_complex
						size > 1
						include_locked = yes
					}
				}
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 2
					instant_build = yes
				}
			}
			random_owned_controlled_state = {
				prioritize = { 439 }
				limit = {
					free_building_slots = {
						building = industrial_complex
						size > 1
						include_locked = yes
					}
				}
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 2
					instant_build = yes
				}
			}
			random_owned_controlled_state = {
				prioritize = { 431 }
				limit = {
					free_building_slots = {
						building = industrial_complex
						size > 1
						include_locked = yes
					}
				}
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 2
					instant_build = yes
				}
			}
		}
	}

	RAJ_attract_british_transportation_investors = {

		icon = gre_investment_decisions

		allowed = {
		}

		available = {
			num_of_controlled_states > 3
			is_subject_of = ENG
			if = {
				limit = {
					ENG = {
						has_war = yes
					}
				}
				NOT = {
					ENG = {
						any_enemy_country = {
							is_major = yes
						}
					}
				}
			}
		}

		days_remove = 60

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 5
		}

		visible = {
			has_completed_focus = RAJ_strengthen_british_investors
		}

		complete_effect = {
		}

		remove_effect = {
			random_owned_controlled_state = {
				prioritize = { 436 }
				limit = {
					free_building_slots = {
						building = infrastructure
						size > 0
						include_locked = yes
					}
				}
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			random_owned_controlled_state = {
				prioritize = { 993 }
				limit = {
					free_building_slots = {
						building = infrastructure
						size > 0
						include_locked = yes
					}
				}
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			random_owned_controlled_state = {
				prioritize = { 435 }
				limit = {
					free_building_slots = {
						building = infrastructure
						size > 0
						include_locked = yes
					}
				}
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
		}
	}
	
	RAJ_discuss_investment_possibilities_with_FROM = {

		icon = aluminium

		allowed = {
			#original_tag = TUR
		}

		available = {
			FROM = {
				OR = {
					tag = FRA
					tag = ITA
					tag = JAP
					tag = GER
					tag = SOV
					tag = ENG
					tag = USA
				}
				has_capitulated = no
				is_in_faction_with = ROOT
				is_subject = no
				NOT = { has_country_flag = ROOT_rejected_raj_invesment }
			}
		}

		target_trigger = {
			FROM = {
				OR = {
					tag = FRA
					tag = ITA
					tag = JAP
					tag = GER
					tag = SOV
					tag = ENG
					tag = USA
				}
				is_in_faction_with = ROOT
				NOT = { has_country_flag = ROOT_rejected_raj_invesment }
			}
		}

		days_remove = 45

		cost = 25

		fire_only_once = no

		ai_will_do = {
			factor = 5
		}

		visible = {
			has_completed_focus = RAJ_air_depot
			NOT = {
				has_country_flag = RAJ_investment_pursued
			}
			NOT = {
				has_country_flag = investment_portfolio_accepted
			}
			FROM = {
				NOT = { has_country_flag = ROOT_rejected_raj_invesment }
			}
		}

		complete_effect = {
			set_country_flag = RAJ_investment_pursued
		}

		remove_effect = {
			FROM = { country_event = { id = raj_armor.803 } }
			clr_country_flag = RAJ_investment_pursued
		}
	}
}

war_measures = {

	RAJ_burma_chindits_operation = {
		icon = revolt

		available = {
			288 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = BRM
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			288 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 288
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			288 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

	RAJ_mandalay_chindits_operation = {
		icon = revolt

		available = {
			640 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = BRM
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			640 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 640
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			640 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

	RAJ_assam_chindits_operation = {
		icon = revolt

		available = {
			432 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			432 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 432
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			432 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

	RAJ_east_bengal_chindits_operation = {
		icon = revolt

		available = {
			430 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			430 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 430
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			430 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_west_bengal_chindits_operation = {
		icon = revolt

		available = {
			431 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			431 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 431
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			431 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_bihar_chindits_operation = {
		icon = revolt

		available = {
			435 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			435 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 435
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			435 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_jharkhand_chindits_operation = {
		icon = revolt

		available = {
			993 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			993 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 993
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			993 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_odisha_chindits_operation = {
		icon = revolt

		available = {
			426 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			426 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 426
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			426 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_uttar_pradesh_chindits_operation = {
		icon = revolt

		available = {
			438 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			438 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 438
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			438 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_jabalpur_chindits_operation = {
		icon = revolt

		available = {
			436 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			436 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 436
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			436 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_bombay_chindits_operation = {
		icon = revolt

		available = {
			429 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			429 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 429
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			429 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_telangana_chindits_operation = {
		icon = revolt

		available = {
			427 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			427 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 427
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			427 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_mysore_chindits_operation = {
		icon = revolt

		available = {
			425 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			425 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 425
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			425 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_madras_chindits_operation = {
		icon = revolt

		available = {
			424 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			424 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 424
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			424 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_tamil_nadu_chindits_operation = {
		icon = revolt

		available = {
			423 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			423 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 423
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			423 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_rajahsthan_chindits_operation = {
		icon = revolt

		available = {
			433 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			433 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 433
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			433 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_gujarat_chindits_operation = {
		icon = revolt

		available = {
			428 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			428 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 428
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			428 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_madhya_pradesh_chindits_operation = {
		icon = revolt

		available = {
			437 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			437 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 437
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			437 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_haryana_chindits_operation = {
		icon = revolt

		available = {
			991 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			991 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 991
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			991 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_uttarakhand_chindits_operation = {
		icon = revolt

		available = {
			992 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			992 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 992
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			992 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_northern_kashmir_chindits_operation = {
		icon = revolt

		available = {
			787 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = PAK
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			787 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 787
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			787 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_kashmir_chindits_operation = {
		icon = revolt

		available = {
			441 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = PAK
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			441 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 441
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			441 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_aksaichin_chindits_operation = {
		icon = revolt

		available = {
			958 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = PAK
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			958 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 958
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			958 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_jammu_chindits_operation = {
		icon = revolt

		available = {
			990 = {
				CONTROLLER = {
					has_war_with = ROOT
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			990 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 990
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			990 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_peshawar_chindits_operation = {
		icon = revolt

		available = {
			442 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = PAK
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			442 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 442
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			442 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_punjab_chindits_operation = {
		icon = revolt

		available = {
			440 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = PAK
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			440 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 440
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			440 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_quetta_chindits_operation = {
		icon = revolt

		available = {
			445 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = PAK
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			445 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 445
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			445 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_sind_chindits_operation = {
		icon = revolt

		available = {
			443 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = PAK
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			443 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 443
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			443 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}

    RAJ_baluchistan_chindits_operation = {
		icon = revolt

		available = {
			444 = {
				CONTROLLER = {
					has_war_with = ROOT
					NOT = {
						tag = PAK
					}
				}
			}
		}

		visible = {
			has_completed_focus = RAJ_equip_the_chindits
			444 = { any_neighbor_state = { CONTROLLER = { has_war_with = ROOT } } }
		}

		highlight_states = {
			highlight_state_targets = {
				state = 444
			}
		}

		days_remove = 30
		days_re_enable = 150

		is_good = yes

		cost = 0
		custom_cost_text = custom_cost_25_cp
		custom_cost_trigger = {
			command_power > 24
		}

		cancel_if_not_visible = yes

		complete_effect = {
			hidden_effect = {
				add_command_power = -25
			}
		}

		remove_effect = {
			444 = {
				add_resistance = 20
				damage_building = {
					type = infrastructure
					damage = 1
				}
			}
		}

		ai_will_do = {
			factor = 800
		}
	}
}