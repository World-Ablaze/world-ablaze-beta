# [sf-doctrine] One grand doctrine, like the naval and air folders: the branch choice lives in the
# subdoctrines, not here, so that two tracks can hold two different branches. It carries the effect
# the three legacy roots shared (category_special_forces org/soft attack); everything the roots did
# per branch moved into that branch's subdoctrines.

special_forces_command = {
    folder = special_forces

    name = special_forces_command
    description = special_forces_command_desc
    icon = GFX_doctrine_special_forces_1_medium
    available = {
        always = yes
    }

    xp_cost = 25
    xp_type = army

    ai_will_do = {
        base = 1
    }

    tracks = {
        special_forces_first
        special_forces_second
    }

    max_track_rows = 1
    max_track_columns = 2

    # EFFECTS
    category_special_forces = {
        max_organisation = 2.5
        soft_attack = 0.025
    }

    milestones = {
        {}
        {}
    }
}
