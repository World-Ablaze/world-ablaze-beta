# [sf-doctrine] The two special-forces tracks. Each holds one branch subdoctrine, so a country
# running both tracks runs two different special-forces branches at once - this is what the legacy
# `sf_trees` variable used to buy. Track two assumes `sf_trees` is still granted by the per-tree
# unlock techs in common/technologies/infantry*.txt; if no site calls
# add_potential_special_forces_tree any more, the second track never opens.

special_forces_first = {
    name = special_forces_first_title
    background = "GFX_paratroopers_bg"
    icon = "GFX_doctrine_milestone_special_forces"
    icon_frame = "GFX_doctrine_decor_special_forces"

    mastery = {
        multiplier = 6
        categories = {
            category_special_forces
        }
    }
}

special_forces_second = {
    name = special_forces_second_title
    background = "GFX_marine_bg"
    icon = "GFX_doctrine_milestone_special_forces"
    icon_frame = "GFX_doctrine_decor_special_forces"

    active = {
        can_unlock_second_track_of_sf_doctrine = yes
    }

    mastery = {
        multiplier = 6
        categories = {
            category_special_forces
        }
    }
}
