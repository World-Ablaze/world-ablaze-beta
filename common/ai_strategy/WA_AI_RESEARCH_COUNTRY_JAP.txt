############################################################################################################
# WA AI Research - COUNTRY_JAP
############################################################################################################
# THIS FILE INTENTIONALLY DECLARES NO STRATEGIES. It is kept as a tombstone so that a grep for
# WA_AI_RESEARCH_COUNTRY_JAP lands on the explanation rather than on nothing.
#
# FIX 69 (2026-08-14, checklist R40/R33) - FIX 57's JAP HALF MOVED TO THE TECHNOLOGY FILE.
# =======================================================================================
# This file used to carry `research_weight_factor = -100` on `jap_submarine_5` (Ro-101) and
# `jap_submarine_6` (Ha-201), the two coastal hulls. It did not work. Campaign f9321934:
# both were researched anyway - Ro-101 on 1942.10.26 and Ha-201 on 1944.9.28 - and JAP's
# single `naval_submarine` line sat on the coastal `hull_5` from 1943.10 and the coastal
# `hull_6` from 1944.12 through to the final save, 35 consecutive months on a coastal boat
# against an `ai_equipment` ladder Fix 48 had already re-ordered to 10/60/120/140/5/3.
#
# The failure is a property of the lever rather than of the value chosen: the token factors
# the technology's `ai_will_do` score, every WA `ai_will_do` is `factor = 1` plus `factor = 0`
# gates, so the AI's menu is mostly zeros already and `0 x anything` is still zero. The full
# argument, including why vanilla's stronger `-500` would not have helped either, is in the
# sibling file WA_AI_RESEARCH_COUNTRY_USA.txt.
#
# Replacement: a hard `allow = { NOT = { is_ai = yes } }` on both technologies in
# common/technologies/naval_jap.txt - the construct vanilla itself uses to make a technology
# unresearchable (`jungle_warfare`, vanilla common/technologies/infantry.txt).
#
# Nothing is stranded and this is the cleanest of the three Fix 69 cases: `jap_submarine_5`
# leads only to `jap_submarine_6`, which is a terminal leaf with no outgoing path edges, so
# the entire cost of the gate is exactly those two coastal hulls. `jap_submarine_4` (Hei,
# 7500 km / str 52) is untouched and remains the intended mainstay.
#
# JAP needs no `+100` counterpart. Unlike USA and SOV, nothing downstream depended on the
# gated branch, so there is no now-sole path that needs prioritising.
