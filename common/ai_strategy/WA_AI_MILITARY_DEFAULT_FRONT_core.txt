############################################################################################################
# WA AI Military V2 - DEFAULT_FRONT_core
# Fix 97 (2026-08-17): the `NOT italy_ethiopian_war_active` exemption on the two sink caps was removed -
# it existed because region 17 (Eritrea/Ethiopia) sat in WA_AI_MILITARY_sink_africa_regions; 17 now lives in
# WA_AI_MILITARY_east_africa_regions and these caps no longer touch it, so the exemption had no reason left.
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { force_concentration_front_factor, force_concentration_target_weight, front_armor_score, front_control, front_unit_request, garrison }.
# Canonical domain: FRONT. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.

############################################################################################################
# WA Military V2 - Core Front Allocation Rules
############################################################################################################

WA_AI_MILITARY_CORE_secondary_sink_soft_cap = {
	enable = {
		has_war = yes
		num_divisions > 79
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = WA_AI_MILITARY_sink_africa_regions
		value = -25
	}

	ai_strategy = {
		type = front_unit_request
		state_trigger = { WA_AI_MILITARY_is_southeast_asia_sink_state = yes }
		value = -25
	}
}



WA_AI_MILITARY_CORE_secondary_sink_hard_cap = {
	enable = {
		has_war = yes
		num_divisions > 149
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = WA_AI_MILITARY_sink_africa_regions
		value = -100 # economy: was -150
	}

	ai_strategy = {
		type = front_unit_request
		state_trigger = { WA_AI_MILITARY_is_southeast_asia_sink_state = yes }
		value = -100 # economy: was -150
	}
}
