# ============================================================================
# equipment_variant XP lever for the ai_equipment design REdesigns
# ============================================================================
# The design pairs in common/ai_equipment/*_tank.txt (the `__cc` twins of
# [sov-cutting-corners-module], the `__wa` / `__la` twins of [resource-grade-downshift]) only
# get REdesigned while something arms the equipment_variant desire: 05_defines.lua starves it to
# 0.1/day against 100/day for templates, so a redesign never wins the XP auction on its own
# (MEASURED on the cutting_corners pairs: iteration 2 needed ~1000 console XP, iteration 3 passed
# at natural XP with this lever). One block, tag-free, the SINGLE writer of this type+id: the two
# gates OR together so SOV's permanent cc latch and any country's 120-day grade window arm the
# same value instead of stacking two blocks (engine summing of same-id values is ASSUMED).
# Value = the one the cutting_corners rerun proved.

WA_AI_PRODUCTION_variant_redesign_xp = {
	enable = {
		OR = {
			WA_AI_EQUIPMENT_should_mount_cutting_corners = yes
			WA_AI_EQUIPMENT_should_spend_xp_on_grade_redesign = yes
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = land_xp_spend_priority
		id = equipment_variant
		value = 500
	}
}
