############################################################################################################
# WA AI Military V2 - DEFAULT_FRONT_archetypes
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { force_concentration_front_factor, force_concentration_target_weight, front_armor_score, front_control, front_unit_request, garrison }.
# Canonical domain: FRONT. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.

############################################################################################################
# WA Military V2 - Country Archetype Allocation Rules
############################################################################################################

WA_AI_MILITARY_ARCHETYPE_european_continental_majors_focus_landfronts = {
	enable = {
		WA_AI_MILITARY_should_archetype_european_continental_majors_focus_landfronts = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = germany
		value = 100
	}

	ai_strategy = {
		type = front_unit_request
		area = france
		value = 75
	}

	ai_strategy = {
		type = front_unit_request
		area = benelux
		value = 75
	}

	ai_strategy = {
		type = front_unit_request
		area = south_america
		value = -100 # economy: was -200
	}

	ai_strategy = {
		type = front_unit_request
		area = central_america
		value = -100 # economy: was -200
	}

	ai_strategy = {
		type = front_unit_request
		area = WA_AI_MILITARY_sink_africa_regions
		value = -100 # economy: was -200
	}
	# [med-axis-posture] review (2026-08-17): East Africa is deliberately NOT re-added here - this archetype also
	# matches the countries the theatre is fought by (SAF/RAJ are minors; ENG is a "continental major"
	# once its army passes 1M men), and a -100 here would cancel the Faction +150 pull for exactly them.
	# The CAPS (home_threatened / not_safe / weak minor) keep the unfit out.

	ai_strategy = {
		type = front_unit_request
		state_trigger = { WA_AI_MILITARY_is_southeast_asia_sink_state = yes }
		value = -100 # economy: was -120
	}
}



WA_AI_MILITARY_ARCHETYPE_naval_majors_global_balance = {
	enable = {
		WA_AI_MILITARY_should_archetype_naval_majors_global_balance = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = north_pacific
		value = 60
	}

	ai_strategy = {
		type = front_unit_request
		area = central_pacific
		value = 60
	}

	ai_strategy = {
		type = front_unit_request
		area = south_pacific
		value = 30
	}

	ai_strategy = {
		type = front_unit_request
		area = WA_AI_MILITARY_sink_africa_regions
		value = -75
	}
}



WA_AI_MILITARY_ARCHETYPE_minors_home_first = {
	enable = {
		WA_AI_MILITARY_should_archetype_minors_home_first = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = garrison
		value = 50
	}

	ai_strategy = {
		type = front_unit_request
		area = south_america
		value = -100 # economy: was -300
	}

	ai_strategy = {
		type = front_unit_request
		area = central_america
		value = -100 # economy: was -300
	}

	ai_strategy = {
		type = front_unit_request
		area = WA_AI_MILITARY_sink_africa_regions
		value = -100 # economy: was -300
	}
	# [med-axis-posture] review (2026-08-17): East Africa is deliberately NOT re-added here - this archetype also
	# matches the countries the theatre is fought by (SAF/RAJ are minors; ENG is a "continental major"
	# once its army passes 1M men), and a -100 here would cancel the Faction +150 pull for exactly them.
	# The CAPS (home_threatened / not_safe / weak minor) keep the unfit out.

	ai_strategy = {
		type = front_unit_request
		state_trigger = { WA_AI_MILITARY_is_southeast_asia_sink_state = yes }
		value = -100 # economy: was -300
	}
}


# purpose: [allied-total-commitment] hand the engine's generic area-defence army to the fronts while
#          nobody can reach home; -5000 dominates minors_home_first's +50 (measured mechanism and the
#          per-tag releases this replaced: WA_AI_MILITARY_SYSTEM.md §16/§23).
# range:   garrison -5000 (WA force-off convention; the engine doc has no garrison section)
# policy:  Negative override while total commitment holds; abort puts minors_home_first back
# domain:  FRONT
WA_AI_MILITARY_ARCHETYPE_committed_minor_releases_home = {
	enable = {
		WA_AI_MILITARY_total_commitment_active = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = garrison
		value = -5000
	}
}
