############################################################################################################
# WA AI Military V2 - COUNTRY_SPR_THEATRE
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { area_priority, put_unit_buffers, spare_unit_factor, theatre_distribution_demand_increase }.
# Canonical domain: THEATRE. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.


# Phase 6 split from WA_AI_MILITARY_SPR_defense_of_madrid (THEATRE domain)
WA_AI_MILITARY_SPR_defense_of_madrid_THEATRE = {

	allowed = {
		original_tag = SPR
	}
	enable = {
		has_country_flag = SPR_republican_spain_flag
		NOT = {
			has_global_flag = scw_over
		}
	}

	abort_when_not_enabled = yes

	#ai_strategy = {
	#	type = put_unit_buffers
	#
	#	order_id = 3
	#
	#	states = {
	#		868
	#	}
	#
	#	ratio = 0.015
	#
	#	subtract_invasions_from_need = no
	#	subtract_fronts_from_need = yes
	#}

	ai_strategy = {
		type = put_unit_buffers

		order_id = 2

		states = {
			41
		}

		ratio = 0.08

		subtract_invasions_from_need = no
		subtract_fronts_from_need = yes
	}
}

# purpose: during the civil war both sides fight on the mainland, not in Morocco (relocated Phase 7c from legacy SPA.txt SPA/SPD_civil_war_focus_on_mainland; the two government-gated twins carried identical entries, so their union - any government - replaces them)
# range:   -1000 (saturated legacy value, E1 worklist)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_SPR_civil_war_mainland_THEATRE = {
	allowed = {
		original_tag = SPR
	}
	enable = {
		has_war = yes
		has_civil_war = yes
		NOT = {
			has_war_with = FRA
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = north_africa
		value = -200 # economy: was -1000
	}
}
