############################################################################################################
# WA AI Military V2 - COUNTRY_ENG_FRONT
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { force_concentration_front_factor, force_concentration_target_weight, front_armor_score, front_control, front_unit_request, garrison }.
# Canonical domain: FRONT. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.


# Phase 6 split from WA_AI_MILITARY_ENG_gang_up_GER_with_USA (FRONT domain)
WA_AI_MILITARY_ENG_gang_up_GER_with_USA_FRONT = {

	allowed = {
		tag = ENG
	}
	enable = {
		FRA = {
			OR = {
				has_capitulated = yes
				exists = no
			}
		}
	}
	abort_when_not_enabled = yes
	# after FRA falls, both ENG and USA will try to help in europe and africa

	ai_strategy = {
		type = front_unit_request
		value = 100
		target = italy
	}
	ai_strategy = {
		type = front_unit_request
		value = 100
		target = south_italy
	}
}


# Phase 6 split from WA_AI_MILITARY_ENG_help_Greece_if_Africa_safe (FRONT domain)
WA_AI_MILITARY_ENG_help_Greece_if_Africa_safe_FRONT = {

	allowed = {
		original_tag = ENG
	}
	enable = {
		date > 1941.3.1
		is_in_faction_with = GRE
		GRE = { has_capitulated = no }
		AND = {
			451 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			450 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			449 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			448 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			661 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			662 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			663 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			665 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			458 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			460 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			513 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			459 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			461 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			1056 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			447 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
			923 = {
				CONTROLLER = {
					NOT = { has_war_with = ENG }
				}
			}
		}

	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = GRE
		value = 100
	}
}


# WA_AI_MILITARY_ENG_war_against_ITA_3_FRONT was RE-HOMED 2026-08-12 to the Faction layer as
# WA_AI_MILITARY_ALLIES_italy_cobelligerent_support_FRONT
# (common/ai_strategy/WA_AI_MILITARY_FACTION_ALLIES_FRONT.txt), values unchanged (+100 italy /
# +100 south_italy, same enable). It was the only post-flip Italian positive in the mod and applied
# to England alone, which is why campaign 9a4cd657 shows ENG at +20 and the USA at -145 on regions
# 21/23 after the Italian defection. Deleted here rather than kept, because Faction and Country
# copies of an Additive type would stack and hand ENG +200 - see WA_AI_MILITARY_SYSTEM.md on not
# duplicating a block across layers. Do not re-add an ENG-specific version; tune the Faction block.


# Phase 6 split from WA_AI_MILITARY_ENG_bef_in_europe (FRONT domain)
WA_AI_MILITARY_ENG_bef_in_europe_FRONT = {

	allowed = {
		tag = ENG
	}
	enable = {
		NOT = {
			has_global_flag = fall_of_france
		}
		is_in_faction_with = FRA
		FRA = { surrender_progress < 0.15 }
		surrender_progress < 0.01
	}
	abort_when_not_enabled = yes

	#Germany
	#	ai_strategy = {
	#		type = front_unit_request
	#		strategic_region = 6
	#		value = -1000
	#	}
	#
	#	ai_strategy = {
	#		type = front_unit_request
	#		strategic_region = 5
	#		value = -1000
	#	}

	#South France/italy

	ai_strategy = {
		type = front_unit_request
	    tag = BEL
		value = 175
	}
	ai_strategy = {
		type = front_unit_request
		strategic_region = 239
		value = 175
	}
	ai_strategy = {
		type = front_unit_request
		strategic_region = 7
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		strategic_region = 20
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		strategic_region = 21
		value = -100 # economy: was -1000
	}
}


# Phase 6 split from WA_AI_MILITARY_ENG_bef_in_europe_leave (FRONT domain)
WA_AI_MILITARY_ENG_bef_in_europe_leave_FRONT = {

	allowed = {
		tag = ENG
	}
	enable = {
		NOT = {
			has_global_flag = fall_of_france
		}
		#has_war_with = GER
		is_in_faction_with = FRA
		OR = {
			surrender_progress > 0.01
			FRA = { surrender_progress > 0.14 }
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		strategic_region = 7
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		strategic_region = 6
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		strategic_region = 20
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		strategic_region = 21
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		strategic_region = 5
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		area = france
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		area = west_france
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		area = south_france
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		area = north_france
		value = -100 # economy: was -1000
	}
}


# [med-axis-posture] (2026-08-17): WA_AI_MILITARY_ENG_africa_war_no_RAJ_FRONT (+20 central_africa when the RAJ is NOT in the
# faction) DELETED - the Faction WA_AI_MILITARY_ALLIES_east_africa_contested_FRONT (+150) serves ENG in both
# configurations; keeping this would stack.




WA_AI_MILITARY_ENG_africa_war_1 = {
	allowed = {
		tag = ENG
	}
	enable = {
		OR = {
			446 = { is_controlled_by = ENG }
			447 = { is_controlled_by = ENG }
			453 = { is_controlled_by = ENG }
			446 = { is_controlled_by = UKE }
			447 = { is_controlled_by = UKE }
			453 = { is_controlled_by = UKE }
		}
		OR = {
			is_in_faction_with = RAJ
			RAJ = { is_subject_of = ENG }
		}
		WA_AI_MILITARY_ENG_africa_hostile_presence = yes
		surrender_progress < 0.05
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = central_africa
		value = -50 # economy: was -200, then -100; [med-axis-posture] (user decision 2026-08-17): -50 - the "let the RAJ do East Africa" preference stays, but no longer cancels the Faction +150 theatre pull (0edbc955: RAJ 0-5 divisions there, ENG never more than 17)
	}

	ai_strategy = {
		type = front_unit_request
		area = north_africa
		value = -40
	}
}


# [med-axis-posture] review (2026-08-17): WA_AI_MILITARY_ENG_africa_war_2_FRONT DELETED. Its gate had been `OR{450,663,451}
# AND NOT{450,663,451,...}` (a NOR - X AND NOT X) since the very first authoring (79d64f6ff): the payload was NEVER
# live in any campaign, and its FRONT value had been flipped to -30 (7619b217a) while dead. Reviving it would
# have stacked a third +200 north_africa on top of africa_war_3_THEATRE and focus_on_land_War_in_north_africa.



# Phase 6 split from WA_AI_MILITARY_ENG_africa_war_3 (FRONT domain)
WA_AI_MILITARY_ENG_africa_war_3_FRONT = {

	allowed = {
		tag = ENG
	}
	enable = {
		OR = {
			446 = { is_controlled_by = ENG }
			447 = { is_controlled_by = ENG }
			453 = { is_controlled_by = ENG }
			450 = { is_controlled_by = ENG }
			663 = { is_controlled_by = ENG }
			451 = { is_controlled_by = ENG }
			446 = { is_controlled_by = UKE }
			447 = { is_controlled_by = UKE }
			453 = { is_controlled_by = UKE }
			450 = { is_controlled_by = UKE }
			663 = { is_controlled_by = UKE }
			451 = { is_controlled_by = UKE }
		}
		surrender_progress < 0.05
		date > 1939.1.1
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = ITA
		ordertype = front
		execution_type = balanced
	}
	ai_strategy = {
		type = front_control
		tag = ITL
		ordertype = front
		execution_type = balanced
	}
	ai_strategy = {
		type = front_unit_request
		area = north_africa
		value = -20
	}
}



WA_AI_MILITARY_ENG_soviets_are_no_threat = {
	allowed = {
		tag = ENG
	}
	enable = {
		NOT = {
			has_war_with = SOV
		}
		SOV = {
			has_war_with = GER
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = SOV
		value = -100 # economy: was -1000
	}
}



WA_AI_MILITARY_ENG_oman_no_threat = {
	allowed = { tag = ENG }
	enable = { NOT = { has_war_with = OMA } }
	abort_when_not_enabled = yes
	ai_strategy = { type = front_unit_request tag = OMA value = -1000 }
}



WA_AI_MILITARY_ENG_yemen_no_threat = {
	allowed = { tag = ENG }
	enable = { NOT = { has_war_with = YEM } }
	abort_when_not_enabled = yes
	ai_strategy = { type = front_unit_request tag = YEM value = -1000 }
}



WA_AI_MILITARY_ENG_afg_are_no_threat = {
	allowed = { tag = ENG }
	enable = { NOT = { has_war_with = AFG } }
	abort_when_not_enabled = yes
	ai_strategy = { type = front_unit_request tag = AFG value = -1000 }
}



WA_AI_MILITARY_ENG_sau_are_no_threat = {
	allowed = { tag = ENG }
	enable = { NOT = { has_war_with = SAU } }
	abort_when_not_enabled = yes
	ai_strategy = { type = front_unit_request tag = SAU value = -1000 }
}


# Phase 6 split from WA_AI_MILITARY_ENG_war_against_ITA_2 (FRONT domain)
WA_AI_MILITARY_ENG_war_against_ITA_2_FRONT = {

	allowed = {
		tag = ENG
	}
	enable = {
		has_war_with = ITA
		OR = {
			is_historical_focus_on = no
			date > 1943.7.1
		}
		NOT = {	451 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	450 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	449 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	448 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	661 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	662 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	663 = { CONTROLLER = { has_war_with = ROOT } } }

		NOT = {	665 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	458 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	460 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	513 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	459 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	461 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	1056 = { CONTROLLER = { has_war_with = ROOT } } }
		115 = { is_controlled_by = ITA }
		1032 = { is_controlled_by = ITA }
		156 = { is_controlled_by = ITA }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = italy
		value = 100
	}
	ai_strategy = {
		type = front_unit_request
		area = south_italy
		value = 25
	}
}



WA_AI_MILITARY_ENG_sealion_is_happening_2 = {
	allowed = {
		tag = ENG
	}

	enable = {
		OR = {
			122 = { CONTROLLER = { has_war_with = ROOT } }
			123 = { CONTROLLER = { has_war_with = ROOT } }
			125 = { CONTROLLER = { has_war_with = ROOT } }
			126 = { CONTROLLER = { has_war_with = ROOT } }
			127 = { CONTROLLER = { has_war_with = ROOT } }
			129 = { CONTROLLER = { has_war_with = ROOT } }
			130 = { CONTROLLER = { has_war_with = ROOT } }
			131 = { CONTROLLER = { has_war_with = ROOT } }
			132 = { CONTROLLER = { has_war_with = ROOT } }
			857 = { CONTROLLER = { has_war_with = ROOT } }
			859 = { CONTROLLER = { has_war_with = ROOT } }
			860 = { CONTROLLER = { has_war_with = ROOT } }
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		strategic_region = 1
		value = 200 # economy: was 1000
	}

	ai_strategy = {
		type = front_unit_request

		strategic_region = 2
		value = 200 # economy: was 650
	}
}


# Phase 6 split from WA_AI_MILITARY_ENG_red_hand (FRONT domain)
WA_AI_MILITARY_ENG_red_hand_FRONT = {

	allowed = {
		tag = ENG
	}
	enable = {
		NOT = {
			has_war_with = IRE
			is_in_faction_with = IRE
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = ireland
		value = -100 # economy: was -1000
	}
}

############################################################################################################
# WA Military V2 - ENG Country Overlay
############################################################################################################
# Phase 2 split: NAVAL domain blocks.

# Phase 2 split: NAVAL domain blocks.
# Phase 6 split from WA_AI_MILITARY_ENG_focus_on_land_War_in_north_africa (FRONT domain)
WA_AI_MILITARY_ENG_focus_on_land_War_in_north_africa_FRONT = {

	allowed = {
		tag = ENG
	}
	enable = {
		OR = {
			446 = { is_controlled_by = ENG }
			447 = { is_controlled_by = ENG }
			453 = { is_controlled_by = ENG }
			446 = { is_controlled_by = UKE }
			447 = { is_controlled_by = UKE }
			453 = { is_controlled_by = UKE }
		}
		WA_AI_MILITARY_ENG_africa_hostile_presence = yes
		surrender_progress < 0.05
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = north_africa
		value = 10
	}
}


# Phase 6 split from WA_AI_MILITARY_ENG_protect_home (FRONT domain)
WA_AI_MILITARY_ENG_protect_home_FRONT = {
	# always-on: home-defence floor: intentional static defensive bias, peace and war alike (Additive front_unit_request).

	allowed = {
		original_tag = ENG
	}
	enable = {
		always = yes
	}
	abort = {
		always = no
	}

	ai_strategy = {
		type = front_unit_request
		area = britain
		value = 80
	}
	ai_strategy = {
		type = garrison
		id = "ENG"
		value = 0 # economy: was -200
	}
	ai_strategy = {
		type = garrison
		value = 0 # economy: was -110
	}
}


# Phase 6 split from WA_AI_MILITARY_ENG_all_in (FRONT domain)
WA_AI_MILITARY_ENG_all_in_FRONT = {

	allowed = {
		tag = ENG
	}
	enable = {
		has_capitulated = no
		OR = {
			121 = { CONTROLLER = { has_war_with = ROOT } }
			122 = { CONTROLLER = { has_war_with = ROOT } }
			123 = { CONTROLLER = { has_war_with = ROOT } }
			125 = { CONTROLLER = { has_war_with = ROOT } }
			126 = { CONTROLLER = { has_war_with = ROOT } }
			127 = { CONTROLLER = { has_war_with = ROOT } }
			129 = { CONTROLLER = { has_war_with = ROOT } }
			130 = { CONTROLLER = { has_war_with = ROOT } }
			131 = { CONTROLLER = { has_war_with = ROOT } }
			132 = { CONTROLLER = { has_war_with = ROOT } }
			133 = { CONTROLLER = { has_war_with = ROOT } }
			136 = { CONTROLLER = { has_war_with = ROOT } }
			857 = { CONTROLLER = { has_war_with = ROOT } }
			859 = { CONTROLLER = { has_war_with = ROOT } }
			860 = { CONTROLLER = { has_war_with = ROOT } }
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = britain
		value = 200 # economy: was 500
	}
}

# purpose: British armour weighted toward the Axis majors while the home isles are safe (relocated
#          Phase 7c from legacy ENG.txt ENG_tanks_in_africa, gates verbatim)
# range:   +100 per enemy
# policy:  Additive
# domain:  FRONT
WA_AI_MILITARY_ENG_tanks_vs_axis_majors_FRONT = {
	allowed = {
		tag = ENG
	}
	enable = {
		NOT = {
			surrender_progress > 0
		}
		OR = {
			has_war_with = GER
			has_war_with = ITA
			is_historical_focus_on = yes
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_armor_score
		id = "ITL"
		value = 100
	}

	ai_strategy = {
		type = front_armor_score
		id = "ITA"
		value = 100
	}

	ai_strategy = {
		type = front_armor_score
		id = "GER"
		value = 100
	}
}


# [commonwealth-handoff] (2026-08-21, campaign 8c0fea4c) - the British half of the East-Africa delegation.
# purpose: take Britain's weight OFF East Africa while the Indian army carries it, so the divisions
#          land in Egypt instead. Without this half the delegate only ADDS divisions to Ethiopia and
#          nothing is freed - which is the whole point of the user's report.
# range:   front_unit_request -75 on the 4-region alias, netting ENG to +75 against the Faction +150
#          (RAJ nets +250). ENG is never pushed NEGATIVE: it keeps a real share of the theatre, and if
#          the delegate becomes unavailable this block aborts and ENG is back at the full +150.
# policy:  Additive per area; gate-exclusive by construction with the RAJ delegate - both read
#          WA_AI_MILITARY_commonwealth_east_africa_available / _RAJ_east_africa_available, the same verdict
# domain:  FRONT
WA_AI_MILITARY_ENG_east_africa_delegated_FRONT = {
	allowed = {
		WA_AI_CONFIG_MILITARY_is_british_mission_owner = yes
	}
	enable = {
		has_war = yes
		WA_AI_MILITARY_east_africa_theatre_contested = yes
		WA_AI_MILITARY_commonwealth_east_africa_available = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = WA_AI_MILITARY_east_africa_regions
		value = -75
	}
}
