############################################################################################################
# WA AI Naval - COUNTRY_USA
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { naval_avoid_region, naval_convoy_raid_region, strategic_air_importance, strike_force_home_base }.
# Canonical domain: NAVAL. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.
# [usa-refactor] legacy_USA_ name noise stripped; hard dates replaced by the same dynamic gates
# as the land files (WA_AI_MILITARY_USA_pacific_offensive_ready /
# WA_AI_MILITARY_has_minimum_expeditionary_army); the bad_torpedos naval twin (a strict SUBSET
# of europe_first's region list, summing to 4000 on 19 shared regions while both armed) deleted
# with the halving accepted - same ruling as the land twins.
# naval_avoid_region is SIGNED: positive = wall, negative = attraction (doc §4).


# purpose: wall the Adriatic while the Italian war is fought in Africa (the Adriatic is
#          Regia-Marina water under land-based air)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_war_against_ITA = {

	allowed = {
		tag = USA
	}
	enable = {
		has_war_with = ITA
		OR = {
			451 = { is_controlled_by = ITA }
			450 = { is_controlled_by = ITA }
			449 = { is_controlled_by = ITA }
			448 = { is_controlled_by = ITA }
			661 = { is_controlled_by = ITA }
			662 = { is_controlled_by = ITA }
			663 = { is_controlled_by = ITA }

			665 = { CONTROLLER = { has_war_with = ROOT } }
			458 = { CONTROLLER = { has_war_with = ROOT } }
			460 = { CONTROLLER = { has_war_with = ROOT } }
			513 = { CONTROLLER = { has_war_with = ROOT } }
			459 = { CONTROLLER = { has_war_with = ROOT } }
			461 = { CONTROLLER = { has_war_with = ROOT } }
			1055 = { CONTROLLER = { has_war_with = ROOT } }
			462 = { CONTROLLER = { has_war_with = ROOT } }
			1056 = { CONTROLLER = { has_war_with = ROOT } }
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 168 #adriatic sea
		value = 2000
	}
}


# purpose: keep the Adriatic wall up while the desert war is won overland (Egypt/Libya
#          friendly, Maghreb quiet; formerly also gated on is_historical/date > 1943.7 - the
#          state-control terms carry the sequencing in any timeline)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_war_against_ITA_2 = {

	allowed = {
		tag = USA
	}
	enable = {
		NOT = {
			856 = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_subject_of = ROOT
						is_in_faction_with = ROOT
						has_government = ROOT
					}
				}
			}
		}
		has_war_with = ITA
		OR = {
			451 = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_subject_of = ROOT
						is_in_faction_with = ROOT
						has_government = ROOT
					}
				}
			}
			450 = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_subject_of = ROOT
						is_in_faction_with = ROOT
						has_government = ROOT
					}
				}
			}
			449 = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_subject_of = ROOT
						is_in_faction_with = ROOT
						has_government = ROOT
					}
				}
			}
			448 = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_subject_of = ROOT
						is_in_faction_with = ROOT
						has_government = ROOT
					}
				}
			}
			661 = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_subject_of = ROOT
						is_in_faction_with = ROOT
						has_government = ROOT
					}
				}
			}
			662 = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_subject_of = ROOT
						is_in_faction_with = ROOT
						has_government = ROOT
					}
				}
			}
			663 = {
				CONTROLLER = {
					OR = {
						tag = ROOT
						is_subject_of = ROOT
						is_in_faction_with = ROOT
						has_government = ROOT
					}
				}
			}
		}
		NOT = {	451 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	450 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	449 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	448 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	661 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	662 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	663 = { CONTROLLER = { has_war_with = ROOT } } }

		NOT = {	665 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	458 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	460 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	513 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	459 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	461 = { CONTROLLER = { has_war_with = ROOT } } }
		NOT = {	1056 = { CONTROLLER = { has_war_with = ROOT } } }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 168 #adriatic sea
		value = 2000
	}
}


# [atlantic-naval] (2026-08-14) - AUDITED, DELIBERATELY NOT CHANGED IN INTENT. Read this before
# "fixing" the Bay of Biscay, because the obvious repair is the wrong one.
#
# Six blocks wrote region 42; the Torch pair carried the only negatives, and being Additive the
# negatives were OUTVOTED in every window in which they could fire (walls +6000 vs -2000/-4000).
# The "#open" intent below has therefore NEVER taken effect. Consequences, still standing:
#  - Deleting the -2000 would be behaviour-neutral but is a design decision (does Torch open
#    the Bay?) reserved to the user - kept.
#  - Geography argues the walls are right: the Bay of Biscay lies off the Britain/US -> Morocco
#    route and was U-boat transit water under Luftwaffe cover. If the Torch route ever needs
#    opening, the walls should YIELD during the operation, not be shouted over.
# [usa-refactor] the two date-staggered Torch blocks (1942.9 prep -2000 / 1942.11 landing
# -2000 + three flank walls) are MERGED - being both armed from 1942.11, their negatives summed
# to -4000; one block now carries -2000 + the flank walls. Still outvoted, so behaviour-neutral;
# the audit's net-per-window table shrinks accordingly (the -4000 window no longer exists).
# Windows re-walked after the date->state regate: wherever this block fires at war with GER,
# the +2000 walls (dont_waste_ships_here, invade_the_axis_..._france, FACTION coast block) are
# state-gated on the same German-held coast and co-fire, so the -2000 stays outvoted; the one
# corner with no wall (war with ITA only, GER absent) is PRE-EXISTING - the old enable had the
# same GER/ITA OR - and unchanged by this refactor.
#
# purpose: Torch corridor book - nominally open the Bay of Biscay (inert, see audit) and wall
#          the Balearic/Ionian flanks and the Norwegian Trench while the landing is on
# range:   -2000 (inert attraction) to +2000 (walls)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_torch_corridor = {

	allowed = {
		tag = USA
	}
	enable = {
		OR = {
			has_war_with = GER
			has_war_with = ITA
		}
		is_in_faction_with = ENG
		WA_AI_MILITARY_has_minimum_expeditionary_army = yes
		ENG = { surrender_progress < 0.2 }
		OR = {
			458 = { CONTROLLER = { has_war_with = ROOT } }
			665 = { CONTROLLER = { has_war_with = ROOT } }
			460 = { CONTROLLER = { has_war_with = ROOT } }
			459 = { CONTROLLER = { has_war_with = ROOT } }
			461 = { CONTROLLER = { has_war_with = ROOT } }
			1055 = { CONTROLLER = { has_war_with = ROOT } }
			462 = { CONTROLLER = { has_war_with = ROOT } }
			1056 = { CONTROLLER = { has_war_with = ROOT } }
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 42 #Bay of Biscay
		value = -2000 #open - inert, outvoted by the walls; see the audit above
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 219 #Baleric Sea
		value = 2000
	}
	# [atlantic-naval] (2026-08-14): dropped `id = 220`. Region 220 is Chahar - a LAND
	# strategic region with no naval_terrain - so the entry never did anything.
	ai_strategy = {
		type = naval_avoid_region
		id = 218 #Ionian Sea
		value = 2000
	}
	# [atlantic-naval] (2026-08-14, checklist R36): dropped `id = 112` (Azores Region) - a
	# stray 1500 km west of the Torch route; the south corridor owns 112 (dominance 70,
	# pull -1000). Same class of defect as the 244 stray in smart_pacific_fleet.
	ai_strategy = {
		type = naval_avoid_region
		id = 173 #Norwegian Trench
		value = 2000
	}
}


# purpose: while the Pacific offensive is not ready, wall the far-Pacific and Indian-Ocean
#          waters (Europe first). The deleted bad_torpedos_suck naval twin was a strict SUBSET
#          of this region list summing to 4000 on 19 shared regions while both armed - stack
#          deliberately not preserved, one wall at the type's band (see the land twins).
# range:   +2000 (walls)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_europe_first = {

	allowed = {
		original_tag = USA
	}
	enable = {
		NOT = { WA_AI_MILITARY_USA_pacific_offensive_ready = yes }
		OR = {
			has_war_with = JAP
			threat > 0.70
			is_in_faction_with = ENG
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 177 #W N Pacific
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 88
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 96
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 84 #Bismark Sea
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 82 #Gulf of carpentaria
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 60 #W Indian Ocean
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 85 #SW Indian Ocean
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 74 #S Indian Ocean
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 99 #East Indian Ocean
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 67
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 79
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 81
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 83
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 93
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 72
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 73
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 71
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 101
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 91
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 92
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 98
		value = 2000
	}
}


# purpose: once the Pacific offensive is ready, keep the USN out of the Indian-Ocean, Malacca
#          and SW-Pacific rear waters (13 regions shared with europe_first, incl. 84/88) - the
#          offensive runs through the central Pacific. Formerly gated date > 1944.8; the old
#          date pair left a 1943.6-1944.8 GAP with no wall at all - the mirror flip closes it
#          (walls continuous across the phase change, the anti-shuffling direction). ASSUMED
#          the gap was date residue, not a designed roaming window - campaign probe: USN
#          presence in 84/88 during a Solomons-type war.
# range:   +2000 (walls)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_pacific_offensive = {

	allowed = {
		tag = USA
	}
	enable = {
		has_war_with = JAP
		WA_AI_MILITARY_USA_pacific_offensive_ready = yes
	}

	abort_when_not_enabled = yes

	# [atlantic-naval] (2026-08-14): removed duplicate entries for 72 Straits of Malacca,
	# 73 Gulf of Thailand and 93 Banda Sea. naval_avoid_region is Additive per
	# region, so each listed id was summing to 4000 - double every other region in
	# the same block, for no stated reason. Intended single values kept.
	ai_strategy = {
		type = naval_avoid_region
		id = 93
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 72
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 73
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 88
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 81
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 83
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 84
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 71
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 101
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 99
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 91
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 92
		value = 2000
	}
	ai_strategy = {
		type = naval_avoid_region
		id = 98
		value = 2000
	}
}


# purpose: keep US convoy raiders off the Mediterranean chokepoints and the Bay (the escorts
#          own those waters) and send them to the Japanese supply lanes instead
# range:   -1000 (keep out) to +1000 (raid here)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_convoy_raid_strategy = {
	allowed = {
		tag = USA
	}

	enable = {
		has_war = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 69
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 327
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 202
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 269
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 218
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 168
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 373 #ligurian sea
		value = -1000
	}
	ai_strategy = {
		type = naval_convoy_raid_region
		id = 169
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 29
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 219
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 68
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 249
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 309
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 42
		value = -1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 94
		value = 1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 225
		value = 1000
	}

	ai_strategy = {
		type = naval_convoy_raid_region
		id = 350
		value = 1000
	}
}


# purpose: keep the peacetime/early-war Pacific fleet out of the western trap waters until the
#          offensive is ready (formerly `date > 1941.5.1` in and `date < 1945.1.1` out - now
#          armed by threat/war and released by the phase trigger, so the walls drop exactly
#          when the invasion fleets need those waters)
# range:   +2000 (walls)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_smart_pacific_fleet = {
	allowed = {
		tag = USA
	}
	enable = {
		OR = {
			has_war_with = JAP
			threat > 0.50
		}
		NOT = { WA_AI_MILITARY_USA_pacific_offensive_ready = yes }
		NOT = {
			386 = { CONTROLLER = { has_war_with = ROOT } }
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 232 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 171 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 114 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 176 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 95 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 221 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 222 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 32 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 113
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 111
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 306
		value = 2000
	}

	# [atlantic-naval] (2026-08-14, checklist R36): dropped `id = 244` (Greenland Iceland
	# Rise) - a north-Atlantic stray in an all-Pacific block that left the USA leg of the
	# north corridor at 7 regions of 8. Nothing else changes; the stray id, not a retune.
}


# purpose: while an enemy holds the western-European coast, no ships in the Bay of Biscay
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_dont_waste_ships_here = {
	allowed = {
		tag = USA
	}
	enable = {
		6 = { CONTROLLER = { has_war_with = ROOT } }
		29 = { CONTROLLER = { has_war_with = ROOT } }
		785 = { CONTROLLER = { has_war_with = ROOT } }
		15 = { CONTROLLER = { has_war_with = ROOT } }
		14 = { CONTROLLER = { has_war_with = ROOT } }
		30 = { CONTROLLER = { has_war_with = ROOT } }
		23 = { CONTROLLER = { has_war_with = ROOT } }
		19 = { CONTROLLER = { has_war_with = ROOT } }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 42 #bay of biscay
		value = 2000
	}
}


# purpose: keep the USN out of the St. Lawrence river (a Country-layer trap the DEFAULT layer
#          does not cover; formerly also gated `date > 1939.1.1` - the wall is wanted in any
#          year)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_smart_atlantic_fleet = {
	allowed = {
		tag = USA
	}
	enable = {
		NOT = {
			468 = { CONTROLLER = { has_war_with = ROOT } }
		}
	}

	abort_when_not_enabled = yes

	# [atlantic-naval] (2026-08-14, checklist R36): trimmed from eight regions to one. Each
	# removal has a named owner elsewhere - none of this is a loosening:
	#   57  Sargasso Sea       -> south corridor (dominance 70, pull -1000). This
	#                             was the second-largest contradiction of the plan
	#                             after ENG's ungated atlantic_sea_zone_corridors.
	#   49  North Atlantic Ridge  \
	#   51  Mid Atlantic gap       > atlantic_off_corridor_avoidance, +1000 each.
	#   58  Central Atlantic Gap   / Same intent, one owner, inside the type's band
	#   256 South Atlantic Gap    /  instead of the 3000 these stacked to.
	#   307 Gulf of Lawrence    -> WA_AI_NAVAL_DEFAULT_..._avoid_sea_region_307_area
	#   56  Limfjord            -> WA_AI_NAVAL_DEFAULT_..._avoid_sea_region_56_area
	# The two DEFAULT blocks reach every country, so those were duplicates.
	#
	# 322 St. Lawrence River is kept: it is the block's only entry with no owner at
	# another layer, and a USN fleet wandering up the St. Lawrence is exactly the
	# kind of country-specific trap a Country-layer block should still catch.
	ai_strategy = {
		type = naval_avoid_region
		id = 322 #St. Lawrence River
		value = 2000
	}
}


# purpose: keep the fleet out of the Gulf of Panama's far side while Central America is quiet
#          (formerly also gated `date > 1939.1.1`)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_smart_central_america_fleet = {
	allowed = {
		tag = USA
	}
	enable = {
		NOT = {
			814 = { CONTROLLER = { has_war_with = ROOT } }
		}
		NOT = {
			813 = { CONTROLLER = { has_war_with = ROOT } }
		}
		NOT = {
			476 = { CONTROLLER = { has_war_with = ROOT } }
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 52 #
		value = 2000
	}
}


# purpose: wall the Adriatic while its coast is not friendly (formerly `date > 1939.1.1` plus
#          a `date < 1942.3.1` always-avoid leg - the control test is the real gate in any
#          timeline)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_smart_axis_coast_adriatic = {
	allowed = {
		tag = USA
	}
	enable = {
		NOT = {
			156 = {
				CONTROLLER = {
					is_in_faction_with = USA
				}
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 168 #
		value = 2000
	}
}


# purpose: wall the Aegean while its coast is not friendly (same recut as the Adriatic twin)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_smart_axis_coast_aegean = {
	allowed = {
		tag = USA
	}
	enable = {
		NOT = {
			47 = {
				CONTROLLER = {
					is_in_faction_with = USA
				}
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 202 #
		value = 2000
	}
}


# purpose: while at peace with both Chinas, stay out of their coastal waters (do not tip the
#          Sino-Japanese war by parking a fleet in it)
# range:   +2000 (walls)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_stop_china_from_falling_seazone = {
	allowed = {
		tag = USA
	}
	enable = {
		NOT = { has_war_with = CHI }
		NOT = { has_war_with = PRC }
	}

	abort = {
		OR = {
			has_war_with = CHI
			has_war_with = PRC
		}
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 77 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 76 #
		value = 2000
	}
}


# purpose: home-base the strike forces on the Pacific anchorages while at peace or at war with
#          Japan (formerly `date < 1944.1.1` kept them Pacific pre-war; peacetime is the
#          dynamic equivalent - in a Europe-only war these disarm and the Faction western
#          anchorage (atlantic_home_base_western) or protect_eastern_waters takes over)
# range:   n/a (strike_force_home_base)
# policy:  Exclusive per region (ownership triggers)
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_pacific_strike_bases = {
	allowed = {
		original_tag = USA
	}
	enable = {
		OR = {
			has_war_with = JAP
			has_war = no
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = strike_force_home_base
		id = 349 # pearl harbor
	}

	ai_strategy = {
		type = strike_force_home_base
		id = 224 #S.E. Emperor Chain
	}

	# [atlantic-naval] (2026-08-14): dropped `id = 234` (Northern Borneo). All of its
	# provinces are type `land` in map/definition.csv, so it can never host a
	# strike force home base. Intent was probably a western-Pacific anchorage;
	# deliberately NOT guessed at a replacement id here - Phase 0 only removes
	# entries that provably do nothing.
	ai_strategy = {
		type = strike_force_home_base
		id = 180 #Micronesian Gap
	}
}


# purpose: home-base the strike forces on the Atlantic seaboard when the Atlantic is the live
#          naval war (mirrored by WA_AI_MILITARY_USA_protect_eastern_waters_active in
#          WA_AI_MILITARY_ownership_triggers.txt - edit both in the same commit)
# range:   n/a (strike_force_home_base)
# policy:  Exclusive per region (ownership slugs strike_force_home_base_55/54)
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_protect_eastern_waters = {
	allowed = {
		original_tag = USA
	}
	enable = {
		OR = {
			AND = {
				is_in_faction_with = ENG
				has_global_flag = ENG_fall_of_the_uk
			}
			has_war_with = ENG
			AND = {
				NOT = { is_in_faction_with = ENG }
				has_war_with = GER
			}
			AND = {
				NOT = { is_in_faction_with = ENG }
				has_war_with = ITA
			}
			AND = {
				NOT = { is_in_faction_with = ENG }
				has_war_with = FRA
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = strike_force_home_base
		id = 55 # Newfoundland Sea (the actual US East Coast region is 54, set below)
	}

	ai_strategy = {
		type = strike_force_home_base
		id = 54 #
	}

	ai_strategy = {
		type = strike_force_home_base
		id = 170 #
	}
}


# purpose: avoid Philippine waters while the islands are enemy-held
# range:   +2000 (walls)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_philippines_is_a_death_trap = {
	allowed = {
		original_tag = USA
	}
	enable = {
		AND = {
			NOT = { 623 = { CONTROLLER = { is_in_faction_with = ROOT } } }
			NOT = { 623 = { CONTROLLER = { is_subject_of = ROOT } } }
			NOT = { 623 = { CONTROLLER = { tag = ROOT } } }
			NOT = { 327 = { CONTROLLER = { is_in_faction_with = ROOT } } }
			NOT = { 327 = { CONTROLLER = { is_subject_of = ROOT } } }
			NOT = { 327 = { CONTROLLER = { tag = ROOT } } }
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 75 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 231 #
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 252 #
		value = 2000
	}
}


# purpose: avoid the Japanese home waters until the offensive is ready to enter them
#          (formerly `date < 1945.9.1`; the phase trigger releases the walls exactly when the
#          invade-Japan book arms). A USA that never reaches the trigger's capability arms
#          keeps these walls up - INTENDED caution, not a lock: the trigger's enemy-state
#          arms (JAP crumbling, GER dead/no GER war) are the state-side release.
# range:   +2000 (walls)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_home_islands_are_a_death_trap = {
	allowed = {
		original_tag = USA
	}
	enable = {
		has_war_with = JAP
		NOT = { WA_AI_MILITARY_USA_pacific_offensive_ready = yes }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 87
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 90
		value = 2000
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 79
		value = 2000
	}
}


# purpose: avoid Okinawan waters while Japan holds the island in strength and the offensive is
#          not ready (formerly `date < 1944.12.1`)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_okinawa_is_a_death_trap = {
	allowed = {
		original_tag = USA
	}
	enable = {
		NOT = { WA_AI_MILITARY_USA_pacific_offensive_ready = yes }
		526 = {
			CONTROLLER = {
				has_war_with = USA
			}
		}
		JAP = {
			has_full_control_of_state = 526
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 240 #
		value = 2000
	}
}


# purpose: avoid Iwo Jima's waters while the enemy holds it and the offensive is not ready
#          (formerly `date < 1944.1.1`)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_iwo_jima_is_a_death_trap = {
	allowed = {
		original_tag = USA
	}
	enable = {
		NOT = { WA_AI_MILITARY_USA_pacific_offensive_ready = yes }
		645 = {
			CONTROLLER = {
				has_war_with = USA
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 94 #
		value = 2000
	}
}


# purpose: stay out of Malaya/Borneo waters while the Commonwealth holds them uninvaded
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_avoid_uninvaded_malaya_borneo = {
	allowed = {
		tag = USA
	}
	enable = {
		639 = { is_controlled_by = UKO }
		725 = { is_controlled_by = UKO }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = naval_avoid_region
		id = 227
		value = 2000
	}
}


# purpose: stay out of the Tasman Sea while Australia/New Zealand hold their own waters
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_avoid_uninvaded_australia_new_zealand = {
	allowed = {
		tag = USA
	}
	enable = {
		OR = {
			636 = { is_controlled_by = ENG }
			726 = { is_controlled_by = NZL }
		}
	}
	abort = {
		OR = {
			USA = { has_war_with = ENG }
			USA = { has_war_with = NZL }
			636 = { is_controlled_by = JAP }
			726 = { is_controlled_by = JAP }
		}
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 178
		value = 2000
	}
}


# Migrated from common/ai_strategy/USA.txt:3665-3694 (NAVAL domain)

# [atlantic-naval] (2026-08-14, checklist R36): WA_AI_NAVAL_COUNTRY_USA_legacy_USA_war_against_axis PROMOTED to
# WA_AI_NAVAL_FACTION_ALLIES_italian_coastal_seas and removed here. It was identical to the ENG block of the same name - same three regions, same
# enable, only the `allowed` tag differed. Both are gone; the promoted block is
# gated on WA_AI_CONFIG_MILITARY_is_anglo_major.


# purpose: once the Allied side holds the Maghreb and no metropolitan-France foothold exists
#          yet, re-wall the Bay of Biscay (Torch is over, the Bay is U-boat water again;
#          formerly `date < 1944.4.20` - the foothold gate matches the land twin)
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_torch_done_biscay_wall = {
	allowed = {
		tag = USA
	}
	enable = {
		OR = {
			has_war_with = GER
			has_war_with = ITA
		}
		OR = {
			has_war_with = VIC
			VIC = { exists = no }
		}
		is_in_faction_with = ENG
		NOT = {
			OR = {
				15 = { CONTROLLER = { OR = { tag = ROOT is_in_faction_with = ROOT } } }
				21 = { CONTROLLER = { OR = { tag = ROOT is_in_faction_with = ROOT } } }
			}
		}
		NOT = { ENG = { surrender_progress > 0.2 } }
		OR = {
			458 = { CONTROLLER = { OR = { tag = ROOT is_in_faction_with = ROOT } } }
			665 = { CONTROLLER = { OR = { tag = ROOT is_in_faction_with = ROOT } } }
			460 = { CONTROLLER = { OR = { tag = ROOT is_in_faction_with = ROOT } } }
			459 = { CONTROLLER = { OR = { tag = ROOT is_in_faction_with = ROOT } } }
			461 = { CONTROLLER = { OR = { tag = ROOT is_in_faction_with = ROOT } } }
			462 = { CONTROLLER = { OR = { tag = ROOT is_in_faction_with = ROOT } } }
		}
	}
	abort_when_not_enabled = yes


	ai_strategy = {
		type = naval_avoid_region
		id = 42 #Bay of Biscay
		value = 2000 #wall
	}
}


# purpose: avoid the Norwegian Trench while Germany holds the Bight coast
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_invade_the_axis_avoid_regions_germany = {
	allowed = {
		tag = USA
	}
	enable = {
		has_war_with = GER
	}
	abort = {
		OR = {
			NOT = { has_war_with = GER }
			36 = { is_controlled_by = HOL }
			NOT = { 58 = { is_controlled_by = GER } }
			NOT = { 803 = { is_controlled_by = GER } }
			NOT = { 59 = { is_controlled_by = GER } }
			NOT = { 56 = { is_controlled_by = GER } }
		}
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 173 #Norwegian Trench
		value = 2000
	}
}


# purpose: avoid the Bay of Biscay while Germany holds the French Atlantic coast
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_invade_the_axis_avoid_regions_france = {
	allowed = {
		tag = USA
	}
	enable = {
		has_war_with = GER
	}
	abort = {
		OR = {
			NOT = { has_war_with = GER }
			14 = { is_controlled_by = FRA }
			30 = { is_controlled_by = FRA }
			23 = { is_controlled_by = FRA }
			19 = { is_controlled_by = FRA }
		}
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 42 #Bay of Biscay
		value = 2000
	}

	# [atlantic-naval] (2026-08-14, checklist R36): dropped `id = 112` (Azores Region) - the
	# second of the two Azores strays, see the torch corridor block above. This block
	# is about staying out of the German-held French Atlantic coast; the Azores are
	# neutral Portuguese islands 1500 km west of it and the enable never mentions
	# them. It fires on `has_war_with = GER` alone, so on the historical path it was
	# walling the USN off the Azores from December 1941 to the end of the war.
}


# purpose: avoid the Voring Plateau while Germany occupies a capitulated Norway
# range:   +2000 (wall)
# policy:  Additive per region
# domain:  NAVAL
WA_AI_NAVAL_COUNTRY_USA_invade_the_axis_avoid_regions_norway = {
	allowed = {
		tag = USA
	}
	enable = {
		has_war_with = GER
		GER = { has_war_with = NOR }
		NOR = { has_capitulated = yes }
	}
	abort = {
		OR = {
			NOT = { has_war_with = GER }
			36 = { is_controlled_by = HOL }
		}
	}

	ai_strategy = {
		type = naval_avoid_region
		id = 174 #Voring Plateau (Norwegian coast approaches)
		value = 2000
	}
}
