############################################################################################################
# WA AI Military V2 - COUNTRY_RAJ_FRONT
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { force_concentration_front_factor, force_concentration_target_weight, force_ratio, front_armor_score, front_control, front_unit_request, garrison, infantry }.
# Canonical domain: FRONT. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.


# Phase 6 split from WA_AI_MILITARY_RAJ_kill_ITS (FRONT domain)
WA_AI_MILITARY_RAJ_asia_first_east_africa_FRONT = { # was RAJ_kill_ITS_FRONT (Fix 97)

	allowed = {
		tag = RAJ
	}
	enable = {
		OR = {
			446 = { is_controlled_by = ENG }
			447 = { is_controlled_by = ENG }
			923 = { is_controlled_by = ENG }
			446 = { is_controlled_by = UKE }
			447 = { is_controlled_by = UKE }
			923 = { is_controlled_by = UKM }
		}
		is_in_faction_with = ENG
		# Fix 97 (2026-08-17): was `NOT war with PER/SOV/JAP` + `country_exists = ITS` + `date < 1941.5.1` with a
		# +200 pull and an execute toward ITS. The pull and the execute now come from the Faction
		# WA_AI_MILITARY_ALLIES_east_africa_* family (tag-free); what is India-specific is the recorded
		# reason for the exclusions - an Asian war comes first - so this block is now the SUPPRESSION side:
		# while the RAJ fights in Asia it does not send divisions to East Africa.
		OR = {
			has_war_with = PER
			has_war_with = SOV
			has_war_with = JAP
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = WA_AI_MILITARY_east_africa_regions
		value = -100 # E2 floor (full veto of the +150 Faction pull is not needed; -100 already nets it to +50)
	}
}

############################################################################################################
# WA Military V2 - RAJ Country Overlay
############################################################################################################
# Phase 2 split: FRONT domain blocks.
# Phase 2 split: FRONT domain blocks.

WA_AI_MILITARY_RAJ_core_front_requests = {
	allowed = { tag = RAJ }
	enable = {
		OR = {
			has_war_with = JAP
			surrender_progress > 0
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		strategic_region = 300
		value = 200 # economy: was 1500
	}

	ai_strategy = {
		type = front_unit_request
		strategic_region = 330
		value = 200 # economy: was 1500
	}

	ai_strategy = {
		type = front_unit_request
		strategic_region = 31
		value = 200 # economy: was 1500
	}

	ai_strategy = {
		type = front_unit_request
		strategic_region = 153
		value = 200 # economy: was 1500
	}

	ai_strategy = {
		type = front_unit_request
		strategic_region = 189
		value = 50
	}

	ai_strategy = {
		type = garrison
		value = -5000 # economy: was -1000
	}
}



WA_AI_MILITARY_RAJ_unit_distribution = {
	allowed = {
		original_tag = RAJ
	}
	enable = {
		has_war = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = france
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		area = south_france
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		area = west_france
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		area = north_france
		value = -100 # economy: was -1000
	}
}


# Phase 6 split from WA_AI_MILITARY_RAJ_abandon_africa (FRONT domain)
WA_AI_MILITARY_RAJ_abandon_africa_FRONT = {

	allowed = {
		tag = RAJ
	}
	enable = {
		NOT = {
			OR = {
				446 = { is_controlled_by = ENG }
				447 = { is_controlled_by = ENG }
				923 = { is_controlled_by = ENG }
				446 = { is_controlled_by = UKE }
				447 = { is_controlled_by = UKE }
				923 = { is_controlled_by = UKM }
			}
		}
		OR = {
			is_in_faction_with = ENG
			is_subject_of = ENG
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = central_africa
		value = -100 # economy: was -2000
	}
	ai_strategy = {
		type = front_unit_request
		area = south_africa
		value = -100 # economy: was -2000
	}
	ai_strategy = {
		type = front_unit_request
		area = north_africa
		value = -100 # economy: was -2000
	}
}
