# WA AI Military V2 - COUNTRY_POL_THEATRE
# Canonical domain: THEATRE. Created Phase 7c for relocations out of legacy POL.txt.

# purpose: Poland faces the German border while Germany is fascist (relocated Phase 7c from legacy POL.txt POL_war_with_GER)
# range:   +1000 per area (saturated legacy values, E1 worklist)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_POL_face_germany_THEATRE = {
	allowed = {
		tag = POL
	}
	enable = {
		GER = { has_government = fascism }
	}
	abort = {
		OR = {
			NOT = { GER = { has_government = fascism } }
			has_war_with = SOV
		}
	}

	ai_strategy = {
		type = area_priority
		id = east_europe
		value = 1000
	}

	ai_strategy = {
		type = area_priority
		id = germany
		value = 1000
	}
}
