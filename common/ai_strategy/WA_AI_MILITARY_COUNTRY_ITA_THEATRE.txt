############################################################################################################
# WA AI Military V2 - COUNTRY_ITA_THEATRE
############################################################################################################
# Every ai_strategy in this file has type ∈ { area_priority, put_unit_buffers, spare_unit_factor, theatre_distribution_demand_increase }.
# Canonical domain: THEATRE. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.


WA_AI_MILITARY_ITA_crush_ethiopia_1_THEATRE = {

	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_crush_ethiopia_1_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = south_africa
		value = 200 # economy: was 500
	}
	ai_strategy = {
		type = area_priority
		id = central_africa
		value = 200 # economy: was 500
	}
}


WA_AI_MILITARY_ITA_struggle_in_ethiopia_THEATRE = {

	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_struggle_in_ethiopia_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = central_africa
		value = 200 # economy: was 500
	}
}


WA_AI_MILITARY_ITA_prolong_the_struggle_in_ethiopia_THEATRE = {

	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_prolong_the_struggle_in_ethiopia_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = central_africa
		value = 200 # economy: was 500
	}
}


# purpose: [med-axis-posture] - Italy's main effort is North Africa while it holds an African-shore port
#          (Libyan four OR Tunis/Bizerte, so a Tunis-only foothold after the loss of Libya keeps the
#          effort alive) and the home is safe. Fires at peace too, for pre-positioning, and at war with
#          whoever controls East Egypt / Tunisia / Algeria - controller-based, no ENG/FRA tag, so an
#          ahistorical enemy there gets the same effort. Ethiopia's conquest phase keeps precedence.
# range:   +200 (E1) - the E1 ceiling, and this is the sole ITA-scope +200 writer for north_africa
# policy:  Additive per area (the FRONT-layer pair beside it is the +60 front_unit_request)
# domain:  THEATRE
WA_AI_MILITARY_ITA_africa_pull_THEATRE = {
	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_africa_pull_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = north_africa
		value = 200
	}
}

# purpose: [med-axis-posture] - the bridgehead is still held but the home is threatened (belt ground lost, Tunisia
#          occupied): Africa keeps a token pull so the line is not abandoned outright, the home tiers
#          in WA_AI_MILITARY_REGION_ITALY_THEATRE.txt take the rest. Mutually exclusive with the two
#          blocks around it by construction (held ∧ threatened ∧ NOT invaded).
# range:   +50 (E1)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_ITA_africa_pull_reduced_THEATRE = {
	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_african_shore_port_held = yes # [med-axis-posture] a Tunis-only port keeps the token pull
		WA_AI_MILITARY_italy_home_threatened = yes
		NOT = { WA_AI_MILITARY_italy_homeland_invaded = yes }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = north_africa
		value = 50
	}
}

# purpose: [med-axis-posture] - give up on North Africa: no African-shore port left to our side (Libyan four
#          + Tunis + Bizerte, WA_AI_MILITARY_african_shore_port_held), or an enemy stands on the Italian
#          mainland / Sicily. Either branch alone fires: a tag-list bar over all four Libyan states is a
#          bar an army can die in place without ever meeting. Releases itself when a port is retaken and
#          the home soil is clear.
# range:   -200 (E1)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_ITA_abandon_north_africa_THEATRE = {
	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_abandon_north_africa_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = north_africa
		value = -200
	}
}

# purpose: [med-axis-posture] - Italy garrisons friendly rear-area ground in Southern France and Greece.
#          Southern France is split shore (9610) / inland (9614) because WHICH state inside a `states`
#          list gets a division is engine-internal: the split cannot steer placement, it shrinks the
#          list the engine spreads over until the landable coast is most of it. Independent order_id
#          per sector - cross-block order_id arbitration is not measurable from a save. Rear-area
#          buffers yield to a land front in their own area so the front supplies the defenders instead
#          of the reserve duplicating them; the anti-landing shore buffer does not (see its pool note).
#          The Albania/Otranto gate is the separate block below.
# range:   0.08 / 0.03 / 0.15 (E4)
# policy:  Additive per state
# domain:  THEATRE
WA_AI_MILITARY_ITA_protect_periphery_THEATRE = {
	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_protect_periphery_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers
		order_id = 9610
		# [med-axis-posture] the Mediterranean shore of region 20 only - Provence (Toulon, Marseille), Languedoc,
		# French Alpes. These are the states an amphibious landing can take.
		states = {
			21
			22
			735
		}
		ratio = 0.08
		area = south_france
		# pool: [med-axis-posture] - anti-landing reserve, must NOT be subtracted away by a front elsewhere in
		# region 20 (lessons-log put_unit_buffers entry)
		subtract_fronts_from_need = no
		subtract_invasions_from_need = no
	}
	ai_strategy = {
		type = put_unit_buffers
		order_id = 9614
		# [med-axis-posture] the inland rear area of region 20 - no coast, so no landing; a third of the
		# shore's weight, and it yields to a front.
		states = {
			20
			25
			26
			31
			32
			33
		}
		ratio = 0.03
		area = south_france
		# pool: rear-area reserve; an active front in Southern France replaces, rather than adds to, it
		subtract_fronts_from_need = yes
		subtract_invasions_from_need = no
	}
	ai_strategy = {
		type = put_unit_buffers
		order_id = 9611
		# Greece's seven 1936 states, plus the Italian Dodecanese
		states = {
			47
			184
			185
			186
			187
			182
			731
			164
		}
		ratio = 0.15
		area = south_balkans
		# pool: rear-area reserve; an active Greek front replaces, rather than adds to, it
		subtract_fronts_from_need = yes
		subtract_invasions_from_need = no
	}
}

# purpose: garrison the ground behind Otranto only while an enemy can still enter the Adriatic
#          through it. Once the two gate controllers are not fighting each other, and every Italian
#          enemy is at war with the controller of either required gate province (11998 in Calabria /
#          11767 in Albania), the strait rule denies that enemy naval access and this rear-area
#          reserve aborts. It returns if Otranto becomes contested or one enemy has neutral access
#          again. Kept separate so the Otranto gate never disables the Southern-France or Greek
#          buffers. [med-axis-posture] order 9613 covers the whole eastern shore, not Albania alone:
#          otherwise the engine holds Split/Zadar/Gorski Kotar with its OWN area-defence orders while
#          the strait is shut and this block has correctly aborted (the `garrison = -5000` elsewhere is
#          the other half that stops it). Those shore states sit in strategic region 24, already inside
#          `area = central_balkans` - no new ai_area, the engine's 72-definition ai_area budget is full.
# range:   0.10 / 0.06 (E4)
# policy:  Additive per state
# domain:  THEATRE
WA_AI_MILITARY_ITA_protect_adriatic_gate_THEATRE = {
	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_protect_adriatic_gate_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers
		order_id = 9612
		states = {
			44 # Albania
		}
		ratio = 0.10
		area = central_balkans
		# pool: rear-area reserve; an active Balkan front replaces, rather than adds to, it
		subtract_fronts_from_need = yes
		subtract_invasions_from_need = no
	}
	ai_strategy = {
		type = put_unit_buffers
		order_id = 9613
		# [med-axis-posture] the eastern Adriatic shore behind Otranto. Istria and Primorska head the list
		# because they are the deepest a landing here can cut; Zadar and Split are the ports. If Italy
		# holds none of them the strategy is simply invalid (documentation.info: "if no state is
		# friendly, strat is invalid"), which is what makes the list safe in an ahistorical Balkans.
		states = {
			736  # Istria
			886  # Primorska
			1022 # Gorski Kotar
			163  # Zadar
			908  # Split
		}
		ratio = 0.06
		area = central_balkans
		# pool: [med-axis-posture] - this IS the anti-landing reserve of the shore, so unlike the Albanian
		# buffer above it must not be subtracted away by a partisan / Balkan front inland (lessons-log
		# put_unit_buffers entry)
		subtract_fronts_from_need = no
		subtract_invasions_from_need = no
	}
}
# purpose: [med-axis-posture] - the Libyan garrison; only while a bridgehead port is ours (own / subject /
#          ally-with-enemy-opposite) and the home is not invaded, because a buffer on ground we are
#          abandoning just idles divisions in Tripoli. Same peace-time pre-positioning clause as
#          protect_periphery above. Deliberately gated on libya_bridgehead_held, NOT
#          african_shore_port_held - this buffer sits on Libyan states so it needs a Libyan port; the
#          Tunisian garrison is the Faction-layer AXIS_tunis_bridge_THEATRE.
# range:   0.25 (E4)
# policy:  Additive per state
# domain:  THEATRE
WA_AI_MILITARY_ITA_protect_libya_THEATRE = {
	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_protect_libya_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers
		order_id = 9609
		# north_africa
		states = {
			448
			450
			451
		}
		ratio = 0.25 # economy: was 1.2
		area = north_africa
		subtract_fronts_from_need = no # pool: [prospecting-coop] Libya must stay garrisoned while the Egyptian front asks for everything; the default (yes) lets that front eat the whole need
		subtract_invasions_from_need = no
	}
}

# purpose: [med-axis-posture] - Italy garrisons the Egyptian ports its side conquers (Alexandria 447, Marsa
#          Matruh 452, Suez 923) while the German expedition - or Italy's own offensive - pushes east:
#          Germany spearheads, Italy holds the rear; the line itself is the +60 front_unit_request in
#          ITA_FRONT. The gate deliberately SURVIVES full conquest of the East-Egypt anchors - the war
#          branch holds it while the advance still fights, the control branch after the ports fall,
#          because a garrison that dissolves at the moment of victory is no garrison. The control
#          branch uses the one-side definition so a GER-conquered port still gets its Italian garrison.
#          `area = north_africa` on purpose: the engine's ai_area database is at its 72-slot ceiling
#          (a 73rd definition crashes on boot), so Egypt has no alias of its own and shares
#          protect_libya's blob and residual. ASSUMED, and the fail-tell for this block: a buffer whose
#          states are still enemy-held sits unfilled until conquest - unverified, every prior buffer
#          here drew on states our side already owned.
# range:   0.10 (E4)
# policy:  Additive per state
# domain:  THEATRE
WA_AI_MILITARY_ITA_garrison_conquered_ports_THEATRE = {
	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_garrison_conquered_ports_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers
		order_id = 9616
		# Egyptian ports on the axis of advance
		states = {
			447
			452
			923
		}
		ratio = 0.10
		area = north_africa
		subtract_fronts_from_need = no # pool: [prospecting-coop] the Egyptian front must not eat the garrison's need
		subtract_invasions_from_need = no
	}
}


WA_AI_MILITARY_ITA_end_the_struggle_in_ethiopia_THEATRE = {

	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_end_the_struggle_in_ethiopia_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = south_africa
		value = 200 # economy: was 500
	}
	ai_strategy = {
		type = area_priority
		id = central_africa
		value = 200 # economy: was 500
	}
}


# purpose: [med-axis-posture] - the East-African colony gets a GARRISON, not half the army: without a stated
#          "enough", a neutral Ethiopia next door pulls most of the Italian army into Eritrea/Somaliland
#          and then into the Sudan/Kenya out of supply. 0.20 of the army spread over the five AOI states
#          with its own order_id is that "enough" - sized for a colony that must fight CUT OFF once at
#          war (the owner_cut_off pair closes the sea lane); it fills in peacetime while the seas are
#          open. The AXIS_abandon_east_africa family supplies the negative that keeps the rest away.
# range:   0.20 (E4)
# policy:  Additive per state
# domain:  THEATRE
WA_AI_MILITARY_ITA_east_africa_garrison_THEATRE = {
	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_should_ita_east_africa_garrison_theatre = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers
		order_id = 9607
		# [aoi-border-garrison] all five AOI states, not just the two port states: with {550,559} only,
		# the whole colonial army stood on Massawa/Mogadishu while the inland border fell (BHU 15176ce6,
		# 1940.9-10: 9/9 divisions in this buffer, Oromia/Somali/Amhara lost in two months). Assumes the
		# five states are all in strategic region 17 (verified against map/strategicregions 2026-08-27).
		states = {
			550
			559
			271
			909
			910
		}
		ratio = 0.20 # [aoi-border-garrison] was 0.10; the colony fights alone once cut off (owner order)
		# `area` = the ai_areas whose orders may draw on this buffer (documentation.info): the colony's own
		# region only, so a front INSIDE the AOI may use the garrison, the offensive into the
		# Sudan (217) may not
		area = WA_AI_MILITARY_east_africa_colony_regions
		subtract_invasions_from_need = no
		# pool: not sized down when a front opens next door - that shrinkage is how the colonial army ends
		# up inside enemy states out of supply
		subtract_fronts_from_need = no
	}
}

