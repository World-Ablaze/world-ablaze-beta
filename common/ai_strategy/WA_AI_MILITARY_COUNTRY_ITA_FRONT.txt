############################################################################################################
# WA AI Military V2 - COUNTRY_ITA_FRONT
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { force_concentration_front_factor, force_concentration_target_weight, front_armor_score, front_control, front_unit_request, garrison }.
# Canonical domain: FRONT. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.


# Phase 6 split from WA_AI_MILITARY_ITA_ignore_the_turks (FRONT domain)
WA_AI_MILITARY_ITA_ignore_the_turks_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		country_exists = TUR
		NOT = {
			has_war_with = TUR
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = "TUR"
		value = -100
	}
}


# Phase 6 split from WA_AI_MILITARY_ITA_war_with_greece (FRONT domain)
WA_AI_MILITARY_ITA_war_with_greece_FRONT = {

	allowed = {
		OR = {
			tag = ITA
			tag = GER
		}
	}
	enable = {
		has_completed_focus = ITA_war_with_greece
		country_exists = GRE
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = GRE
		ordertype = front
		execute_order = yes
	}
	ai_strategy = {
		type = front_unit_request

		strategic_region = 25
		value = 75
	}
}


# Phase 6 split from WA_AI_MILITARY_ITA_stop_crowding_up_the_soviet_line (FRONT domain)
WA_AI_MILITARY_ITA_stop_crowding_up_the_soviet_line_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_is_axis_member = yes
		has_war_with = SOV
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = south_russia
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		area = caucasus
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		area = north_russia
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		area = west_russia
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		area = crimea
		value = -100 # economy: was -1000
	}
}


# Phase 6 split from WA_AI_MILITARY_ITA_switzerland (FRONT domain)
WA_AI_MILITARY_ITA_switzerland_FRONT = {

	allowed = {
		tag = ITA
		is_historical_focus_on = yes
	}
	enable = {
		country_exists = SWI
		NOT = {
			has_war_with = SWI
		}
		date < 1946.1.1
		has_government = fascism
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = SWI
		value = -100 # economy: was -1000
	}
}



WA_AI_MILITARY_ITA_dont_walk_into_austria_trap = {
	allowed = {
		tag = ITA
	}
	enable = {
		has_war_with = GER
		date < 1940.1.1
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		strategic_region = 294
		ordertype = front
		execute_order = no
		manual_attack = no
	}
	ai_strategy = {
		type = front_control
		strategic_region = 7
		ordertype = front
		execute_order = no
		manual_attack = no
	}
}



WA_AI_MILITARY_ITA_careful_around_tunisia = {
	enable = {
		original_tag = ITA
		NOT = { is_in_faction_with = FRT }
		date < 1940.10.1
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = FRT
		value = -50
	}
}


# Phase 6 split from WA_AI_MILITARY_ITA_crush_ethiopia_1 (FRONT domain)
WA_AI_MILITARY_ITA_crush_ethiopia_1_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		has_war_with = ETH
		# Fix 98 (2026-08-17, campaign 0edbc955): was `date < 1937.1.1` + rule in {DEFAULT, FASCIST_HISTORICAL} -
		# FASCIST_ALTERNATE / RANDOM had NO execute order vs Ethiopia before 1937, nobody had one after 1938.
		# Now: every rule option except the deliberately slow DEMOCRATIC / MONARCHIST path (struggle -> prolong
		# -> end_the_struggle), for as long as the war lasts. crush_ethiopia_2 (the 1937-38 twin) merged here.
		NOT = { has_game_rule = { rule = ITA_ai_behavior option = DEMOCRATIC } }
		NOT = { has_game_rule = { rule = ITA_ai_behavior option = MONARCHIST } }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = ETH
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
	ai_strategy = {
		type = front_unit_request
		tag = ETH
		value = 200 # economy: was 500
	}
}


# Fix 98 (2026-08-17): WA_AI_MILITARY_ITA_crush_ethiopia_2_FRONT (the 1937.1.1-1938.1.1 twin, no rule gate) merged into
# crush_ethiopia_1_FRONT above, which now runs for the whole war on every rule except DEMOCRATIC / MONARCHIST. Those two
# keep struggle/prolong/end_the_struggle (end_the_struggle_THEATRE still carries the +200 area pulls for them; the +200
# `front_unit_request tag = ETH` of this block is the one thing the DEM/MON 1937-38 window loses).



# Phase 6 split from WA_AI_MILITARY_ITA_struggle_in_ethiopia (FRONT domain)
WA_AI_MILITARY_ITA_struggle_in_ethiopia_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		has_war_with = ETH
		date < 1936.05.16
		NOT = { has_full_control_of_state = 910 }
		OR = {
			AND = {
				has_game_rule = {
	 					rule = ITA_ai_behavior
	 					option = DEMOCRATIC
	 				}
			}
			AND = {
				has_game_rule = {
					rule = ITA_ai_behavior
					option = MONARCHIST
				}
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		state = 910
		priority=2
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
	ai_strategy = {
		type = front_control
		state = 909
		state = 271
		ordertype = front
		execution_type = careful
		execute_order = no
	}
}


# Phase 6 split from WA_AI_MILITARY_ITA_prolong_the_struggle_in_ethiopia (FRONT domain)
WA_AI_MILITARY_ITA_prolong_the_struggle_in_ethiopia_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		has_war_with = ETH
		date < 1936.05.16
		has_full_control_of_state = 910
		OR = {
			AND = {
				has_game_rule = {
	 					rule = ITA_ai_behavior
	 					option = DEMOCRATIC
	 				}
			}
			AND = {
				has_game_rule = {
					rule = ITA_ai_behavior
					option = MONARCHIST
				}
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		state = 910
		ordertype = front
		execute_order = no
	}
	ai_strategy = {
		type = front_control
		state = 909
		state = 271
		ordertype = front
		execute_order = no
	}
}


# Phase 6 split from WA_AI_MILITARY_ITA_war_against_ENG (FRONT domain)
# Fix 96: gate was the ITA/ITL Libyan controller list - now WA_AI_MILITARY_libya_bridgehead_held
# (own / subject / ally port), which also survives the ITL subject's annexation at the 1943 flip.
# Fix 108 (2026-08-19): the second gate was `NOT = { has_war_with = ETH }`, carried verbatim from the
# pre-convention import (`79d64f6ff`, alongside the #Komrade Libyan controller list). Its intended
# question is "is the 1935-36 colonial war over" - but it also reads TRUE for a liberated Ethiopia that
# re-enters the war as a co-belligerent, which is permanent. MEASURED on campaign 07270b64: ETH is at
# war with ITA from 1941.2.7 to the end, so this block - and its DIPLOMACY / NAVAL siblings, split from
# the same original - were dead for the rest of the war, and Italy spent 16 of 106 divisions on
# engine-generated coastal area-defence (Adriatic included) that this -5000 exists to suppress.
# WA_AI_MILITARY_ethiopian_war_finished asks the real question (ETH gone / capitulated / no Italy-vs-ETH
# war that is not already part of a major war) and is the gate the five sibling ITA blocks already use.
WA_AI_MILITARY_ITA_war_against_ENG_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		WA_AI_MILITARY_libya_bridgehead_held = yes
		WA_AI_MILITARY_ethiopian_war_finished = yes # Fix 108: was NOT = { has_war_with = ETH }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = garrison
		value = -5000 # economy: was -1000
	}
	ai_strategy = {
		type = garrison
		id = ITA
		value = -5000 # economy: was -2000
	}
}


# Phase 6 split from WA_AI_MILITARY_ITA_end_the_struggle_in_ethiopia (FRONT domain)
WA_AI_MILITARY_ITA_end_the_struggle_in_ethiopia_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		has_war_with = ETH
		date > 1936.05.15 # Fix 98: was 05.17 - contiguous with struggle/prolong (< 05.16); the two-day hole is closed
		OR = {
			AND = {
				has_game_rule = {
	 					rule = ITA_ai_behavior
	 					option = DEMOCRATIC
	 				}
			}
			AND = {
				has_game_rule = {
					rule = ITA_ai_behavior
					option = MONARCHIST
				}
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = ETH
		priority=5
		ratio=0.5
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
}


# purpose: R13 - Italy mans the North-African front. Audit finding (campaign 66d6b53c): ITA had zero
#          offensive front content toward Egypt - no front_unit_request, no execute_order vs the
#          Egypt-side enemy - and the desert front froze for 33 months. This block supplies the
#          divisions whenever Italy (or its ITL subject) holds a Libyan foothold and is at war with
#          whoever controls East Egypt; controller-based so it works ahistorically (any enemy in
#          Egypt, not hardcoded ENG). Deliberately NOT balance-gated: when the offensive is off the
#          divisions still have to hold the line. Home threat aborts it - Africa is triaged away
#          when the mainland is at risk (same reflex as the abandon_north_africa THEATRE rule).
# range:   +60 (E2 [-100, +200])
# policy:  Additive per area
# domain:  FRONT
WA_AI_MILITARY_ITA_north_africa_offensive_units_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		# Libyan bridgehead port, own / subject / ally (Fix 96: was the ITA/ITL controller list)
		WA_AI_MILITARY_libya_bridgehead_held = yes
		# At war with whoever controls East Egypt (anchor set of WA_AI_MILITARY_ENG_holds_east_egypt)
		OR = {
			446 = { CONTROLLER = { has_war_with = ROOT } }
			447 = { CONTROLLER = { has_war_with = ROOT } }
			453 = { CONTROLLER = { has_war_with = ROOT } }
		}
		# Fix 96: home threat is geographic (belt ground lost / Tunisia occupied / mainland invaded),
		# not the surrender_progress reading of WA_AI_MILITARY_home_threatened
		NOT = { WA_AI_MILITARY_italy_home_threatened = yes }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = north_africa
		value = 60
	}
}


# purpose: R13 - execute battle plans toward Egypt while the force balance supports it.
#          WA_AI_MILITARY_north_africa_offensive_viable consumes the weekly posture calculus
#          (posture_vs_<controller of East Egypt> > 0, or an enemy too small for a verdict). When
#          the balance turns unfavourable the verdict drops to 0, this block aborts, and the front
#          falls back to defence - the +60 unit request above stays up so the line remains manned.
#          Levels 1 and 2 both qualify; balanced execution is intended either way
#          (WA_AI_MILITARY_SOV_counterattack precedent).
#          Priority 100 on purpose: above an unset Country default of 0 (SPA lesson), below the
#          500/10000 equipment emergency stops in DEFAULT_FRONT_control - losing to
#          low_equipment_hold while starving is correct behaviour.
# range:   n/a (front_control)
# policy:  Exclusive per area (sole ITA-scope writer for area north_africa); outranked by the
#          level-500/10000 emergency stops in DEFAULT_FRONT_control.
# domain:  FRONT
WA_AI_MILITARY_ITA_north_africa_offensive_exec_FRONT = {

	allowed = {
		tag = ITA
	}
	enable = {
		# Same geographic gate as the unit-request block above (Fix 96 form)
		WA_AI_MILITARY_libya_bridgehead_held = yes
		OR = {
			446 = { CONTROLLER = { has_war_with = ROOT } }
			447 = { CONTROLLER = { has_war_with = ROOT } }
			453 = { CONTROLLER = { has_war_with = ROOT } }
		}
		NOT = { WA_AI_MILITARY_italy_home_threatened = yes }
		WA_AI_MILITARY_north_africa_offensive_viable = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		area = north_africa
		priority = 100
		ordertype = front
		execution_type = balanced
		execute_order = yes
		manual_attack = yes
	}
}


# purpose: Fix 97 - FRONT twin of WA_AI_MILITARY_ITA_ignore_neutral_ethiopia_DIPLOMACY: no divisions requested
#          against a neutral Ethiopia's border (WA_AI_MILITARY_ITA_switzerland_FRONT precedent for the form).
# range:   -100 (E2 veto; escape hatch = the same gate - the moment ETH is hostile it is off)
# policy:  Additive per target
# domain:  FRONT
WA_AI_MILITARY_ITA_ignore_neutral_ethiopia_FRONT = {
	allowed = {
		tag = ITA
	}
	enable = {
		country_exists = ETH
		NOT = { has_war_with = ETH }
		NOT = { ETH = { any_allied_country = { has_war_with = ROOT } } }
		NOT = { ETH = { any_country = { has_war_with = ROOT has_war_together_with = PREV } } }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = ETH
		value = -100
	}
}


# purpose: Fix 98 - the Ethiopian war is WON or LOST by two states, not by Addis. The scripted mission
#          ETH_push_into_ethiopia_mission (common/decisions/ITA.txt, 100 days from 1936.5.1) succeeds only
#          when Italy controls BOTH Amhara 910 AND Somali 909; on campaign 0edbc955 the AIFC sector anchored
#          on 271 (Addis, the industry/VP magnet) with objectives {271, 909} from March to August, the Somali
#          armies drained 11 -> 2 divisions toward Oromia, 909 stayed Ethiopian, the mission timed out and the
#          scripted peace declared Italy beaten while it was winning 17:1. This block steers the AIFC
#          objective set at the mission's states while either is still Ethiopian, and brakes Addis until then.
# aifc-tuning: scripted colonial-war schwerpunkt - complements the AIFC sector engine (which scores
#          industry / thin seams and therefore Addis) with the mission's own objective; boost and paired
#          suppression share this block's gate (E5). Fall Rot precedent: WA_AI_MILITARY_GER_fall_rot_*.
#          `force_concentration_target_weight state =` (literal ids) is the proven form; the sector engine's
#          own `_ref` arrays read unresolved tokens on this campaign - see the r98 checklist note.
# range:   +80 / -60 (E5: aifc-tuning); front_unit_request +50 per state (E2 - sums with the +200 tag pull
#          to +250 on the two Ethiopian segments, stated on purpose: the whole point is that these two
#          segments outweigh the Addis one)
# policy:  Additive
# domain:  FRONT
WA_AI_MILITARY_ITA_ethiopia_mission_objectives_FRONT = {
	# aifc-tuning: scripted colonial-war schwerpunkt - steers the AIFC objective set at the mission's two states (909/910) and brakes Addis until they are Italian; boost and suppression share this block's gate.
	allowed = {
		tag = ITA
	}
	enable = {
		has_war_with = ETH
		OR = {
			NOT = { 909 = { is_controlled_by = ROOT } }
			NOT = { 910 = { is_controlled_by = ROOT } }
		}
		# the aggressive path only - DEM/MON keep their slow, unsteered struggle
		NOT = { has_game_rule = { rule = ITA_ai_behavior option = DEMOCRATIC } }
		NOT = { has_game_rule = { rule = ITA_ai_behavior option = MONARCHIST } }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = force_concentration_target_weight
		state = 909 # Somali - mission state
		state = 910 # Amhara - mission state
		value = 80
	}
	ai_strategy = {
		type = force_concentration_target_weight
		state = 271 # Oromia / Addis - AIFC's natural magnet; braked until the mission states are ours
		value = -60
	}
	ai_strategy = {
		type = front_unit_request
		state = 909
		value = 50
	}
	ai_strategy = {
		type = front_unit_request
		state = 910
		value = 50
	}
}

