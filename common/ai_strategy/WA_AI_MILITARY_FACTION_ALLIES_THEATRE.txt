############################################################################################################
# WA AI Military V2 - FACTION_ALLIES_THEATRE
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { area_priority, put_unit_buffers, spare_unit_factor, theatre_distribution_demand_increase }.
# Canonical domain: THEATRE. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.


# Phase 6 split from WA_AI_MILITARY_ALLIES_ignore_raj_until_invasion (THEATRE domain)
WA_AI_MILITARY_ALLIES_ignore_raj_until_invasion_THEATRE = {

	allowed = {
		NOT = { tag = RAJ }
	}
	enable = {
		is_in_faction_with = RAJ
		has_war_with = GER
		RAJ = { surrender_progress < 0.1 }

		# Phase 5 mutual exclusion: yield to Country-layer overrides.
		NOT = { WA_AI_MILITARY_country_owns_ddab_RAJ = yes }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = malaysia
		value = -200 # economy: was -5000
	}
	ai_strategy = {
		type = area_priority
		id = east_asia
		value = -200 # economy: was -5000
	}
}


# Phase 6 split from WA_AI_MILITARY_ALLIES_war_against_ITA_central_africa (THEATRE domain)
WA_AI_MILITARY_ALLIES_war_against_ITA_central_africa_THEATRE = {

	allowed = {
		WA_AI_CONFIG_MILITARY_is_anglo_major = yes
	}
	enable = {
		OR = {
			has_war_with = ITA
			has_war_with = GER
		}
		date < 1943.7.1
		country_exists = ITS
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = north_africa
		value = -30 #divert resources from el alamein
	}
}


# Phase 6 split from WA_AI_MILITARY_ALLIES_commonwealth_north_africa_support (THEATRE domain)
WA_AI_MILITARY_ALLIES_commonwealth_north_africa_support_THEATRE = {

	allowed = {
		OR = {
			tag = SAF
			tag = RAJ
		}
	}
	enable = {
		WA_AI_MILITARY_allies_africa_support_conditions = yes

		# Phase 5 mutual exclusion: yield to Country-layer overrides.
		NOT = { WA_AI_MILITARY_country_owns_fdab_target_north_africa = yes }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers
		order_id = 9101
		states = {
			447
			446
			454
			553
			451
			450
			448
			923
		}
		ratio = 0.25 # economy: was 0.5
		area = north_africa
		subtract_invasions_from_need = no
	}
}


# Phase 6 split from WA_AI_MILITARY_ALLIES_no_italy_invasion_early (THEATRE domain)
WA_AI_MILITARY_ALLIES_no_italy_invasion_early_THEATRE = {

	allowed = {
		WA_AI_CONFIG_MILITARY_is_western_invasion_pool = yes
	}
	enable = {
		WA_AI_MILITARY_is_allies_member = yes
		NOT = {
			tag = ITA
		}
		NOT = {
			AND = {
				tag = USA
				date > 1944.12.1
				has_war_with = JAP
			}
		}
		OR = {
			AND = {
				date > 1944.3.1
				date < 1944.6.6
			}
			date < 1943.7.1
			AND = {
				any_country = {
					WA_AI_MILITARY_is_allies_member = yes
					has_full_control_of_state = 115
				}
				any_country = {
					WA_AI_MILITARY_is_allies_member = yes
					has_full_control_of_state = 1032
				}
				any_country = {
					WA_AI_MILITARY_is_allies_member = yes
					has_full_control_of_state = 156
				}
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = italy
		value = -200 # economy: was -1000
	}
}


# Phase 6 split from WA_AI_MILITARY_ALLIES_war_against_ITA_north_africa (THEATRE domain)
WA_AI_MILITARY_ALLIES_war_against_ITA_north_africa_THEATRE = {

	allowed = {
		WA_AI_CONFIG_MILITARY_is_anglo_major = yes
	}
	enable = {
		OR = {
			has_war_with = ITA
			has_war_with = GER
		}
		OR = {
			date < 1943.7.1
			448 = { is_controlled_by = ITA }
			449 = { is_controlled_by = ITA }
			450 = { is_controlled_by = ITA }
			451 = { is_controlled_by = ITA }
			661 = { is_controlled_by = ITA }
			662 = { is_controlled_by = ITA }
			663 = { is_controlled_by = ITA }

			665 = { is_controlled_by = ITA }
			458 = { is_controlled_by = ITA }
			1061 = { is_controlled_by = ITA }
			460 = { is_controlled_by = ITA }

			448 = { is_controlled_by = ITL }
			449 = { is_controlled_by = ITL }
			450 = { is_controlled_by = ITL }
			451 = { is_controlled_by = ITL }
			661 = { is_controlled_by = ITL }
			662 = { is_controlled_by = ITL }
			663 = { is_controlled_by = ITL }

			665 = { is_controlled_by = ITL }
			458 = { is_controlled_by = ITL }
			1061 = { is_controlled_by = ITL }
			460 = { is_controlled_by = ITL }
		}

		# Phase 5 mutual exclusion: yield to Country-layer overrides.
		NOT = { WA_AI_MILITARY_country_owns_contain_ITA = yes }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = north_africa
		value = 100
	}
}

############################################################################################################
# WA Military V2 - Allies Faction Overlay
############################################################################################################
# Phase 2 split: THEATRE domain blocks.

WA_AI_MILITARY_ALLIES_theatre_boost_north_africa = {
	enable = {
		WA_AI_MILITARY_is_allies_member = yes
		has_war_with = ITA
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = theatre_distribution_demand_increase
		id = 447
		value = 10
	}
}



WA_AI_MILITARY_ALLIES_commonwealth_stage_usa_east = {
	allowed = {
		OR = {
			tag = AST
			tag = NZL
		}
	}
	enable = {
		WA_AI_MILITARY_allies_usa_east_staging_conditions = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers
		order_id = 9102
		states = {
			366
			816
			365
			364
			363
			362
			361
			829
			359
			861
			834
			830
			840
			842
			838
			839
		}
		ratio = 1.0 # siege: exile staging - the dominion army relocates to the US east coast (was 10)
		area = usa_east
		subtract_invasions_from_need = no
	}
}



WA_AI_MILITARY_ALLIES_commonwealth_protect_africa = {
	allowed = {
		WA_AI_CONFIG_MILITARY_is_commonwealth_dominion = yes
	}
	enable = {
		#NOT = {
		#	AND = {
		#		date < 1943.1.1
		#		OR = {
		#			casualties_inflicted_by = {
		#				opponent = GER
		#				thousands > 500
		#			}
		#			casualties_inflicted_by = {
		#				opponent = ITA
		#				thousands > 500
		#			}
		#		}
		#	}
		#}
		ENG = {
			is_ai = yes
		}
		WA_AI_MILITARY_is_allies_member = yes
		NOT = {
			OR = {
				surrender_progress > 0
				has_war_with = JAP
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 2

		# north_africa
		states = {
			447
			446
			923
		}

		ratio = 0.25 # economy: was 2

		area = north_africa

		subtract_invasions_from_need = no
		subtract_fronts_from_need = yes
	}
}


# Phase 6 split from WA_AI_MILITARY_ALLIES_husky_fire (THEATRE domain)
WA_AI_MILITARY_ALLIES_husky_fire_THEATRE = {

	allowed = {
		WA_AI_CONFIG_MILITARY_is_anglo_major = yes
	}
	enable = {
		NOT = {
			has_global_flag = soviet_union_defeated
		}
		GER = { has_war_with = SOV }
		has_war_with = ITA
		date > 1943.8.5
		date < 1943.12.1
		NOT = {
			AND = {
				date > 1944.3.1
				date < 1944.8.1
			}
		}
		OR = {
			AND = {
				NOT = {
					665 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					458 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					460 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					513 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					459 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					461 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					462 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					1055 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					1056 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
			}
			AND = {
				NOT = {	451 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	450 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	449 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	448 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	661 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	662 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	663 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	665 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	460 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	513 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	459 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	461 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	462 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	1055 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	1056 = { CONTROLLER = { has_war_with = ROOT } } }
			}
		}
		any_country = {
			WA_AI_MILITARY_is_axis_member = yes
			controls_state = 115
		}
		any_country = {
			WA_AI_MILITARY_is_axis_member = yes
			controls_state = 1032
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = italy
		value = 1000
	}
	ai_strategy = {
		type = area_priority
		id = south_italy
		value = 1000
	}
}


# Phase 6 split from WA_AI_MILITARY_ALLIES_husky_prep (THEATRE domain)
WA_AI_MILITARY_ALLIES_husky_prep_THEATRE = {

	allowed = {
		WA_AI_CONFIG_MILITARY_is_anglo_major = yes
	}
	enable = {
		NOT = {
			has_global_flag = soviet_union_defeated
		}
		GER = { has_war_with = SOV }
		has_war_with = ITA
		date > 1943.6.15
		date < 1943.8.1
		OR = {
			AND = {
				NOT = {
					665 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					458 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					460 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					513 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					459 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					461 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					462 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					1055 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
				NOT = {
					1056 = {
						CONTROLLER = {
							OR = {
								has_war_with = ROOT
								tag = VIC
								is_subject_of = VIC
							}
						}
					}
				}
			}
			AND = {
				NOT = {	451 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	450 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	449 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	448 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	661 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	662 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	663 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	665 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	460 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	513 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	459 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	461 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	462 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	1055 = { CONTROLLER = { has_war_with = ROOT } } }
				NOT = {	1056 = { CONTROLLER = { has_war_with = ROOT } } }
			}
		}
		any_country = {
			WA_AI_MILITARY_is_axis_member = yes
			controls_state = 115
		}
		any_country = {
			WA_AI_MILITARY_is_axis_member = yes
			controls_state = 1032
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = italy
		value = 1000
	}
	ai_strategy = {
		type = area_priority
		id = south_italy
		value = 1000
	}
}


# purpose: "hold Sicily" bridge - every husky support block above aborts the moment the Axis no
#          longer controls both Sicily states (their enable requires Axis control of 115 AND 1032),
#          which left the island unsupported between the landing and the scripted mainland
#          follow-up (campaign 2b607968: USA contingent died, ENG held Sicily alone vs the ITA
#          counterattack). This block takes over exactly when the husky blocks die: the faction side
#          controls 115 OR 1032 and an enemy still controls Calabria (156). The OR is deliberate -
#          the husky gate is an AND, so it dies at the FIRST captured Sicily state; requiring both
#          here left the landing itself in a support hole (measured 1943.7.15, campaign 2b607968:
#          1032 Allied / 115 still ITA, husky aborted, this bridge asleep, USA net -25 on region 238
#          from its own suppressions, and 32 Allied divisions parked in fresh Sardinia/Calabria/
#          Dodecanese invasion plans while 13 fought on the island). Gate is fully
#          controller/faction-based - no dates, no Axis/ITA tags - so it also covers ahistorical
#          Italian campaigns. south_italy (+800, single-region area) carries the fight; italy (+400,
#          2 regions) stages the jump north. Sized below the aborting husky pull (+1000 each) and
#          well below the strategic-bombing germany-area pulls (+4000) so it bridges without
#          out-shouting the Germany priorities; husky and this block never overlap, one starts where
#          the other stops. FRONT-domain twin: WA_AI_MILITARY_ALLIES_hold_sicily_bridge_FRONT (keep
#          the enable conditions of the two twins and of the two USA windows identical).
# range:   0 to +1000 (narrow areas, per WA_AI_MILITARY_ECONOMY.md E1)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_ALLIES_hold_sicily_bridge_THEATRE = {

	allowed = {
		WA_AI_CONFIG_MILITARY_is_western_allies_major = yes
	}
	enable = {
		has_war = yes
		OR = {
			115 = {
				OR = {
					is_controlled_by = ROOT
					CONTROLLER = { is_in_faction_with = ROOT }
				}
			}
			1032 = {
				OR = {
					is_controlled_by = ROOT
					CONTROLLER = { is_in_faction_with = ROOT }
				}
			}
		}
		156 = { CONTROLLER = { has_war_with = ROOT } }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = south_italy
		value = 800
	}
	ai_strategy = {
		type = area_priority
		id = italy
		value = 400
	}
}


# purpose: the Allies do not commit ground forces to South America (relocated Phase 7c from legacy
#          ENG.txt ENG_ignore_south_america - it had no tag gate, so it was always a Faction rule)
# range:   -5000 (saturated legacy value; Phase 8 renormalises toward net 0 against the +100 baseline)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_ALLIES_ignore_south_america_THEATRE = {
	enable = {
		is_in_faction_with = ENG
		date > 1938.1.1
	}
	abort = {
		OR = {
			has_war_with = VEN
			has_war_with = BRA
		}
	}

	ai_strategy = {
		type = area_priority
		id = south_america
		value = -200 # economy: was -5000
	}
}
