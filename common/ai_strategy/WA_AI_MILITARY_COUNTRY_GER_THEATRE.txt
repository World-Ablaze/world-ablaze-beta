############################################################################################################
# WA AI Military V2 - COUNTRY_GER_THEATRE
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { area_priority, put_unit_buffers, spare_unit_factor, theatre_distribution_demand_increase }.
# Canonical domain: THEATRE. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.


# Phase 6 split from WA_AI_MILITARY_GER_gank_everyone (THEATRE domain)
WA_AI_MILITARY_GER_gank_everyone_THEATRE = {
	 #####DONT TOUCH
	allowed = {
		tag = GER
	}
	enable = {
		has_war_with = HOL
		has_war_with = BEL
		OR = {
			HOL = { has_capitulated = no }
			BEL = { has_capitulated = no }
		}
	}
	abort = {
		HOL = { has_capitulated = yes }
		BEL = { has_capitulated = yes }
	}

	#ai_strategy = {
	#	type = front_control
	#	tag = FRA
	#	ordertype = front
	#	execution_type = careful ####rush before dont touch
	#	#execute_order = yes
	#	manual_attack = yes
	#}

	ai_strategy = {
		type = area_priority
		id = scandinavia
		value = -200
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_gank_france (THEATRE domain)
WA_AI_MILITARY_GER_gank_france_THEATRE = {
	 ######DONT TOUCH
	allowed = {
		tag = GER
	}
	enable = {
		has_war_with = HOL
		has_war_with = BEL
		has_war_with = FRA
		HOL = { has_capitulated = yes }
		BEL = { has_capitulated = yes }
	}
	abort = {
		OR = {
			FRA = { has_capitulated = yes }
			date > 1942.1.1
			has_war_with = SOV
		}
	}

	ai_strategy = {
		type = area_priority
		id = north_france
		value = 1000
	}
	ai_strategy = {
		type = area_priority
		id = france
		value = 1000
	}
	ai_strategy = {
		type = area_priority
		id = south_france
		value = 1000
	}
	ai_strategy = {
		type = area_priority
		id = scandinavia
		value = -200
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_gank_france_2 (THEATRE domain)
WA_AI_MILITARY_GER_gank_france_2_THEATRE = {
	 #####DONT TOUCH
	allowed = {
		tag = GER
	}
	enable = {
		has_war_with = HOL
		has_war_with = BEL
		has_war_with = FRA
		OR = {
			HOL = { has_capitulated = no }
			BEL = { has_capitulated = no }
			FRA = { has_capitulated = no }
		}
		date > 1940.9.1
	}
	abort = {
		OR = {
			FRA = { has_capitulated = yes }
			date > 1940.10.1
		}
	}

	ai_strategy = {
		type = area_priority
		id = north_france
		value = 1000 # economy: was 5000
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_gank_france_3 (THEATRE domain)
WA_AI_MILITARY_GER_gank_france_3_THEATRE = {

	allowed = {
		tag = GER
	}
	enable = {
		has_war_with = HOL
		has_war_with = BEL
		has_war_with = FRA
		date > 1940.11.1
		OR = {
			HOL = { has_capitulated = no }
			BEL = { has_capitulated = no }
			FRA = { has_capitulated = no }
		}
	}
	abort = {
		OR = {
			FRA = { has_capitulated = yes }
			date > 1940.12.1
		}
	}

	ai_strategy = {
		type = area_priority
		id = north_france
		value = 1000 # economy: was 5000
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_focus_on_north_africa (THEATRE domain)
WA_AI_MILITARY_GER_focus_on_north_africa_THEATRE = {

	allowed = {
		tag = GER
	}
	enable = {
		OR = {
			NOT = { has_war_with = FRA }
			FRA = { has_capitulated = yes }
		}
		OR = {
			118 = { is_controlled_by = ENG }
			446 = { is_controlled_by = ENG }
			446 = { is_controlled_by = UKM }
			446 = { is_controlled_by = UKE }
		}
		surrender_progress < 0.05
		WA_AI_MILITARY_allied_with_italian_homeland_power = yes # Fix 96: was is_in_faction_with = ITA
		has_war_with = ENG
		NOT = {
			has_war_with = SOV
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = north_africa
		value = 130
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_atlantik_wall_north_france (THEATRE domain)
WA_AI_MILITARY_GER_atlantik_wall_north_france_THEATRE = {

	allowed = {
		tag = GER
	}
	enable = {
		# NOTE: no RCZ_garrison_flag gate here - RCZ complements GER's own coastal defense, it does not replace it
		date > 1941.1.1
	}

	abort_when_not_enabled = yes

	# Campaign 5709c8b9: with the wall blocks sharing order_id 1 (one ratio pool
	# diluted from Norway to the Balkans) and fronts in-area subtracting from
	# buffer need, GER eroded to 3 divisions in France by D-Day. Dedicated
	# order_ids + subtract_fronts_from_need = no mirror the festung blocks;
	# ratios are per-pool now, so they are sized per sector (0.12 of ~300
	# divisions = ~36 for the Channel coast).
	ai_strategy = {
		type = put_unit_buffers

		order_id = 11

		# france
		states = {
			29
			23
			30
			14
			15
		}

		ratio = 0.12

		area = france

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_army_group_finland_does_not_exist (THEATRE domain)
WA_AI_MILITARY_GER_army_group_finland_does_not_exist_THEATRE = {

	allowed = {
		NOT = { tag = FIN }
	}
	enable = {
		is_in_faction_with = GER
		FIN = {
			is_in_faction_with = GER
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = finland
		value = -200 # economy: was -1000
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_war_with_norway (THEATRE domain)
WA_AI_MILITARY_GER_war_with_norway_THEATRE = {

	allowed = {
		tag = GER
	}
	enable = {
		country_exists = NOR
		has_completed_focus = GER_western_war_plans
		NOT = {
			is_in_faction_with = NOR
		}
		NOT = {
			NOR = { is_subject_of = GER }
		}
		NOR = { has_capitulated = no }
		OR = {
			110 = { CONTROLLER = { has_war_with = ROOT } }
			1017 = { CONTROLLER = { has_war_with = ROOT } }
			1020 = { CONTROLLER = { has_war_with = ROOT } }
			#99 = { CONTROLLER = { is_in_faction_with = ROOT } }
			#99 = { CONTROLLER = { is_subject_of = ROOT } }
			#99 = { CONTROLLER = { tag = ROOT } }
			#37 = { CONTROLLER = { is_in_faction_with = ROOT } }
			#37 = { CONTROLLER = { is_subject_of = ROOT } }
			#37 = { CONTROLLER = { tag = ROOT } }
		}
		NOT = { surrender_progress > 0.1 }
		OR = {
			AND = {
				date < 1940.5.10
				NOT = { has_war_with = HOL }
				NOT = { has_war_with = BEL }
			}
			AND = {
				date < 1941.5.1
				110 = { CONTROLLER = { has_war_with = ROOT } }
			}
		}
	}
	abort_when_not_enabled = yes

	#	ai_strategy = {
	#		type = front_unit_request
	#		area = scandinavia
	#		value = 10
	#	}

	ai_strategy = {
		type = area_priority
		id = scandinavia
		value = 50
	}
	ai_strategy = {
		type = area_priority
		id = finland
		value = 50
	}
}



WA_AI_MILITARY_GER_festung_berlin = {
	allowed = {
		tag = GER
	}
	enable = {
		surrender_progress > 0
		has_war_with = SOV
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 2

		# north_russia
		states = {
			810
		}

		ratio = 0.2

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}



WA_AI_MILITARY_GER_festung_danzig = {
	allowed = {
		tag = GER
	}
	enable = {
		surrender_progress > 0
		has_war_with = SOV
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 3

		states = {
			85
			797
		}

		ratio = 0.2

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}



WA_AI_MILITARY_GER_festung_breslau = {
	allowed = {
		tag = GER
	}
	enable = {
		surrender_progress > 0
		has_war_with = SOV
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 4

		states = {
			66
		}

		ratio = 0.1

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}



WA_AI_MILITARY_GER_festung_poznan = {
	allowed = {
		tag = GER
	}
	enable = {
		surrender_progress > 0
		has_war_with = SOV
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 5

		states = {
			86
		}

		ratio = 0.25

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}



WA_AI_MILITARY_GER_festung_stettin = {
	allowed = {
		tag = GER
	}
	enable = {
		surrender_progress > 0
		has_war_with = SOV
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 6

		states = {
			63
		}

		ratio = 0.2

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_germany_protect_the_fatherland (THEATRE domain)
WA_AI_MILITARY_GER_germany_protect_the_fatherland_THEATRE = {

	allowed = {
		original_tag = GER
	}
	enable = {
		has_government = fascism
		date > 1942.1.1
		OR = {
			surrender_progress > 0.05
			AND = {
				has_war_with = FRA
				OR = {
					29 = { is_controlled_by = FRA }
					29 = { is_controlled_by = ENG }
					29 = { is_controlled_by = USA }
				}
			}
			AND = {
				has_war_with = FRA
				OR = {
					18 = { is_controlled_by = FRA }
					18 = { is_controlled_by = ENG }
					18 = { is_controlled_by = USA }
				}
			}
			AND = {
				has_war_with = FRA
				OR = {
					17 = { is_controlled_by = FRA }
					17 = { is_controlled_by = ENG }
					17 = { is_controlled_by = USA }
				}
			}
			AND = {
				has_war_with = BEL
				OR = {
					6 = { is_controlled_by = BEL }
					6 = { is_controlled_by = FRA }
					6 = { is_controlled_by = ENG }
					6 = { is_controlled_by = USA }
				}
			}
			AND = {
				has_war_with = HOL
				OR = {
					7 = { is_controlled_by = HOL }
					7 = { is_controlled_by = FRA }
					7 = { is_controlled_by = ENG }
					7 = { is_controlled_by = USA }
				}
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = area_priority
		id = north_france
		value = -200 # economy: was -2000
	}
	ai_strategy = {
		type = area_priority
		id = france
		value = -200 # economy: was -2000
	}
	ai_strategy = {
		type = area_priority
		id = south_france
		value = -200 # economy: was -2000
	}
	ai_strategy = {
		type = area_priority
		id = south_balkans
		value = -200 # economy: was -2000
	}
	ai_strategy = {
		type = area_priority
		id = central_balkans
		value = -200 # economy: was -2000
	}
	ai_strategy = {
		type = area_priority
		id = north_russia
		value = -200 # economy: was -2000
	}
	ai_strategy = {
		type = area_priority
		id = west_russia
		value = -200 # economy: was -2000
	}
	ai_strategy = {
		type = area_priority
		id = south_russia
		value = -200 # economy: was -2000
	}
	ai_strategy = {
		type = area_priority
		id = caucasus
		value = -200 # economy: was -2000
	}
}



WA_AI_MILITARY_GER_margarethe = {
	allowed = {
		tag = GER
	}
	enable = {
		is_in_faction_with = HUN
		has_country_flag = hungary_is_flipping_sides
		HUN = { is_ai = no }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 1

		# north_balkans
		states = {
			889
		}

		ratio = 0.25 # economy: was 0.3

		area = north_balkans

		subtract_invasions_from_need = no
	}
}



# Fix 96 (2026-08-17): WA_AI_MILITARY_GER_fall_achse_a / _b (put_unit_buffers 0.2 on the Italian mainland
# and 0.2 on Albania, armed by GER_fall_achse_prepared) DELETED. The flag is stamped by the AI mission
# 60 days after ita_armor.893 - on campaign 0edbc955 that was 1943.11.28, after Rome had fallen and
# Italy was down to Sardinia - and the blocks aborted the instant `has_war_with = ITA` became true, i.e.
# exactly when Germany needed to be in Italy. Replaced by the geography-gated Faction pair
# WA_AI_MILITARY_AXIS_italy_theatre_guard_THEATRE / _invaded_THEATRE (WA_AI_MILITARY_FACTION_AXIS_THEATRE.txt)
# and WA_AI_MILITARY_AXIS_hold_italy_after_defection_FRONT. The GER_fall_achse_prepared flag itself
# stays: decisions/GER.txt (RIT release), ita_armor.897 (state transfer) still read it.



WA_AI_MILITARY_GER_atlantik_wall_south_france = {
	allowed = {
		tag = GER
	}
	enable = {
		# NOTE: no RCZ_garrison_flag gate here - RCZ complements GER's own coastal defense, it does not replace it
		tag = GER
		OR = {
			GER = { has_full_control_of_state = 21 }
			ITA = { has_full_control_of_state = 21 }
		}
		date > 1941.1.1
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 12

		# south_france
		states = {
			19
			21
			735
		}

		ratio = 0.05

		area = south_france

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}



WA_AI_MILITARY_GER_atlantik_wall_benelux = {
	allowed = {
		tag = GER
	}
	enable = {
		# NOTE: no RCZ_garrison_flag gate here - RCZ complements GER's own coastal defense, it does not replace it
		tag = GER
		date > 1941.1.1
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 13

		# benelux
		states = {
			6
			7
			35
			36
		}

		ratio = 0.06

		area = benelux

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}



WA_AI_MILITARY_GER_atlantik_wall_germany = {
	allowed = {
		tag = GER
	}
	enable = {
		# NOTE: no RCZ_garrison_flag gate here - RCZ complements GER's own coastal defense, it does not replace it
		tag = GER
		date > 1941.1.1
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 14

		# germany
		states = {
			56
			59
		}

		ratio = 0.05

		area = germany

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}



WA_AI_MILITARY_GER_atlantik_wall_scandinavia = {
	allowed = {
		tag = GER
	}
	enable = {
		# NOTE: no RCZ_garrison_flag gate here - RCZ complements GER's own coastal defense, it does not replace it
		tag = GER
		date > 1941.1.1
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = put_unit_buffers

		order_id = 15

		# scandinavia
		states = {
			1017
			1020
			142
			143
			144
		}

		ratio = 0.06

		area = scandinavia

		subtract_invasions_from_need = no
		subtract_fronts_from_need = no # pool: dedicated reserve that must not yield to front demand (economy E4; lessons-log put_unit_buffers entry)
	}
}

############################################################################################################
# Phase 7c relocations from legacy GER.txt (gates copied verbatim; area_priority values are DELTAS
# against the flat +100 baseline in WA_AI_MILITARY_DEFAULT_THEATRE.txt).
############################################################################################################

# purpose: extra weight on Benelux once Poland falls (relocated from legacy GER.txt
#          GER_make_sure_holland_is_ganked_asap; benelux is a 2-region narrow area)
# range:   +200 (delta)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_GER_holland_rush_THEATRE = {
	allowed = {
		tag = GER
	}
	enable = {
		has_war_with = POL
		POL = { has_capitulated = yes }
	}
	abort = {
		OR = {
			POL = { has_capitulated = no }
			HOL = { has_capitulated = yes }
		}
	}

	ai_strategy = {
		type = area_priority
		id = benelux
		value = 200
	}
}

# purpose: while France stands and Norway is done, Scandinavia goes quiet and Benelux gets the weight
#          (relocated from legacy GER.txt ignore_scandanavia_until_france_is_dealt_with)
# range:   -200 / +100 (deltas)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_GER_west_first_THEATRE = {
	allowed = {
		tag = GER
	}
	enable = {
		has_war_with = NOR
		has_war_with = FRA
		NOR = { has_capitulated = yes }
		OR = {
			NOT = { has_war_with = SWE }
			SWE = { has_capitulated = yes }
		}
	}
	abort = {
		OR = {
			FRA = { has_capitulated = yes }
			NOR = { has_capitulated = no }
		}
	}

	ai_strategy = {
		type = area_priority
		id = scandinavia
		value = -200
	}

	ai_strategy = {
		type = area_priority
		id = benelux
		value = 100
	}
}

# purpose: Barbarossa opening-year focus on the northern/central axis (relocated from legacy GER.txt
#          war_with_soviets; its force_concentration entries were deleted per economy E5 - AIFC owns
#          concentration and these fought its non-sector catch-all)
# range:   +50 to +250 (deltas; west_russia +250 exceeds E1 and is clamped in the Phase 8 value pass)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_GER_barbarossa_opening_THEATRE = {
	allowed = {
		tag = GER
	}
	enable = {
		country_exists = SOV
		NOT = { is_in_faction_with = SOV }
		OR = {
			AND = {
				FRA = { has_capitulated = yes }
				has_war_with = FRA
			}
			date > 1941.6.1
		}
		date < 1942.1.1
	}
	abort = {
		OR = {
			NOT = { country_exists = SOV }
			AND = {
				FRA = { has_capitulated = no }
				has_war_with = FRA
			}
			date < 1941.1.1
			is_in_faction_with = SOV
			date > 1942.1.1
		}
	}

	ai_strategy = {
		type = area_priority
		id = north_russia
		value = 200
	}

	ai_strategy = {
		type = area_priority
		id = west_russia
		value = 200 # economy: was 250
	}

	ai_strategy = {
		type = area_priority
		id = south_russia
		value = 50
	}

	ai_strategy = {
		type = area_priority
		id = caucasus
		value = 50
	}
}

# purpose: 1942 southern-campaign tilt (relocated from legacy GER.txt war_with_soviets_4; its
#          Stalingrad/Rostov force_concentration_target_weight was deleted per economy E5)
# range:   +20 (deltas)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_GER_fall_blau_THEATRE = {
	allowed = {
		tag = GER
	}
	enable = {
		has_war_with = SOV
		date > 1942.3.1
	}
	abort = {
		OR = {
			NOT = { country_exists = SOV }
			date < 1942.10.1
		}
	}

	ai_strategy = {
		type = area_priority
		id = south_russia
		value = 20
	}

	ai_strategy = {
		type = area_priority
		id = caucasus
		value = 20
	}
}

# purpose: Germany's standing strategic tilt, converted from the legacy absolute frame
#          (GER_area_priority, which had no baseline underneath it) to deltas against the flat +100
#          baseline. Same nets for listed areas; the audit's key fix is what is ABSENT here: the legacy
#          frame left north_russia/south_russia/caucasus/crimea at 0 outside the scripted 1941-42
#          windows, so late-war Germany valued France over most of its own eastern front. Under the
#          flat baseline those areas now sit at parity by default.
# range:   -110 to +20 (deltas)
# policy:  Additive per area
# domain:  THEATRE
# always-on: static continental posture of the original tag - the tilt encodes geography (Germany is a
#            European land power), not a situation; situational shifts layer on top of it.
WA_AI_MILITARY_GER_area_tilt_THEATRE = {
	allowed = {
		original_tag = GER
	}
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = area_priority id = west_russia value = 20 }
	ai_strategy = { type = area_priority id = north_balkans value = 10 }
	ai_strategy = { type = area_priority id = central_balkans value = 10 }
	ai_strategy = { type = area_priority id = south_balkans value = 10 }
	ai_strategy = { type = area_priority id = east_europe value = 10 }
	ai_strategy = { type = area_priority id = scandinavia value = -70 }
	ai_strategy = { type = area_priority id = germany value = 10 }
	ai_strategy = { type = area_priority id = france value = 10 }
	ai_strategy = { type = area_priority id = benelux value = 10 }
	ai_strategy = { type = area_priority id = south_america value = -110 }
	ai_strategy = { type = area_priority id = asia value = -90 }
	ai_strategy = { type = area_priority id = middle_east value = -80 }
	ai_strategy = { type = area_priority id = north_africa value = -80 }
	# Fix 96: the sub-Saharan sink was open - 0edbc955 saw GER armies given "Northern Kenya" /
	# "Mombasa" / "Khartoum" fronts (12 divisions in Dec 1943) because only north_africa carried a tilt
	ai_strategy = { type = area_priority id = central_africa value = -80 }
	ai_strategy = { type = area_priority id = south_africa value = -80 }
	ai_strategy = { type = area_priority id = north_pacific value = -100 }
	ai_strategy = { type = area_priority id = central_pacific value = -100 }
	ai_strategy = { type = area_priority id = south_pacific value = -100 }
	ai_strategy = { type = area_priority id = oceania value = -110 }
	ai_strategy = { type = area_priority id = australia_new_zealand value = -110 }
}

# purpose: while the war in the west is undecided, the eastern border areas go quiet (relocated
#          Phase 7c from legacy GER.txt ignore_soviet_union; its diplomacy entries stayed there)
# range:   -100 per area (delta - suppresses to net 0 against the flat baseline)
# policy:  Additive per area
# domain:  THEATRE
WA_AI_MILITARY_GER_eastern_quiet_before_fall_of_france_THEATRE = {
	allowed = {
		tag = GER
	}
	enable = {
		has_war_with = FRA
		has_war_with = POL
	}
	abort = {
		POL = { has_capitulated = yes }
		OR = {
			FRA = { has_capitulated = yes }
			has_war_with = SOV
		}
	}

	ai_strategy = {
		type = area_priority
		id = east_europe
		value = -100
	}

	ai_strategy = {
		type = area_priority
		id = north_russia
		value = -100
	}

	ai_strategy = {
		type = area_priority
		id = west_russia
		value = -100
	}

	ai_strategy = {
		type = area_priority
		id = south_russia
		value = -100
	}
}
