############################################################################################################
# WA AI Military V2 - COUNTRY_GER_FRONT
############################################################################################################
# Phase 6 split: every ai_strategy in this file has type ∈ { force_concentration_front_factor, force_concentration_target_weight, front_armor_score, front_control, front_unit_request, garrison }.
# Canonical domain: FRONT. See documentation/WA_AI_MILITARY_TYPES_REFERENCE.md.


# Phase 6 split from WA_AI_MILITARY_GER_africa_is_lost (FRONT domain)
# purpose: Africa is lost - stop feeding the north_africa front. [med-axis-posture] (2026-08-17): the gate was
#          `date > 1942.10.1` + ENG/subject/faction controls Suez 923 - a vanilla-inherited post-El-Alamein
#          CALENDAR (git log -S: present since the initial commit) that armed on 1942.10.1 in every
#          historical game, five weeks before Torch, and would arm on that date in an ahistorical game where
#          Suez was never contested. Now: at war and our side (self / subject / ally / war partner) holds NO
#          port on the African shore - the four Libyan ports plus Tunis/Bizerte
#          (WA_AI_MILITARY_african_shore_port_held). Historical walk: 1939.9 arms (no Axis port; inert -
#          Italy neutral, no German presence in Africa is possible), 1940.6 releases (ITL's Tripoli is at war
#          together with us), held through Torch (Nov 42: net front request on the blob -75, not -175, so
#          the [med-axis-posture] Tunis bridge is not fighting this veto), arms again when Tunis AND the Libyan ports
#          are gone (May 43 historically) - and releases if a port is ever retaken.
# range:   -100 (E2)
# policy:  Additive per area
# domain:  FRONT
WA_AI_MILITARY_GER_africa_is_lost_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_africa_is_lost_front = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = north_africa
		value = -100 # economy: was -1000
	}
}

############################################################################################################
# WA Military V2 - GER Country Overlay
############################################################################################################
# Phase 2 split: FRONT domain blocks.
# Phase 2 split: FRONT domain blocks.

WA_AI_MILITARY_GER_holland_is_a_weakpoint = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_holland_is_a_weakpoint = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = HOL
		value = 60
	}
}



WA_AI_MILITARY_GER_belgium_is_a_weakpoint = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_belgium_is_a_weakpoint = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = BEL
		value = 60
	}
}



WA_AI_MILITARY_GER_luxembourg_is_a_weakpoint = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_luxembourg_is_a_weakpoint = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = LUX
		value = 60
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_gank_everyone (FRONT domain)
WA_AI_MILITARY_GER_gank_everyone_FRONT = {
	 #####DONT TOUCH
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_gank_everyone_front = yes
	}
	abort = {
		HOL = { has_capitulated = yes }
		BEL = { has_capitulated = yes }
	}

	#ai_strategy = {
	#	type = front_control
	#	tag = FRA
	#	ordertype = front
	#	execution_type = careful ####rush before dont touch
	#	#execute_order = yes
	#	manual_attack = yes
	#}
	ai_strategy = {
		type = front_unit_request
		area = east_europe
		value = -50
	}
	ai_strategy = {
		type = front_unit_request
		area = north_russia
		value = -50
	}
	ai_strategy = {
		type = front_unit_request
		area = west_russia
		value = -50
	}
	ai_strategy = {
		type = front_unit_request
		area = south_russia
		value = -50
	}
	ai_strategy = { #########FM define lowered
		type = front_unit_request
		strategic_region = 19
		value = -75
	}
	ai_strategy = {
		type = front_control
		tag = HOL
		ordertype = front
		execution_type = rush
		execute_order = yes
		#manual_attack = yes
	}
	ai_strategy = {
		type = front_control
		tag = BEL
		ordertype = front
		execution_type = rush
		execute_order = yes
		#manual_attack = yes
	}
	ai_strategy = {
		type = front_control
		strategic_region = 5
		ordertype = front
		execution_type = rush
		execute_order = yes
		#manual_attack = yes
	}
	ai_strategy = { ####new, if ai breaks remove
		type = front_control
		strategic_region = 239
		ordertype = front
		execution_type = rush_weak
		execute_order = yes
		#manual_attack = yes
	}
	ai_strategy = {
		type = front_control
		state = 28
		ordertype = front
		execution_type = rush_weak
		#execute_order = yes
		manual_attack = yes
	}
}



WA_AI_MILITARY_GER_fall_rot = {
	# aifc-tuning: scripted Fall Rot schwerpunkt - steers the AIFC objective set during the 1940 France collapse window.
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_fall_rot = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = FRA
		value = 200 # economy: was 1000
	}
	ai_strategy = {
		type = force_concentration_target_weight
		state = 29 # Nord-Pas_de_Calais
		state = 785 # Picardy
		state = 855 # Paris
		state = 16 # Ile de France
		#state = 18 # Champagne
		state = 1016
		state = 15 # Normandy
		# include Belgium as well even though we hopefully have already taken it
		state = 979 # Brabant
		state = 6 # Vlaanderen
		state = 34 # Wallonie
		state = 790 # Eupen Malmady
		state = 985 # Ardennes

		value = 80 # +80%
	}

	ai_strategy = {
		# Avoid the areas behind the Maginot line
		type = force_concentration_target_weight
		state = 17 # Franche-Comte
		state = 28 # Alsace-Lorraine
		state = 27 # Bourgogne

		value = -60 # -60%
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_gank_france (FRONT domain)
WA_AI_MILITARY_GER_gank_france_FRONT = {
	 ######DONT TOUCH
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_gank_france_front = yes
	}
	abort = {
		OR = {
			FRA = { has_capitulated = yes }
			date > 1942.1.1
			has_war_with = SOV
		}
	}

	ai_strategy = {
		type = front_unit_request
		area = east_europe
		value = -50
	}
	ai_strategy = {
		type = front_unit_request
		area = north_russia
		value = -50
	}
	ai_strategy = {
		type = front_unit_request
		area = west_russia
		value = -50
	}
	ai_strategy = {
		type = front_unit_request
		area = south_russia
		value = -50
	}
	ai_strategy = {
		type = front_control
		strategic_region = 239
		ordertype = front
		execution_type = rush_weak
		execute_order = yes
		#manual_attack = yes
	}
	ai_strategy = {
		type = front_control
		state = 28
		ordertype = front
		execution_type = rush_weak
		#execute_order = yes
		manual_attack = yes
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_gank_france_2 (FRONT domain)
WA_AI_MILITARY_GER_gank_france_2_FRONT = {
	 #####DONT TOUCH
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_gank_france_2_front = yes
	}
	abort = {
		OR = {
			FRA = { has_capitulated = yes }
			date > 1940.10.1
		}
	}

	ai_strategy = {
		type = front_control
		tag = FRA
		ordertype = front
		execution_type = balanced
		execute_order = yes
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_gank_france_3 (FRONT domain)
WA_AI_MILITARY_GER_gank_france_3_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_gank_france_3_front = yes
	}
	abort = {
		OR = {
			FRA = { has_capitulated = yes }
			date > 1940.12.1
		}
	}

	ai_strategy = {
		type = front_control
		tag = FRA
		ordertype = front
		execution_type = balanced
		execute_order = yes
	}
}



WA_AI_MILITARY_GER_war_with_denmark = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_war_with_denmark = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		strategic_region = 6
		ordertype = front
		execution_type = rush
		execute_order = yes
	}

	ai_strategy = {
		type = front_control
		strategic_region = 207
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_naval_invade_norway (FRONT domain)
WA_AI_MILITARY_GER_naval_invade_norway_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_naval_invade_norway_front = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control

		area = norwegian_coast

		ordertype = invasion
		execute_order = yes
	}
}


# purpose: [med-axis-posture] - the Afrika Korps (user decision 2026-08-20): while no enemy stands in Western
#          Europe and Germany is not fighting the Soviets, the Egypt-side enemy gets a German
#          armoured expedition instead of the legacy economy veto. Replaces the old payload
#          (front_unit_request -75): the ITL entry was INERT -
#          `id` is the ENEMY tag of the front (vanilla usage: GER id=POL 250 for Poland,
#          id=SOV 500 for Barbarossa) and Germany is never at war with its ally ITL. The armor
#          ids below are the mod's own East-Egypt anchor tags (payload, not gating - rule 4);
#          an ahistorical Egypt controller still gets the request and the exec block below, only
#          the armour bias degrades. HYPOTHESIS carried by probe R80, not by this comment: a
#          positive id-keyed score on the Egypt holder moves German armour to Africa.
#          Window close (Barbarossa / D-Day) removes the PULL, it does not repatriate the force:
#          t0 (same day) this block aborts, war_with_soviets_2 re-arms -75 on the blob; t1 (weekly
#          allocator + AIFC reconcile) surplus divisions re-target and armour re-steers to the new
#          sector enemy; t2 (months, convoy-bound) the force drains at sea-lift speed - a cut
#          strait strands it (the historical DAK shape; accepted, R80 fail-tell). Sea-lift INTO
#          Africa is MEASURED viable: GER lifted 0 -> 8 divisions into Tunisia in 31 days (R64,
#          campaign 9d83084c).
# range:   +60 (E2 [-100, +200]); armor 150 (vanilla band 100-500)
# policy:  Additive per area / per armor id
# domain:  FRONT
WA_AI_MILITARY_GER_focus_on_north_africa_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		# [med-axis-posture] (2026-08-21, campaign 8c0fea4c): the `NOT = { has_war_with = SOV }` term that stood
		# here is GONE. User ruling 2026-08-21: German armour STAYS in Africa - the expedition must
		# not end at Barbarossa. The D-Day half of the 2026-08-20 rule is unchanged and still
		# dynamic: it lives inside the window trigger as
		# `NOT = { WA_AI_MILITARY_AIR_theatre_contested_western_europe }`, so the window still closes
		# by itself when the West opens, with no date and no tag.
		# MEASURED 8c0fea4c 1942.11.21: every one of the window's five terms was TRUE (Axis with the
		# Italian homeland power; ITL holds 448/450/451; ENG holds the East-Egypt anchor 446/447/453;
		# no enemy on Reich soil; no Allied ground in Western Europe) and this single clause was the
		# only thing holding the expedition shut - with 0 German divisions in Africa on all five
		# saves 1941.4 -> 1943.3, while Germany OWNED and CONTROLLED Tunis 458, Bizerte 1061 and
		# Gabes 665 defended by 1 Italian division.
		# Paired with WA_AI_MILITARY_GER_war_with_soviets_africa_suppression_FRONT below: removing
		# this gate alone would have left the -75 Africa suppression standing against it.
		WA_AI_MILITARY_afrika_korps_window = yes
	}
	abort_when_not_enabled = yes

	# [med-axis-posture] (2026-08-21, campaign 8c0fea4c): 60 -> 30. [med-axis-posture]'s expedition worked and then
	# saturated the theatre's logistics. MEASURED at 1940.12: 57 Axis divisions stood in Libya /
	# Egypt (GER 16, ITA 35, ITL 6) on a corridor with 4 BREAK hops out of 9 and nothing above
	# level 1 east of Sirte. The user's ruling is a 50 % cut, not a cancellation - the expedition
	# is still wanted, at half the pull. Net over the standing -80 area tilt: -45 instead of -15.
	# ASSUMED, and it is the whole risk of this change: that halving a `front_unit_request` weight
	# halves the divisions that arrive. The engine's weight-to-division mapping is not observable
	# in a save. The one anchor that IS measured: at 60, the peak was 16 German divisions.
	# The division-count brake (a `divisions_in_state` cap per corridor state) was designed and
	# DEFERRED by owner decision 2026-08-21 - do the arithmetic cut first, measure, then decide.
	#
	# [med-axis-posture] (2026-08-21, campaign 8c0fea4c, checklist R82): 30 -> 40. Owner ruling after the first
	# in-game run of [med-axis-posture]. MEASURED on that run (saves 1940.7.24 / 1940.10.12 / 1940.11.20 /
	# 1941.4.19 / 1941.6.15 / 1941.10.23): German divisions in the African scope went 0 -> 2 -> 7 ->
	# 0 -> 0 -> 0, and at 1941.10.23 Germany held NO African front order at all while all five terms
	# of the window were true. So the cut did halve the peak (16 -> 7, R82 leg 1 met) and the
	# expedition still evaporated - R82 leg 2 fails.
	# STATED PLAINLY, because the number alone will probably not close it: the collapse to ~0 in
	# early 1941 happened at 60 too (16 at 1940.12 -> 1 at 1941.2 on the previous campaign), i.e.
	# BEFORE and INDEPENDENT of the cut. The binding constraint is the engine's arbitration between
	# 18 contiguous eastern fronts and one overseas theatre, which no `front_unit_request` magnitude
	# is known to move. This +10 buys margin; `theatre_distribution_demand_increase` gated on the
	# window is the recorded next lever if R82 leg 2 fails again.
	# `area_priority` (GER_THEATRE, 105 over the standing -80 = net +25) is deliberately NOT raised
	# with it: two magnitudes moved in one commit makes the next campaign unreadable.
	ai_strategy = {
		type = front_unit_request
		area = north_africa
		value = 40
	}
	# [aifc] the +150 armor ids (ENG/UKE/UKM) were deleted: they fought the dynamic
	# +400/-150 from WA_AI_AIFC_armor_reconcile. Armor-follows-schwerpunkt owns this now.
}


# purpose: [med-axis-posture] - execute battle plans toward Egypt while the force balance supports it; mirror
#          of the ITA R13 exec block (same viability verdict, same priority rung: above an unset
#          Country default of 0, below the 500/10000 equipment emergency stops).
# range:   n/a (front_control)
# policy:  Exclusive per area+ordertype (sole GER-scope ordertype=front writer for north_africa;
#          the ordertype=invasion execute_order=no landing freeze elsewhere in this file is a
#          different ordertype and stays)
# domain:  FRONT
WA_AI_MILITARY_GER_north_africa_offensive_exec_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		# [med-axis-posture] SOV term removed, same ruling as the _FRONT twin above. The offensive executor
		# must not outlive-then-die with the request block; the two are a pair and share a gate.
		WA_AI_MILITARY_afrika_korps_window = yes
		WA_AI_MILITARY_north_africa_offensive_viable = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		area = north_africa
		priority = 100
		ordertype = front
		execution_type = balanced
		execute_order = yes
		manual_attack = yes
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_war_with_soviets_2 (FRONT domain)
WA_AI_MILITARY_GER_war_with_soviets_2_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_war_with_soviets_2_front = yes
	}
	abort_when_not_enabled = yes

	# [med-axis-posture] the front_armor_score id "ITL" value 0 entry that stood here was deleted with its
	# counterpart in focus_on_north_africa (both inert - id is the enemy tag, ITL is an ally).
	# [med-axis-posture] the `area = north_africa value = -75` line MOVED OUT of this block, to
	# WA_AI_MILITARY_GER_war_with_soviets_africa_suppression_FRONT below. It is the same value under
	# the same Soviet-war gate, plus one exclusion: it stands down while the Afrika Korps window is
	# open. `front_unit_request` is ADDITIVE per area, so leaving it here would have cancelled the
	# expedition the window now keeps alive (+30 against -75 against the standing -80 tilt).
	# The two strategic-region lines and both garrison lines are NOT part of that trade and stay.
	ai_strategy = {
		type = front_unit_request
		strategic_region = 133
		value = -35
	}
	ai_strategy = {
		type = front_unit_request
		strategic_region = 135
		value = -35
	}
	ai_strategy = {
		type = garrison
		value = -5000 # economy: was -1000
	}
	ai_strategy = {
		type = garrison
		id = GER
		value = -5000 # economy: was -2000
	}
}


# purpose: [med-axis-posture] - the Soviet war's Africa suppression, split out of
#          WA_AI_MILITARY_GER_war_with_soviets_2_FRONT so it can stand down while the Afrika Korps
#          window is open. Same value, same Soviet gate, one exclusion added.
# range:   -75 (unchanged; E2 [-100, +200])
# policy:  Exclusive with WA_AI_MILITARY_GER_focus_on_north_africa_FRONT's +30 - the two are the
#          only GER-scope `front_unit_request area = north_africa` writers and must never both be
#          enabled. `front_unit_request` is additive per area: enabled together they net -45 over
#          the standing -80 tilt, i.e. the expedition would exist on paper and never arrive.
# domain:  FRONT
WA_AI_MILITARY_GER_war_with_soviets_africa_suppression_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_war_with_soviets_africa_suppression_front = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = north_africa
		value = -75
	}
}


WA_AI_MILITARY_GER_war_with_soviets_3 = {
	# aifc-tuning: scripted Barbarossa objective weighting - complements the AIFC sector engine in the opening year.
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_war_with_soviets_3 = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		strategic_region = 30
		value = 150
	}

	ai_strategy = {
		type = front_control
		strategic_region = 30
		ordertype = front
		execution_type = balanced
		execute_order = yes
	}

	ai_strategy = {
	 	type = front_unit_request
		state = 196
		value = 150
	}

	ai_strategy = {
	 	type = front_unit_request
		state = 137
		value = 150
	}

	ai_strategy = {
		type = force_concentration_target_weight
		state = 137 #Sevastopol

		value = 80 # +80%
	}
}



WA_AI_MILITARY_GER_war_with_soviets_5 = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_war_with_soviets_5 = yes
	}
	abort = {
		OR = {
			NOT = { country_exists = SOV }
			date > 1942.10.1
		}
	}

	ai_strategy = {
		type = garrison
		value = 0 # economy: was -200
	}
}



WA_AI_MILITARY_GER_frontline_requests = { ###stops the AI from garrisoning random VPs
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_frontline_requests = yes
	}
	abort_when_not_enabled = yes

	####eastern front
	ai_strategy = {
		type = front_unit_request

		area = north_russia
		value = 200 # economy: was 1000
	}

	ai_strategy = {
		type = front_unit_request

		area = west_russia
		value = 200 # economy: was 1000
	}

	ai_strategy = {
		type = front_unit_request

		area = south_russia
		value = 200 # economy: was 1000
	}

	ai_strategy = {
		type = front_unit_request

		area = crimea
		value = 200 # economy: was 1000
	}

	ai_strategy = {
		type = front_unit_request

		state = 195
		value = -100 # economy: was -300
	}


	####germany and poland
	ai_strategy = {
		type = front_unit_request

		area = germany
		value = 200 # economy: was 1000
	}

	ai_strategy = {
		type = front_unit_request

		area = east_europe
		value = 200 # economy: was 1000
	}

	ai_strategy = {
		type = front_unit_request

		strategic_region = 38
		value = 200 # economy: was 750
	}

	ai_strategy = {
		type = front_unit_request

		area = austria
		value = 200 # economy: was 800
	}

	####western front
	ai_strategy = {
		type = front_unit_request

		area = benelux
		value = 200 # economy: was 900
	}

	ai_strategy = {
		type = front_unit_request

		area = south_france
		value = 200 # economy: was 400
	}

	ai_strategy = {
		type = front_unit_request

		area = west_france
		value = 200 # economy: was 750
	}

	ai_strategy = {
		type = front_unit_request

		area = north_france
		value = 200 # economy: was 750
	}

	ai_strategy = {
		type = front_unit_request

		area = france
		value = 200 # economy: was 800
	}

	#############################

	ai_strategy = {
		type = front_unit_request
		area = italy
		value = 200 # economy: was 450
	}

	ai_strategy = {
		type = front_unit_request
		area = south_italy
		value = 200 # economy: was 450
	}
	# [aifc] the -100 (ITA) / -50 (ENG) armor ids were deleted: they fought the dynamic
	# +400/-150 from WA_AI_AIFC_armor_reconcile. Armor-follows-schwerpunkt owns this now.
}



WA_AI_MILITARY_GER_frontline_requests_2 = { ###stops the AI from garrisoning random VPs
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_frontline_requests_2 = yes
	}
	abort_when_not_enabled = yes

	####eastern front
	ai_strategy = {
		type = front_unit_request

		area = north_russia
		value = 200 # economy: was 500
	}

	ai_strategy = {
		type = front_unit_request

		area = west_russia
		value = 200 # economy: was 500
	}

	ai_strategy = {
		type = front_unit_request

		area = south_russia
		value = 200 # economy: was 500
	}

	ai_strategy = {
		type = front_unit_request

		area = caucasus
		value = 200 # economy: was 500
	}

	####germany and poland
	ai_strategy = {
		type = front_unit_request

		area = germany
		value = 200 # economy: was 800
	}

	ai_strategy = {
		type = front_unit_request

		area = east_europe
		value = 200 # economy: was 750
	}

	ai_strategy = {
		type = front_unit_request

		area = austria
		value = 200 # economy: was 500
	}

	####western front
	ai_strategy = {
		type = front_unit_request

		area = benelux
		value = 200 # economy: was 500
	}

	ai_strategy = {
		type = front_unit_request

		area = south_france
		value = 200 # economy: was 500
	}

	ai_strategy = {
		type = front_unit_request

		area = west_france
		value = 200 # economy: was 500
	}

	ai_strategy = {
		type = front_unit_request

		area = north_france
		value = 200 # economy: was 500
	}

	ai_strategy = {
		type = front_unit_request

		area = france
		value = 200 # economy: was 500
	}

	####fall achse

	ai_strategy = {
		type = front_unit_request

		area = italy
		value = 200 # economy: was 300
	}

	ai_strategy = {
		type = front_unit_request

		area = south_italy
		value = 200 # economy: was 300
	}
}



WA_AI_MILITARY_GER_frontline_requests_3_vichy = { ###stops the AI from garrisoning random VPs
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_frontline_requests_3_vichy = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = south_france
		value = 200 # economy: was 500
	}
}



WA_AI_MILITARY_GER_frontline_requests_4_caucasus = { ###stops the AI from garrisoning random VPs, only go into caucasus once rostov is secure
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_frontline_requests_4_caucasus = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = caucasus
		value = 200 # economy: was 550
	}
}



WA_AI_MILITARY_GER_frontline_requests_5 = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_frontline_requests_5 = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		strategic_region = 20
		value = -100 # economy: was -1000
	}

	ai_strategy = {
		type = front_unit_request
		strategic_region = 290
		value = -100 # economy: was -1000
	}
}



# [med-axis-posture] (2026-08-17): WA_AI_MILITARY_GER_frontline_requests_6 (+100 on regions 23/21, gated
# `has_war_with = ITA` + `country_exists = RIT`) DELETED - replaced by the tag-free
# WA_AI_MILITARY_AXIS_hold_italy_after_defection_FRONT (WA_AI_MILITARY_FACTION_AXIS_FRONT.txt).



WA_AI_MILITARY_GER_run_fools_you_are_encircled_2 = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_run_fools_you_are_encircled_2 = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = crimea
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		state = 196
		value = -100 # economy: was -1000
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_dont_invade_siberia (FRONT domain)
WA_AI_MILITARY_GER_dont_invade_siberia_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_dont_invade_siberia_front = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		strategic_region = 150 #arctic russia
		value = -25
	}
}



WA_AI_MILITARY_GER_war_With_GRE = {
	enable = {
		WA_AI_MILITARY_should_ger_war_with_gre = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		strategic_region = 295
		value = 50
	}

	ai_strategy = {
		type = front_unit_request
		strategic_region = 25
		value = 50
	}

	ai_strategy = {
		type = front_control
		strategic_region = 295
		ordertype = front
		execution_type = balanced
		execute_order = yes
	}

	ai_strategy = {
		type = front_control
		strategic_region = 25
		ordertype = front
		execution_type = balanced
		execute_order = yes
	}
}



WA_AI_MILITARY_GER_war_With_GRE_rush = {
	enable = {
		WA_AI_MILITARY_should_ger_war_with_gre_rush = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		strategic_region = 295
		value = 100
	}

	ai_strategy = {
		type = front_unit_request
		strategic_region = 25
		value = 100
	}

	ai_strategy = {
		type = front_control
		strategic_region = 295
		ordertype = front
		execution_type = rush
		execute_order = yes
	}

	ai_strategy = {
		type = front_control
		strategic_region = 25
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_stop_shuffling_vic (FRONT domain)
WA_AI_MILITARY_GER_stop_shuffling_vic_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_stop_shuffling_vic_front = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = south_france
		value = -100 # economy: was -1000
	}
}



# [med-axis-posture] (2026-08-17): WA_AI_MILITARY_GER_fall_achse_c (front_control country_trigger tag = ITA,
# `date > 1942`) and WA_AI_MILITARY_GER_protect_our_weak_underbelly (front_unit_request italy +200,
# gated on GER holding FRENCH states 29/18/17 - a historical-path proxy for "France has fallen")
# DELETED. Replaced by WA_AI_MILITARY_AXIS_hold_italy_after_defection_FRONT and
# WA_AI_MILITARY_AXIS_italy_theatre_guard_FRONT (WA_AI_MILITARY_FACTION_AXIS_FRONT.txt), gated on
# the ally's / enemy's Italian geography.



WA_AI_MILITARY_GER_fall_gelb = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_fall_gelb = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = north_france
		value = 50
	}

	ai_strategy = {
		type = front_unit_request

		area = benelux
		value = 50
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_army_group_finland_does_not_exist (FRONT domain)
WA_AI_MILITARY_GER_army_group_finland_does_not_exist_FRONT = {

	allowed = {
		NOT = { tag = FIN }
	}
	enable = {
		WA_AI_MILITARY_should_ger_army_group_finland_does_not_exist_front = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = finland
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request

		area = karelia
		value = -100 # economy: was -1000
	}
}



WA_AI_MILITARY_GER_shank_greece = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_shank_greece = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		area = south_balkans
		value = 200 # economy: was 750
	}
}



WA_AI_MILITARY_GER_dont_suicide_into_maginot_line = {
	# aifc-tuning: suppresses concentration against the Maginot - safety rail; complements the AIFC sector engine.
	allowed = {
		tag = GER
	}

	enable = {
		WA_AI_MILITARY_should_ger_dont_suicide_into_maginot_line = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = force_concentration_front_factor
		state = 28  # Alsace Lorraine
		state = 17  # Franche Comte
		state = 18  # Chamagne
		ratio = 0.5      # Only if > 50 % of the front consists of Alsace-Lorraine. This should make it deactivate if the frontline extends around the maginot
		value = -100
	}

	ai_strategy = {
		type = force_concentration_target_weight
		state = 28 # Alsace-Lorraine
		state = 17  # Franche Comte
		state = 18  # Chamagne
		value = -100 # -100 % / Don't even consider targets here. Infantry and Artillery are better at dealing with the Maginot than tank formations are
	}

	ai_strategy = {
		type = front_control
		state = 28
		ordertype = front
		execute_order = no
	}
}



WA_AI_MILITARY_GER_german_civil_war = {
	allowed = {
		original_tag = GER
	}
	enable = {
		has_civil_war = yes
	}
	abort_when_not_enabled = yes
	ai_strategy = {
        type = front_control
        area = germany
        ordertype = front
        execution_type = balanced
        execute_order = yes
    }
	ai_strategy = {
		type = front_unit_request
		tag = POL
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		tag = AUS
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		tag = FRA
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		tag = CZE
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		tag = BEL
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		tag = HOL
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		tag = LUX
		value = -100 # economy: was -1000
	}
	ai_strategy = {
		type = front_unit_request
		tag = DEN
		value = -100 # economy: was -1000
	}
}


# Phase 6 split from WA_AI_MILITARY_GER_ignore_invading_these_countries (FRONT domain)
WA_AI_MILITARY_GER_ignore_invading_these_countries_FRONT = {

	allowed = {
		WA_AI_MILITARY_should_ger_ignore_invading_these_countries_front_allowed = yes
	}
	enable = {
		WA_AI_MILITARY_should_ger_ignore_invading_these_countries_front = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control

		area = north_africa

		ordertype = invasion
		execute_order = no
	}
}

############################################################################################################
# WA Military V2 - GER Country Overlay
############################################################################################################
# Phase 2 split: NAVAL domain blocks.

# Phase 2 split: NAVAL domain blocks.
# Phase 6 split from WA_AI_MILITARY_GER_battle_of_britain_yes (FRONT domain)
WA_AI_MILITARY_GER_battle_of_britain_yes_FRONT = {

	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_battle_of_britain_yes_front = yes
	}
	abort_when_not_enabled = yes

	#ai_strategy = {
	#	type = strategic_air_importance
	#	id = 2
	#	value = -500000 #value of stocked main front in active combat is usually around 35,000
	#}

	ai_strategy = {
		type = front_control
		strategic_region = 1
		ordertype = front
		execute_order = yes
	}
	ai_strategy = {
		type = front_control
		strategic_region = 2
		ordertype = front
		execute_order = yes
	}
	ai_strategy = {
		type = front_control
		strategic_region = 3
		ordertype = front
		execute_order = yes
	}
	ai_strategy = {
		type = front_unit_request
		state = 857
		value = -50
	}
	ai_strategy = {
		type = front_unit_request
		state = 127
		value = -50
	}
}

############################################################################################################
# WA Military V2 - GER Country Overlay
############################################################################################################

# Phase 6 split from WA_AI_MILITARY_GER_germany_protect_the_fatherland (FRONT domain)
WA_AI_MILITARY_GER_germany_protect_the_fatherland_FRONT = {

	allowed = {
		original_tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_germany_protect_the_fatherland_front = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control

		tag = SOV

		ratio = 0.75

		priority = 100
		ordertype = front
		execution_type = careful
		execute_order = no
	}
	ai_strategy = {
		type = front_unit_request
		area = germany
		value = 200 # economy: was 500
	}
	ai_strategy = {
		type = front_unit_request
		area = east_europe
		value = 200 # economy: was 500
	}
	ai_strategy = {
		type = front_unit_request
		area = benelux
		value = 200
	}
}


############################################################################################################
# Neutral-border economy - keep the field army off the VIC / Nationalist Spain / Swiss borders
############################################################################################################
# Observer-campaign evidence (Aug 1944 screenshot): GER lined the Vichy demarcation line, the Pyrenees
# and the Swiss border with division chains while its fronts starved. The ignore/conquer suppression in
# GER.txt kills the strategic desire against these neutrals but not the engine's border fronts - the
# front_unit_request factor is what actually empties them. Each block stands down automatically when the
# neutral stops being safe: war with the target, or (ahistorical games) the target joining a faction
# that is not ours.

WA_AI_MILITARY_GER_no_army_on_vichy_border = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_no_army_on_vichy_border = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = VIC
		value = -100 # economy: was -1000
	}
}

WA_AI_MILITARY_GER_no_army_on_nationalist_spain_border = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_no_army_on_nationalist_spain_border = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		# SPA is a country tag ALIAS (common/country_tag_aliases/tag_aliases.txt: original_tag = SPR
		# + SPR_nationalist_spain_flag), not a real tag in common/country_tags. The `tag =` payload of
		# an ai_strategy is resolved against the static tag list at load time, so `tag = SPA` threw
		# "SPA - is not in the tag list" / "invalid tag SPA" and killed the parse of this block.
		# country_trigger is evaluated at runtime in the enemy country's scope, where the alias
		# resolves normally - same targeting, no load-time tag lookup.
		country_trigger = {
			tag = SPA
		}
		value = -100 # economy: was -1000
	}
}

WA_AI_MILITARY_GER_no_army_on_swiss_border = {
	allowed = {
		tag = GER
	}
	enable = {
		WA_AI_MILITARY_should_ger_no_army_on_swiss_border = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		tag = SWI
		value = -100 # economy: was -1000
	}
}
