# Tokyo Express: is this state "held" for anchor election purposes?
# Held = controlled by Japan's side at large (Japan itself, its subjects, or any
# faction ally - user ruling 2026-07-15), or the debug override flag is up.
# Evaluated in STATE scope from JAP_te_refresh_anchors_effect (auto-generated).
JAP_te_state_held_trigger = {
	OR = {
		JAP = { has_country_flag = JAP_te_debug_anchors }
		CONTROLLER = {
			OR = {
				tag = JAP
				is_subject_of = JAP
				is_in_faction_with = JAP
			}
		}
	}
}
