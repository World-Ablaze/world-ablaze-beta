# ============================================================================
# WA AI DIFFICULTY - derived difficulty verdicts (layer 2)
# ============================================================================
# The five WA_AI_DIFFICULTY_is_* level triggers stay in WA_AI_CONFIG.txt: they read the raw
# `difficulty` enum, which is DECLARATION data and is forbidden anywhere else (checker rule
# DIFFICULTY-RAW). What lives here is every verdict COMPOSED from those five - it reads
# triggers and nothing else, so it is an observation, not a declaration.
# ============================================================================

WA_AI_DIFFICULTY_has_cheats_enabled = { # 1 or 4 = the two Hard buttons - see [difficulty-mapping] above
	OR = {
		WA_AI_DIFFICULTY_is_historical_hard = yes
		WA_AI_DIFFICULTY_is_competitive_hard = yes
	}
}

# Securities = the two Hard buttons PLUS Historical Normal: scripted assists that keep the WW2
# sequence on track (a doctrine catch-up, not an edge over the player). Off only on Historical
# Easy and Competitive Normal. Owner ruling 2026-09-04 - see [difficulty-mapping] above.
WA_AI_DIFFICULTY_has_securities_enabled = {
	OR = {
		WA_AI_DIFFICULTY_is_historical_normal = yes
		WA_AI_DIFFICULTY_is_historical_hard = yes
		WA_AI_DIFFICULTY_is_competitive_normal = yes
		WA_AI_DIFFICULTY_is_competitive_hard = yes
	}
}
