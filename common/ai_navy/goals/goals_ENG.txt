# [eng-minelaying] ENG-only raise of the mines_planting goal so the Royal Navy mines its home
# waters; assumes generic_mine_laying carries blocked_for ENG (goals_generic.txt) so the two
# goals never coexist for one country - if that blocked_for is gone, this file is a duplicate.
ENG_mine_laying = {
    objective_type = mines_planting

    available_for = {
        ENG
    }

    min_priority = 10
    max_priority = 18
}
