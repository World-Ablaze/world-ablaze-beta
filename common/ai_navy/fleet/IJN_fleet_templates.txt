# Fleet template for the IJN Tokyo Express subject. Fleet templates reference taskforce
# templates BY NAME, and every generic fleet template only names the generic taskforces
# (which exclude IJN), so without this file IJN cannot form any fleet at all.
# Other countries cannot use this fleet either: its only taskforce is allowed = IJN.
IJN_tokyo_express_fleet = {
	required_taskforces = {
		IJN_TokyoExpressPatrol_1 = 1
	}
	optional_taskforces = {
		IJN_TokyoExpressPatrol_1 = 2
	}
}
