############################################################################################################
# WA AI Navy - CAN taskforce templates
############################################################################################################

# [can-transit-attrition] DD escort while frigates < 100: Canada runs a destroyer-majority
# fleet on the British tech tree, and the generic escort template is frigate-only, so its
# navy cannot form escort task forces at all. factor 4 < generic frigate 5 keeps frigates
# preferred once available; the allowed threshold assumes `allowed` re-evaluates in play
# (unattested - owner imgui ai_navy check owed, see WORK.md).
CAN_ConvoyEscort_DD_1 = {
	allowed = {
		original_tag = CAN
		NOT = { has_navy_size = { size > 99 unit = frigate } }
	}
	ai_will_do = {
		factor = 4
	}
	mission = { convoy_escort }
	min_composition = {
		destroyer = {
			amount = 4
		}
	}
	optimal_composition = {
		destroyer = {
			amount = 10
		}
	}
}
