############################################################################################################
# WA AI Navy - CAN taskforce templates
############################################################################################################

# [can-transit-attrition] DD escort until the frigate mass is reached: Canada runs a
# destroyer-majority fleet on the British tech tree, and the generic escort template is
# frigate-only, so its navy cannot form escort task forces at all. factor 4 < generic
# frigate 5 keeps frigates preferred once available. Assumes `allowed` re-evaluates in play
# AND resolves scripted triggers (both unattested - owner imgui ai_navy check owed, WORK.md).
CAN_ConvoyEscort_DD_1 = {
	allowed = {
		original_tag = CAN
		NOT = { WA_AI_NAVY_frigate_mass_reached = yes }
	}
	ai_will_do = {
		factor = 4
	}
	mission = { convoy_escort }
	min_composition = {
		destroyer = {
			amount = 4
		}
	}
	optimal_composition = {
		destroyer = {
			amount = 10
		}
	}
}
