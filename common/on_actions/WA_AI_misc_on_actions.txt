############################################################################################################
#	World Ablaze AI mod - on_actions
############################################################################################################

### Fix 75: the railway eligibility constants that used to be redeclared here are gone. The filter
### block that read them is now the scripted trigger WA_AI_PC_country_can_build_own_logistics
### (common/scripted_triggers/WA_AI_CONSTRUCTION_triggers.txt), which owns its own copies - so this
### file no longer participates in the "@ constants are file-scoped, keep the copies in sync"
### contract at all. WA_AI_CONSTRUCTION_PRIORITY_railway_core.txt remains authoritative.

on_actions = {
	on_daily = {
		effect = {

			### cancel invalid projects

#			if = { limit = { check_variable = { WA_AI_PC_active_projects > 0 } }
#
#				for_each_loop = { array = WA_AI_PC_project_array
#
#					if = { limit = { check_variable = { v < 0 } }
#
#						set_variable = { WA_AI_PC_project_type = i }
#
#						if = {
#							limit = {
#								OR = {
#									CONTROLLER = { has_war_with = ROOT } # target state controller at war with project owner
#
#									var:v = { NOT = { WA_AI_PC_building_slot_available = yes } } # building slot unavailable in target state
#								}
#							}
#
#							set_variable = { WA_AI_PC_project = i }
#							WA_AI_PC_end_project = yes
#						}
#					}
#				}
#
#				clear_variable = WA_AI_PC_project_type
#			}

			### ai scripts

			if = { limit = { is_ai = yes }

				### train navy (game AI never training its navy workaround)

				if = {
					limit = {
						check_variable = { num_ships > 0 }
						OR = {
							has_war = no
							alliance_naval_strength_ratio > 10
						}
						has_navy_experience < 100
						fuel_ratio > 0.5
					}

					WA_AI_train_navy = yes
				}

				###

				# WA_AI_manage_xp = yes

				if = {
					limit = {
						num_of_available_civilian_factories > 1
						NOT = {
							has_country_flag = WA_AI_construction_timer
						}
						NOT = {
							OR = {
								AND = {
									tag = ENG
									date < 1942.1.1
								}
								AND = {
									tag = USA
									date < 1942.6.1
								}
							}
						}
					}
					country_event = WA_AI_C.10
				}
			}
		}
	}
	on_weekly = {
		effect = {

			if = { limit = { check_variable = { WA_AI_prepare_for_war_targets^num > 0 } }

				for_each_scope_loop = { array = WA_AI_prepare_for_war_targets

					if = { limit = { NOT = { has_country_flag = WA_AI_prepare_for_war_from_@ROOT } }

						add_to_temp_array = { remove_prepare_for_war_targets = THIS.id }
					}
				}

				for_each_scope_loop = { array = remove_prepare_for_war_targets

					remove_from_array = { array = ROOT.WA_AI_prepare_for_war_targets value = THIS.id }
				}
			}

			### Priority Construction System - Weekly Update
			#
			# FIX 75 (2026-08-14, campaign f9321934): the block gate was `is_ai = yes` +
			# `has_capitulated = no`. It is now `is_ai = yes` + `exists = yes`, and the three calls
			# that genuinely need the capitulation test carry it individually (training modifier,
			# offensive posture, lend-lease relief). Statement ORDER is unchanged - that is why the
			# gate moved onto the calls instead of the block being split in two.
			#
			# Root cause it fixes: `WA_AI_priority_construction_strategies` fires from
			# WA_AI_background.1 gated only on `is_ai = yes`, so a capitulated country kept QUEUEING
			# projects while never assigning factories to them or progressing them - the queue was
			# write-only. FRA on f9321934 sat on 14 projects byte-identical across 12 months
			# (assigned_factories and progress frozen mid-value), 10 of them railways aimed at
			# GER-held metropolitan France, with 21 civ factories idle and 49 controlled states
			# including the whole Maghreb. `has_capitulated` answers "did this government fall", not
			# "can this country build" - same category error R27 records for surrender_progress.
			#
			# Safety already in place for the newly-iterated population: Fix 34's controller test in
			# WA_AI_PC_assign_factories refuses factories to any project whose target state is
			# hostile-controlled, so the France-aimed fossils stay frozen rather than burning civ
			# weeks; the Fix 41 aging lane then revalidates them and cancels what is obsolete, which
			# is how the queue self-cleans. WA_AI_PC_complete_project_by_id applies the same
			# controller test before spawning anything. `exists = yes` keeps annexed corpses out (the
			# open question Fix 68 flagged as unverified) at the cost of one cheap trigger.
			if = {
				limit = {
					is_ai = yes
					exists = yes
				}

				### AI Training Modifier System
				# Still capitulation-gated: this trains divisions, which is not what a fallen
				# government should be doing with the pulse.
				if = {
					limit = { has_capitulated = no }

					if = {
						limit = {
							NOT = { has_dynamic_modifier = { modifier = WA_AI_training_modifier } }
						}
						add_dynamic_modifier = { modifier = WA_AI_training_modifier }
					}
					WA_AI_update_training_modifier = yes
				}

				# Dynamic factory allocation to projects
				WA_AI_PC_assign_factories = yes

				# Update construction progress and complete finished projects
				WA_AI_PC_update_project_progress = yes

				### Railway building - with eligibility filters to reduce processing.
				# Fix 75: the filter block that used to be inlined here now lives in
				# WA_AI_PC_country_can_build_own_logistics (WA_AI_CONSTRUCTION_triggers.txt), terms
				# and thresholds unchanged. It is the same judgement Fix 74's ally leg needs, and
				# two hand-maintained copies of it were already listed as a drift risk on R46.
				if = {
					limit = { WA_AI_PC_country_can_build_own_logistics = yes }
					if = { limit = { has_country_flag = WA_AI_construction_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | RAILWAY DEBUG: on_weekly eligibility PASSED - calling WA_AI_PC_railway" }
					WA_AI_PC_railway = yes
				}
				else = {
					# Keep the interval counter ticking while ineligible, so a country that
					# recovers eligibility fires on its first eligible week instead of waiting
					# out up to 8 remaining weeks of a frozen interval.
					if = {
						limit = { check_variable = { WA_AI_PC_railway_INTERVAL_counter > 0 } }
						subtract_from_variable = { WA_AI_PC_railway_INTERVAL_counter = 1 }
					}
				}

				### Offensive posture and lend-lease relief - both still capitulation-gated (Fix 75).
				# Posture publishes an "attack now" verdict for front_control blocks and lend-lease
				# pulls equipment from allies; neither is something a fallen government should be
				# doing, and unlike PC neither has a controller test protecting it downstream.
				if = {
					limit = { has_capitulated = no }

					### Offensive posture - weekly "should we execute battle plans" verdict
					# Publishes WA_AI_MILITARY_posture / _posture_vs_<TAG>, consumed by front_control blocks.
					# Eligibility lives in WA_AI_MILITARY_should_update_posture; the effect no-ops when the
					# country does not qualify. Switches: WA_AI_MILITARY_posture_triggers.txt.
					WA_AI_MILITARY_update_posture = yes

					### Lend-lease surplus relief - pull a transfer from a hoarding ally
					# ROOT = recipient; no-ops unless ROOT is starving (per archetype). Policy
					# triggers live in WA_AI_LEND_LEASE_triggers.txt, effect in
					# WA_AI_lend_lease_effects.txt; donors are required to be AI by the pair trigger.
					WA_AI_LEND_LEASE_request_surplus_relief = yes
				}
			}

			### AI Force Concentration - weekly sector maintenance
			# Eligibility lives in WA_AI_AIFC_should_select_sector; the effect no-ops (and clears any
			# stale sector) when the country does not qualify. Switches: WA_AI_AIFC_triggers.txt.
			#
			# FIX 68 (2026-08-14, campaign f9321934): this call used to sit inside the Priority
			# Construction block above, which is gated `is_ai = yes` + `has_capitulated = no`. That made
			# WA_AI_AIFC_core.txt's "not eligible -> clear sector" branch UNREACHABLE for exactly the
			# population it was written for: a capitulated country was never iterated, so its sector could
			# not age, could not be revalidated, and could not be cleared. It simply froze at whatever was
			# selected the week before the capital fell. FRA carried its May-1940 sector (anchor = 34
			# Wallonie, age = 4) byte-identical through SEVEN samples spanning 1942.6 -> 1946.8 while alive
			# and controlling 50 states, and surfaced on checklist R39 leg 1 as a 5-division sector holder
			# that Fix 56's `has_army_size` term could never reach. Annexed tags (DEN, SHA, SIC, PER, BRM
			# on 02bd4445) show the same frozen sectors; whether THAT half is also fixed here depends on
			# whether on_weekly iterates non-existent countries at all, which is UNVERIFIED - do not claim
			# it without checking a save. Harmless either way: the tags do not exist and nothing reads them.
			#
			# Lifted to its own `is_ai = yes` block rather than deleting `has_capitulated = no` from the
			# block above: that word is load-bearing for everything else in it - the AI training modifier,
			# PC factory assignment, PC project progress, the railway queue, offensive posture and
			# lend-lease relief would ALL newly run for every capitulated tag, a far larger blast radius
			# than the defect being fixed. AIFC is the only one of the seven whose effect is DESIGNED to be
			# entered while ineligible, so it is the only one that can safely move out.
			#
			# Cost for the newly-iterated tags is a failed cheap trigger (has_capitulated is the 3rd
			# conjunct, ahead of every scope walk) plus an idempotent clear - four clear_array, two
			# clear_variable - and an armour reconcile that finds empty books after the first pass. The
			# expensive selection body is NOT reached: it lives behind the else branch.
			#
			# NOTE this does not give a capitulated country a sector - `has_capitulated = no` still stands
			# in WA_AI_AIFC_should_select_sector, which is where that judgement belongs. It gives it a
			# CLEARED one. If a campaign ever shows a capitulated-but-recovered major (a liberated France
			# fielding a real army) that visibly wants a schwerpunkt, relax the term in the trigger file -
			# not here.
			if = {
				limit = { is_ai = yes }

				WA_AI_AIFC_update_sector = yes
			}
		}
		effect = {
			if = {
				limit = {
					is_ai = yes
					not = {has_country_flag = trade_fix_ai_strat}
				}
				if = {
					limit = {
						not = {has_tech = trade_fix_tech}
					}
					set_technology = { trade_fix_tech = 1 popup = no }
				}
				set_country_flag = trade_fix_ai_strat
				add_equipment_production = {
					equipment = {
						type = trade_fix_equipment_0
					}
					requested_factories = 1
					progress = 0.65
					amount = 1
				}
				add_offsite_building = { type = dockyard level = 1 }
			}
		}
	}
	on_monthly = {
		effect = {
			if = {
				limit = { is_ai = yes }
				WA_AI_TEMPLATES_calculate_templates = yes
				WA_AI_RESEARCH_check_for_empty_research_slots = yes

				# Fix 39: maintain the industry-overextension hysteresis flag (reads the
				# WA_AI_needs_* trackers; consumed by WA_AI_queue_MIC's substitution
				# cascade and the synth research triggers). Evaluation logic and flag
				# semantics: WA_AI_update_industry_overextension in WA_AI_misc_effects.txt.
				WA_AI_update_industry_overextension = yes

				# WA_TLM telemetry: month clock (once per month via timed global flag)
				# + gauge/ring-buffer sampling. Write-only observability - no gameplay
				# reads. (documentation/WA_TLM_TELEMETRY_SYSTEM.md; WA_TLM_core.txt)
				WA_TLM_tick_clock = yes
				WA_TLM_monthly_sample = yes

				# Fix 47: latch the ai_equipment design-context gates into timed
				# country flags (resource headroom, long-range air need, enemy
				# strategic bombing). Design switches cost XP and reset production
				# efficiency, so the ai_equipment priority modifiers read the
				# LATCHED flags, never the instantaneous readings. Latch shapes and
				# lifetimes: WA_AI_EQUIPMENT_update_context_flags in
				# common/scripted_effects/WA_AI_EQUIPMENT_effects.txt. Placed after
				# WA_TLM_tick_clock so its R31/R32 probe stamps carry this month's
				# clock value.
				WA_AI_EQUIPMENT_update_context_flags = yes
			}
		}
	}
	on_bi_yearly_pulse = {
		effect = {

			if = { limit = { NOT = { has_country_flag = WA_AI_on_bi_yearly_pulse_checked } } # on_bi_yearly_pulse triggers 24x per pulse?

				set_country_flag = { flag = WA_AI_on_bi_yearly_pulse_checked value = 1 days = 10 }
			}
		}
	}
	on_puppet = {
		effect = {

			### clear ideology advisors

			ROOT = {

				if = { limit = { is_ai = yes }

					clr_country_flag = WA_AI_democratic_focus
					clr_country_flag = WA_AI_fascist_focus
					clr_country_flag = WA_AI_communist_focus
					clr_country_flag = WA_AI_neutral_focus

					remove_ideas_with_trait = communist_revolutionary
					remove_ideas_with_trait = ambitious_union_boss
					remove_ideas_with_trait = fascist_demagogue
					remove_ideas_with_trait = syncretic_revanchist
					remove_ideas_with_trait = staunch_monarchist
					remove_ideas_with_trait = colonial_communist
					remove_ideas_with_trait = social_reformer
					remove_ideas_with_trait = liberal_journalist
					remove_ideas_with_trait = ambitious_union_boss
					remove_ideas_with_trait = socialist_novelist
					remove_ideas_with_trait = anti_communist_crusader
					remove_ideas_with_trait = monarchist_media_mogul
					remove_ideas_with_trait = royalist_bulldog
					remove_ideas_with_trait = shadow_of_calles

					if = { limit = { FROM = { WA_AI_democratic_nation = yes } } set_country_flag = WA_AI_democratic_focus }
					else_if = { limit = { FROM = { WA_AI_fascist_nation = yes } } set_country_flag = WA_AI_fascist_focus }
					else_if = { limit = { FROM = { WA_AI_communist_nation = yes } } set_country_flag = WA_AI_communist_focus }
					else_if = { limit = { FROM = { WA_AI_nonaligned_nation = yes } } set_country_flag = WA_AI_neutral_focus }
				}
			}
		}
	}
#	on_annex = {
#		effect = {
#
#			# remove all projects by the annexed country
#			if = { limit = { check_variable = { WA_AI_PC_active_projects > 0 } }
#
#				for_each_loop = { array = WA_AI_PC_project_array
#
#					if = { limit = { check_variable = { v < 0 } }
#
#						set_variable = { WA_AI_PC_project = i }
#						WA_AI_PC_end_project = yes
#					}
#				}
#			}
#		}
#	}
	on_war = {
		effect = {
			if = {
				limit = {
					is_ai = yes
					has_idea = WA_AI_peace_attrition_cheat
				}
				remove_ideas = WA_AI_peace_attrition_cheat
			}
		}
	}

	on_declare_war = {
		effect = {

			# force update scipted AI
			if = { limit = { FROM = { is_ai = yes } }
				hidden_effect = {
					FROM = { country_event = { id = WA_AI_background.0 hours = 1 } }
				}
			}
			if = { limit = { is_ai = yes }
				hidden_effect = {
					country_event = { id = WA_AI_background.0 hours = 1 }
				}
			}

			# barbarossa
			if = {
				limit = {
					is_ai = yes
					is_in_faction_with = GER
					is_neighbor_of = SOV
					FROM = { tag = SOV }
				}

				hidden_effect = {
					# set_country_flag = { flag = WA_AI_barbarossa_blitz value = 1 days = 30 }

					SOV = { set_country_flag = { flag = WA_AI_barbarossa_defensive value = 1 days = 270 } }
				}
			}
		}
	}
	on_nuke_drop = {
		effect = {
			if = { limit = { has_country_flag = WA_AI_logging } log = "[Root.GetName] nuked [From.GetName]" }
		}
	}
	on_state_control_changed = { #ROOT is new controller #FROM is old controller #FROM.FROM is state ID
		effect = {

			FROM.FROM = {
				# Check if old controller (FROM) has any projects in this state
				if = { limit = { check_variable = { WA_AI_PC_projects_in_state_of_@FROM^num > 0 } }

					# Collect project IDs to cancel
					clear_temp_array = _cancel_projects_IDS
					for_each_loop = { array = WA_AI_PC_projects_in_state_of_@FROM value = _proj_id

						# Check if new controller has permission to continue this project
						if = {
							limit = {
								NOT = {
									is_in_array = { array = FROM.WA_AI_PC_construction_permissions value = ROOT.id }
									FROM = { tag = ROOT }
									FROM = { is_in_faction_with = ROOT }
									FROM = { is_subject_of = ROOT }
									ROOT = { is_subject_of = FROM }
								}
							}

							add_to_temp_array = { _cancel_projects_IDS = _proj_id }
						}
					}

					# Cancel invalid projects
					if = { limit = { check_variable = { _cancel_projects_IDS^num > 0 } }
						FROM = { WA_AI_PC_cancel_projects = yes }
					}
				}
			}
		}
		effect = {
			if = {
				limit = {
					FROM.FROM = {
						OR = {
							is_one_state_island = yes
							is_island_state = yes
						}
						NOT = {
							has_dynamic_modifier = { modifier = island_province_ai }
						}
						CONTROLLER = {
							is_ai = yes
						}
					}
				}
				FROM.FROM = {
					add_dynamic_modifier = { modifier = island_province_ai }
				}
			}
		}
	}
	on_startup = {
		effect = {
			if = {
				limit = {
					is_historical_focus_on = yes
					POR = {
						is_ai = yes
					}
				}
				POR = {
					set_rule = { can_not_declare_war = yes }
				}
			}
		}
	}

	### Fix 5 & 19: Clear railway projects when peace is made
	### This prevents outdated railway projects from continuing after frontlines disappear
	on_peace = {
		effect = {
			if = {
				limit = {
					is_ai = yes
					check_variable = { WA_AI_PC_queue^num > 0 }
				}

				# Cancel all railway projects (type 13) from the PC queue
				# Store IDs first, then remove (can't modify array during iteration)
				set_temp_variable = { _cancel_count = 0 }
				while_loop_effect = {
					limit = {
						check_variable = { WA_AI_PC_queue^num > 0 }
						check_variable = { _cancel_count < 100 }  # Safety limit
					}

					set_temp_variable = { _found_railway = 0 }
					# Reset the break var each pass: temp vars persist across while_loop iterations, so a
					# stale _found_break = 1 would make every later pass break on element 0 and miss the
					# remaining railway projects (only ~1 project per peace deal was actually cancelled).
					set_temp_variable = { _found_break = 0 }
					for_each_loop = { array = WA_AI_PC_queue value = _proj_id break = _found_break
						if = { limit = { check_variable = { WA_AI_PC_building_type^_proj_id = 13 } }
							set_temp_variable = { _project_id = _proj_id }
							set_temp_variable = { _found_railway = 1 }
							set_temp_variable = { _found_break = 1 }
						}
					}

					if = { limit = { check_variable = { _found_railway = 1 } }
						WA_AI_PC_end_project_by_id = yes
						add_to_temp_variable = { _cancel_count = 1 }
					}
					else = {
						# No more railway projects
						set_temp_variable = { _cancel_count = 100 }
					}
				}

				# Reset interval counter to trigger immediate recalculation
				set_variable = { WA_AI_PC_railway_INTERVAL_counter = 0 }

				if = { limit = { has_country_flag = WA_AI_construction_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSTRUCTION: Railway projects cancelled due to peace" }
			}
		}
		effect = {
			if = {
				limit = {
					is_ai = yes
					has_war = no
					NOT = { has_idea = WA_AI_peace_attrition_cheat }
				}
				add_ideas = WA_AI_peace_attrition_cheat
			}
		}
	}

	on_peaceconference_ended = {
		effect = {
			every_country = {
				limit = {
					is_ai = yes
					has_war = no
					NOT = { has_idea = WA_AI_peace_attrition_cheat }
				}
				add_ideas = WA_AI_peace_attrition_cheat
			}
		}
	}

	on_capitulation_immediate = {
		effect = {
			ROOT = {
				if = {
					limit = {
						tag = HOL
						is_ai = yes
					}
					remove_resource_rights = 672
					remove_resource_rights = 334
					remove_resource_rights = 673
					remove_resource_rights = 287
				}
			}
		}
	}

	on_weekly_GER = {
		effect = {
			if = {
				limit = {
					is_ai = yes
					tag = GER
					has_war_with = SOV
					date > 1941.10.1
					date < 1945.4.1
				}
				if = {
					limit = {
						has_equipment = { infantry_equipment < 1000 }
					}
					add_equipment_to_stockpile = {
						type = infantry_equipment
						producer = GER
						amount = 40000
					}
				}
				if = {
					limit = {
						has_equipment = { support_equipment < 1000 }
					}
					add_equipment_to_stockpile = {
						type = support_equipment
						producer = GER
						amount = 35000
					}
				}
				if = {
					limit = {
						has_equipment = { heavy_infantry_equipment < 1000 }
					}
					add_equipment_to_stockpile = {
						type = heavy_infantry_equipment
						producer = GER
						amount = 30000
					}
				}
				if = {
					limit = {
						has_equipment = { artillery_equipment < 100 }
					}
					add_equipment_to_stockpile = {
						type = artillery_equipment
						producer = GER
						amount = 3000
					}
				}
				if = {
					limit = {
						has_equipment = { heavy_artillery_equipment < 100 }
					}
					add_equipment_to_stockpile = {
						type = heavy_artillery_equipment
						producer = GER
						amount = 3000
					}
				}
				if = {
					limit = {
						has_equipment = { mechanized_equipment < 100 }
					}
					add_equipment_to_stockpile = {
						type = mechanized_equipment
						producer = GER
						amount = 1000
					}
				}
				if = {
					limit = {
						has_equipment = { heavy_anti_air_equipment < 100 }
					}
					add_equipment_to_stockpile = {
						type = heavy_anti_air_equipment
						producer = GER
						amount = 2000
					}
				}
				if = {
					limit = {
						has_equipment = { anti_tank_equipment < 100 }
					}
					add_equipment_to_stockpile = {
						type = anti_tank_equipment
						producer = GER
						amount = 3000
					}
				}
				if = {
					limit = {
						has_equipment = { medium_tank_chassis < 100 }
					}
					add_equipment_to_stockpile = {
						type = medium_tank_chassis
						producer = GER
						amount = 500
					}
				}
				if = {
					limit = {
						has_tech = ger_modern_tank_chassis_1
						has_equipment = { modern_tank_chassis < 100 }
					}
					add_equipment_to_stockpile = {
						type = modern_tank_chassis
						producer = GER
						amount = 500
					}
				}
			}
		}
	}
}
