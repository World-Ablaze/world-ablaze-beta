add_namespace = rom_armor

country_event = {
	id = rom_armor.1
	hidden = yes

	mean_time_to_happen = { days = 2 }

	fire_only_once = yes

	trigger = {
		tag = ROM
		is_ai = yes
		OR = {
			has_tech = generic_anti_tank_1
			has_tech = ger_anti_tank_1
		}
		has_tech = tech_recon
		has_tech = tech_logistics_company
	}

	immediate = {
		# set_country_flag = infantry_template_rom # Removed - was blocking reserve template system
		ROM_leonard_mociulschi = {
			add_trait = { trait = infantry_leader }
		}
		if = {
			limit = {
				is_ai = yes
			}
			add_advisor_role = {
				character = "ROM_leonard_mociulschi"
				advisor = {
					slot = high_command
					idea_token = ROM_leonard_mociulschi
					ledger = army
					traits = {
						army_infantry_2
					}
					cost = 100
					ai_will_do = {
						factor = 100
					}
				}
			}
		}
	}

	option = {
	}
}