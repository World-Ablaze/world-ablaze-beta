############################################################################################################
# RECRUIT LOOP console harness (subject: recruit-loop). harness-contract: v1.
#
# Recipe (observer game; fire per tag, do NOT tag into the country):
#   event wa_test_rl.1 ARG    -> counts + gate/watchdog state in logs/game.log under "RECRUIT LOOP"
#   event wa_test_rl.2 ARG    -> one real recruit pass; post line prints the general-count delta
#                                (+1 = generate_character registers, the orphan bug is dead;
#                                 0 = unpause 1-2 days, re-fire wa_test_rl.1, judge then)
#   CONTROL (known-closed gate): event wa_test_rl.1 TUR -> generals >= 3, no recruit expected.
# Read the "scope :" line first: anything but 1 1 1 1 0 means STOP (see the harness file).
# Effects: common/scripted_effects/WA_TEST_recruit_loop.txt
# Own file, own namespace, per the harness contract - never home this in wa_events_test.txt.
############################################################################################################

add_namespace = wa_test_rl

# Gate + watchdog report
country_event = {
	id = wa_test_rl.1
	title = wa_test_rl.1.t
	desc = wa_test_rl.1.d
	is_triggered_only = yes
	hidden = yes
	fire_only_once = no

	immediate = {
		WA_TEST_RL_report = yes
	}
}

# Execute one real recruit pass
country_event = {
	id = wa_test_rl.2
	title = wa_test_rl.2.t
	desc = wa_test_rl.2.d
	is_triggered_only = yes
	hidden = yes
	fire_only_once = no

	immediate = {
		WA_TEST_RL_execute = yes
	}
}
