add_namespace = saf_armor

country_event = {
	id = saf_armor.901
	hidden = yes

	mean_time_to_happen = { days = 2 }

	fire_only_once = yes

	trigger = {
		tag = SAF
		is_ai = yes
		has_war = yes
		controls_state = 275
		has_completed_focus = SAF_raise_the_south_african_army
	}

	immediate = {
		if = {
			limit = {
				NOT = {
					has_doctrine = decisive_strategy
				}
			}
			set_sub_doctrine = decisive_strategy
		}
		WA_add_mastery_infantry_100 = yes
	}

	option = {
	}
}

