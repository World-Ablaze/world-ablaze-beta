add_namespace = sik_armor

country_event = {
	id = sik_armor.1
	hidden = yes

	mean_time_to_happen = { days = 1 }

	fire_only_once = yes

	trigger = {
		tag = SIK
		date > 1937.9.1
		OR = {
			is_in_faction_with = SOV
			is_subject_of = SOV
		}
		TUG = { is_in_faction = no }
	}

	immediate = {
		declare_war_on = {
			target = TUG
			type = annex_everything
		}
	}

	option = {}

}