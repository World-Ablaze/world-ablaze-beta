# Work queue

Exactly one **ACTIVE** subject at a time. Anything found on the way goes to **QUEUE**, never into
the subject in hand. That is the rule that stops a session fanning out into six directions.

Enforced by `python tools/check_worklist.py`.

- **ACTIVE** — exactly one entry. Opening a second means demoting the first to QUEUE with its state.
- **QUEUE** — three columns are mandatory: the subject, why it matters, and **what would close it**.
  A row with no closing criterion is a wish, not a task.
- **DONE** — the last ten, then pruned. `git log` is the archive.

---

## ACTIVE

- **Allied North Africa: the invader's own logistics (opened 2026-08-20 from campaign `3d68a183` 1944.4).** Two legs, one subject - what the Allies need in order to finish North Africa, measured rather than guessed. Leg 1, RAIL, is **implemented and untested in game**: the corridor node list began at Tunis and ran east, i.e. the defender's corridor, so the Torch approach march Oran-Algiers-Constantine-Bone-Bizerte-Tunis sat at its **1936 level 2** (20 supply, `rail.py` BREAK=0 THIN=5) for the whole campaign while the front asked 41 at Constantine - and east of Tunis the same system had reached level 3-4. Fix 113 prepends five nodes (Oran 7132, Algiers 1145, Constantine 9976, Bone 7081, Bizerte 9994; all already carry a supply node, the four coastal ones marked `port_ = 1` because Bone is level 1 = 5 supply and binds the injection) and raises `corridor.max_routes_per_run` 14 -> 19 in the same change, because that constant is the validator's completeness guarantee and not a throughput knob. Leg 2, AIR, is **implemented and untested in game** (Fix 114, `685c36fb7`): `WA_AI_build_theatre_air_bases` already did the hard parts (contest detection, per-theatre deficit target, ground commitment, the ally-funding gate that lets ENG build on FRA soil) but its ladder selected on `building_level@air_base` alone over a theatre spanning Morocco to Egypt, so at 1944.4 the four live `theatre_air` projects were ENG->Marrakech, ENG->Casablanca, USA->Tafialet and USA->Constantine while front-line Constantine ran at 150 % nominal load and front-line Batna was level 0. The ladder now runs twice - a contested-edge phase (`WA_AI_PC_state_is_near_contested_edge`, two neighbour rings) then the old whole-theatre ladder as fallback - rather than a second strategy, which would have opened a second admission path into the same `qmax.theatre_air = 3` / `prio.air_front` lane, the shape Fix 77 closed. **Closed when** a campaign shows the western hops above level 2 while the Allies stand in Algeria, AND the theatre air ladder placing its projects on the contested edge rather than in Morocco. Probes R77 (rail) and R78 (air) are written, both NOT YET TESTED; both fixes are registered.

---

## QUEUE

| # | Subject | Why it matters | Closed when |
| --- | --- | --- | --- |
| 0h | **Italy's Mediterranean garrison posture: three fixes shipped, none campaign-tested (Fix 108 / 109 / 110, opened 2026-08-19 from `07270b64` 1943.8).** Diagnosis: the Adriatic garrisons the user reported are ENGINE area-defence orders (`area_defense_settings = 100`), not WA buffers, and Provence/Toulon had zero Italian divisions because WA's nine-state southern-France buffer spread six divisions over five inland states. Root cause of the engine's free hand: the `garrison = -5000` block, and its DIPLOMACY and NAVAL siblings, were gated on `NOT = { has_war_with = ETH }` and died on 1941.2.7 when Ethiopia re-entered the war. **Fix 108** swaps that gate for `WA_AI_MILITARY_ethiopian_war_finished`; **Fix 109** puts the eastern Adriatic shore (order 9613) under the existing Otranto gate; **Fix 110** splits the southern-France buffer into a non-yielding coastal order 9610 {21,22,735} and an inland 9614 at a third of the weight. Probes R74 / R75 / R76 written, all NOT YET TESTED. **Closed when** a campaign scores all three, with R74 leg 1 the first in-game measurement of what `garrison = -5000` actually does to engine area-defence orders. | Three fixes shipped 2026-08-19, none has run in a campaign; demoted 2026-08-20 when Allied North Africa was opened, state intact | A campaign scores R74, R75 and R76, with R74 leg 1 the first in-game measurement of what `garrison = -5000` does to engine area-defence orders |
| 0e | Trade law: R28's two defects fixed, three dead gates removed — SHIPPED `32c03c550`, PLUS A REVERT ON TOP, NOT TESTED IN GAME. `WA_AI_can_take_limited_exports` was unsatisfiable for every country at every date: two bare multi-child `NOT` blocks are NOR in HOI4, so one demanded `has_war = no` and the other `has_war = yes` (R28 defect 1, proved empirically by R28 — zero AI-path grants across a full campaign). Both are now `NOT = { AND = { ... } }`. Three clauses went with it, each with no recoverable reason (`git log -S` returns only `bd11fde6b`, the commit that created the file; Expert AI 5.0 has no counterpart): `NOT = { has_government = democratic  tag = ENG }` (a NOR, so it barred every democracy plus Britain), the minor-in-a-faction-at-war bar, and the democratic arm's `any_enemy_country = { ic_ratio > 0.2 }` (it inverts late-war). The eight bare `75` literals in `WA_AI_calculate_resource_need` now read `constant:wa_ai_resource.trade_law.resource_floor`. REVERTED on top of the commit: the export-recovery admission path (`WA_AI_LAW_calculate_export_recovery`, `WA_AI_LAW_export_recovery_pays`, `min_net_gain`). An in-game probe against `test_BHU_1943_04_01_04` MEASURED that `resource_exported@<r>` returns `to_export`, the amount OFFERED, not the amount sold — 12 readings of 12, exact to the unit, Sweden reading 123 offered steel while its `exported` column is 0. The path's loss term therefore always exceeded its gain (real nets −15.9 to −96.3 civ-equivalents) and it could never fire. It failed CLOSED, so nothing regressed, but an inert path with a 40-line header is the shape Fix 106 deleted. `resource_produced@` and `resource_imported@` were probed at the same time and DO match their columns. Falsifiable prediction, from `07270b64` itself: the USA is the one country in that campaign that clears every surviving clause — at 1944.10 its `wa_ai_average_resource_need` is 0.60126 (bar 0.60) and three of the five shortage counters read 3 (rubber, chromium, tungsten) against a bar of two, and the `ic_ratio` clause that used to veto it is gone. So a re-run should show USA holding `limited_exports` from roughly 1944.10, its rubber/chromium/tungsten deficits shrinking, and no other country changing law. If the USA does not switch, the R28 fix did not take. Closed on that, plus no country flipping trade law more than once a year. One campaign, one case — another world will place it elsewhere. (demoted 2026-08-19, state intact) | Shipped and reverted-in-part on the same day; nothing has run in game since. | A campaign scores R28 against the shipped `WA_AI_can_take_limited_exports`, and the reverted export-recovery path is either re-authored against a `to_export`-aware measure or written off in a ledger row. |
| 0g | Four more `NOT = { has_war_with = ETH }` gates, deliberately left alone | Fix 108 changed only the three blocks split from the same original `ITA_war_against_ENG`. The `ignore_neutral_ethiopia` pair (ITA_FRONT:458, ITA_DIPLOMACY:912) genuinely asks "is ETH neutral" and is Fix 97's, so it is correct as written - but nobody has checked whether Fix 97's intent survives an ETH that is hostile rather than neutral, which is the state `07270b64` actually reached. | A reading of Fix 97's two blocks against a hostile-ETH campaign says whether "neutral" or "not a threat" was the intended question. |
| 0f | Abruzzo is in no WA garrison list (Corsica closed by Fix 111) | Fix 108 restores `garrison = -5000`, which removes the engine order 883 that was the only cover for both. Corsica now has its own buffer (Fix 111, order 9615); **Abruzzo does not** — cored Italian mainland on the Adriatic, absent from Fix 96's four mainland anchors (Piedmont, Tuscany, Latium, Rome). | Either Abruzzo joins the Fix 96 anchor list with a stated reason, or a campaign shows the mainland front covers it and the row is written off. |
| 0 | Garrison echelon removed — IMPLEMENTED, UNDER CAMPAIGN TEST (demoted 2026-08-19, state intact) | `role = garrison` no longer exists in `common/ai_templates/` (replace_path'd, so vanilla's is not loaded either), and its `role_ratio` share, gate trigger, calculator, flag and type code went with it. The two composition attempts of 2026-08-19 (`4f67ea1c0` light, `a60d5a976` heavy) both regressed; removing the role removes the capture mechanism instead of re-tuning a match-score margin. Static checks pass; nothing measured in game yet | A campaign shows no AI country holding a garrison-role template, SOV/ITA/FIN keep recruiting their starting rifle divisions past 1936.1.7 (the 87-division freeze does not recur), occupied territory is still garrisoned, and total wanted/fielded divisions are not below the last scored campaign |
| 0a | `WA_AI_trade_law_recently_changed` is written and read by nothing | MEASURED 2026-08-19: one occurrence mod-wide, the `set_country_flag` in `WA_AI_upgrade_trade_law` that creates it, comment "limits the AI from changing trade law due to minor fluctuations". So no trade-law transition in the mod has any anti-thrash brake — a country can walk the ladder every ~2 days. The new export-recovery path honours the flag; the four pre-existing `WA_AI_can_take_*` thresholds do not, and wiring them all in would freeze every country's law ladder for a year as a side effect | The three older paths either read the flag, or the item records why an unguarded ladder is intended — and a campaign shows how often a law actually changes today |
| 0b | Fix 102 `WA_AI_coop_can_supply_<r>` is 1 for everyone — its sold-out test never happens | It computes `min_export x resource_produced@r - resource_exported@r` expecting the UNSOLD residue. MEASURED 2026-08-19 in game (`test_BHU_1943_04_01_04`, 12 of 12 readings exact): `resource_exported@r` returns `to_export`, i.e. `min_export x (produced + transfer)`, so the subtraction yields `-f x transfer` — zero or negative for every country not feeding an overlord, always under one quantum, always "sold out". Every producer whose OFFER clears one quantum reads `can_supply = 1` whether or not a single unit is bought. The effect ships since 2026-08-17 and feeds `WA_AI_allies_need_<r>` across a 28-state decision array. The code site now records the disproof; the logic is untouched | The sold-out test is rebuilt on a quantity script can actually read, or the item records that no such quantity exists and `WA_AI_coop_can_supply_<r>` is retired with its readers |
| 0d | The recovery path only covers export_focus/free_trade → `limited_exports` | `WA_AI_can_take_export_focus` selects its threshold through a four-branch `if/else_if` chain; wrapping that is the in-place rewrite of shared code AGENTS.md warns against, so the free_trade → export_focus transition has no recovery path. Measured cost of the omission is currently nil (no country scored above 0.4 for that step on any of six dates), but the asymmetry is undocumented in the trigger itself | Either the chain carries the same second path, or the trigger says why that transition does not need one |
| 1 | `front_control`: is resolution per-field or whole-block? | The engine says only "higher prio strats will override lower" and, per field, "if set will override". The whole §6.1.1 collision audit rests on the answer: `CHINA_FRONT_careful_exec_vs_japan` (330) sets no `execute_order`, so under per-field resolution it never overrode the CHI holds and two of the listed collisions do not exist. | The answer is MEASURED - in-game, or from vanilla / Expert AI usage that can only work one way - and §6.1.1 is re-scored against it |
| 2 | The three real `front_control` collisions still have no fix | GER `protect_the_fatherland` (hold vs SOV under `AXIS_exec_vs_sov`), CZE `sit_on_your_forts`, FRA `paris_commune_sit_tight_2`. Re-tiering was refused by both reviewers: GER's gate is monotonic and would become a permanent Eastern-Front veto, FRA's is self-latching. The sanctioned route is a §6.2 ownership slug with the exclusion inside the exec block's `country_trigger`. | Each of the three either carries a slug, or the item records why it is subordinate on purpose - and a probe exists that tells "overridden" from "never enabled" |
| 3 | Four `CHI_war_with_JAP*` blocks tie at priority 0 with contradictory payloads | `_AI_FRONT` and `_FRONT` are holds, `_2_FRONT` sets no `execute_order`, `_3` is `balanced` + `manual_attack = yes` - and from 1944 three of them can be enabled at once on `tag = JAP` at the same priority. An unresolved intra-layer tie, found by the architecture reviewer during the collision audit; distinct from the Faction-override question. | The four are collapsed, or separated by gate or priority so at most one applies at a time |
| 4 | Confirm the toolpack fix on a boot with the tool ENABLED | The `ROOT = { }` wrapper was removed from all 8 window containers on 2026-08-18, but every boot since has had the toolpack off (`has_global_flag = toolpack_enabled` is a player action), so the fix is **unverified**. Baseline to beat: 3,531 `root: Invalid Scope` at `toolpack.txt:10` plus 3,627 paired `could not find this character in country SWE`, in ~2 minutes of a PAUSED 1936.1.1 game. | A boot with the toolpack enabled writes neither signature |
| 5 | The clean-boot actionable list, now that it is readable | `python tools/triage_error_log.py` on a clean boot (112 lines) returns **31 actionable sites in 3 families**: 25 country tags with no history file (OCC, ORO, OVO, PAP, PRE, QEM, RAP, RIF, RIG, SAX, SHL, SID and 13 more), 5 WA buildings with no 3D entity (`steel_refinery`, `hydro_steel_refinery`, `aluminium_refinery`, `hydro_aluminium_refinery`, `dummy`), and one missing dynamic token (`theorist_cost_factor`). None was visible before; all were buried under 4,000 lines of scope-validation noise. | Each of the three families is fixed, or carries a line saying it is intentional |
| 6 | 11 error.log lines the triage tool cannot classify | Deliberately left UNKNOWN rather than guessed at: 8 `Subdoctrine <x> has no scripted rewards` (bomber_formations, dogfighting, search_patterns, target_acquisition, battlegroup_primacy, carrier_primacy, battlefleet_support, trade_interdiction), one loc-key collision pointing at `text.log`, one `No styles scripted`. Each needs a judgement about whether it is intentional. | Each of the 11 is classified in `tools/triage_error_log.py` with its reason |
| 7 | 25 of 43 items have not been scored in the last 3 campaigns | Now that the registry is complete and ordered, the dormancy figure is trustworthy for the first time: 25 of 43 riding along unprobed, longest gaps R9 / R17 / R24 / R28 at **6 campaigns** - not measured since `af003548` on 2026-08-15. Their statuses say so, but nothing schedules them. | A scoring pass covers the six-campaign gaps, or each records why it is not worth probing |
| 8 | R53 cannot be scored: its probe calls a tool that does not exist | The Probe line names `lines.py`, a scratchpad extractor from the 2026-08-15/16 local runs that was never added to the skill's scripts. That is why R53 is the one `NEVER-SCORED` item. The measurement IS reconstructible - `production/general_lines` in the save, 402 lines on 1940.6, spread distribution already visible - but owner attribution is unsolved, and leg 1 is stated per country. | `lines.py` exists under `.claude/skills/wa-savegame-analysis/scripts/` with owner attribution, or R53's probe is recut against a tool that does |
| 9 | The European war is won too slowly to finish - F5, recut | The original row said the front stops moving. **It does not:** GER's controller count peaks at 1944.6 and halves by 1946.5 (402 -> 210), with RUK 141 -> 38 and RBL 109 -> 41 in the final quarter alone. The Reich is collapsing; the ten-year run simply ends first, with Japan still at ~485 and untouched. The question is why the roll-back takes from mid-1944 to past 1946.5, not why it never starts. | The rate of Axis collapse is measured against a target (historical VE-day is 1945.5), and the reading names the constraint - production, supply, front width or invasion cadence |
| 10 | A verified way to count who controls what | The F5 measurement rests on counting bare `controller=` lines, whose denominator I could not establish: a state-block parse returned 12,440 blocks for a ~1,300-state map. The trajectory is consistent across six saves and usable as a relative signal, but no rate or share can be quoted from it, which is exactly what the recut F5 row needs. | `savegame.py` can report states-controlled-by-TAG with a count that matches a known figure (e.g. the state count of a country at 1936.1) |
| 11 | `put_unit_buffers` docs still claim same-`order_id` pooling | `WA_AI_MILITARY_SYSTEM.md` section 4 and `WA_AI_MILITARY_ECONOMY.md` E4b repeat the engine-doc wording that same `order_id` entries share one ratio pool, while the lessons log records direct save measurement showing separate orders and leaves `order_id` semantics unknown. The Italian-garrison change avoided the claim with independent ids, but the shared references still contradict the measured lesson. | Every `order_id` pooling claim in the two military docs is audited against the 2026-08-17 save measurement; refuted wording is removed or labelled ASSUMED, and lint rules no longer enforce an unmeasured pool model |
| 12 | 320 of 334 countries never get a WA infantry template | Measured on `2f8cbd51` 1946.7: only 14 tags hold `WA_INFANTRY_TEMPLATE`. `WA_AI_TEMPLATES_has_infantry_focus_completed` (`WA_AI_TEMPLATES_triggers.txt:185`) is a 22-entry hard-coded focus list with **no `generic.txt` and no `south_america_generic.txt` focus**, so every minor on a shared tree is excluded — ARG completed `SOUTH_AMERICA_army_modernization` and still got nothing. Without the flag the only enabled target sits in `WA_infantry_fallback` (`ai_templates/WA_AI_TEMPLATES_infantry.txt:26`) whose **role-level `upgrade_prio` is 0**, which the engine doc defines as "prevent the AI from spending XP on upgrading templates for this role"; `common/ai_templates` is `replace_path`'d so there is no vanilla fallback. Result: ARG/BRA/CHL/MEX/TUR/DEN/NOR/POR/YUG templates are byte-identical 1936.6 → 1946.7. | The shared-tree army focuses are in the trigger (or the exclusion is recorded as intentional), the two `role = infantry` entries no longer collide, and a campaign shows at least one generic-tree minor upgrading its infantry template |
| 20 | Germany's 2 700-hull convoy pile has no known source (the arsenal half is now Fix 115) | Two findings, one measurement (campaign `3d68a183`, 2026-08-20). (a) `WA_DEFAULT_production_convoy_stop_MAJORS` disables itself via `NOT = { WA_AI_PRODUCTION_should_build_convoy_relief = yes }` in **both** `enable` and `abort`, so an 81+ dockyard major with any convoy-starving partner is exempt from the >1 000 brake for as long as the partner is short — permanently, for GER/ITA. MEASURED: GER runs four convoy lines, three at the 15-dockyard cap, **46 → 59 dockyards = 6.0-7.6 % of every factory on its lines**, 1942.6 → 1944.6, holding ~2 700 convoys. The arsenal itself works (51 transfers, 2 201 hulls donated), so the answer is a ceiling, not a removal. (b) That stock is NOT what those dockyards built: it steps 981 → 2 711 between the 1942.9 and 1942.12 saves, and 46 dockyards at `value = 5` IC/day against a 700 IC convoy is ~120/year. Production, war booty (the creator is GER, so it is Germany's own make), trade-convoy release and returned loans were all considered; none is confirmed | **(a) CLOSED 2026-08-20 by Fix 115** — the arsenal is now gated on the coalition's weighted sea share, so a land coalition never opens it and its >1 000 brake arms normally; probe R79. **(b) still open:** the 1942.9 → 1942.12 step of +1 730 GER-built hulls is attributed to a named mechanism, or the row records that it is unattributable from a save |
| 14 | Commonwealth defensive handoff — implemented, not yet campaign-tested | CAN takes the safe British-isles guard, SAF and RAJ share El-Alamein, RAJ takes Kuwait; ENG keeps a floor and dynamically replaces any unavailable delegate. Code, documentation, pre-fix probe and static checks were complete when it was demoted on 2026-08-19; only the in-game measurement is missing. | A post-change campaign shows the delegate orders, each ENG fallback is exercised by making its delegate unavailable, ENG's buffered/armoured distribution improves, and Britain, Marsa Matruh and Kuwait remain held |
| 15 | JAP never opens a convoy line, and `is_starving_convoys` measures trade rather than scarcity | On `07270b64` JAP runs 0 `convoy_1` lines for the whole campaign while its free pool falls to 106 - checklist R56 leg 1 requires a line for any AI major under 500 convoys. Separately, every convoy gate reads `num_equipment@convoy` = the FREE pool, which any trading country holds at ~0 by construction, so 200-432 weeks of "famine" are recorded on nearly every belligerent including a GER sitting on 1,013 free hulls; the trigger cannot tell a real shortage from ordinary trade. | JAP opens a convoy line within 3 months of crossing the production bar in a campaign, and the starving/surplus gates read a quantity that separates assigned hulls from a genuine shortfall (or the checklist records why the free pool is the right reading) |
| 17 | FRA's priority-construction queue is deadlocked on projects queued before the 1940 armistice | MEASURED on `3d68a183` 1944.4: FRA holds 13 projects, **0 assigned factories**, 19 controlled civ levels, PC using 0.0%. Six railways in the `rail-war` band (prio 1000) target metropolitan states 1016 and 15, stalled **193–207 weeks**, 47 months in the queue; two air bases stalled 194–206 weeks. The stall sweep cannot evict them because it only fires when `assigned_factories_total > 0`. FRA is the controller of Algiers, Constantine and Batna, so this is also why nothing is built there by the host nation | The stall sweep can evict a project on a queue that has never been funded (or a project whose target state is no longer reachable/controlled is cancelled), and a campaign shows FRA's queue turning over after a capitulation |
| 21 | The `wa_events_test.txt` call-site anomaly has no cause, and every other console harness homed there is suspect | MEASURED 2026-08-20 (save 1940.5, GER): `WA_TEST_CA_report` fired from `wa_events_test.txt` read EVERY country-valued trigger false — literal `tag = GER` included — while value triggers read true; byte-identical effect fired from `wa_test_scope_isolation.txt` read correctly. Cause UNKNOWN after clearing BOM, braces, duplicate ids, namespace, engine and indirection (lessons log "Two call sites, one effect"). The convoy-arsenal harness was rehomed (`wa_ca`), but the railway suite, the pathfind probes (`wa_test.301/.302`) and every other event in that file may produce equally poisoned readings — one such reading already shipped Fix 118 on a false diagnosis | Either the anomaly is reproduced and named: bisect `wa_events_test.txt` in game (fire `WA_TEST_CA_report` from a copy of the file with half its events removed, halving until the poisoning definition is isolated), or the remaining console harnesses are rehomed to their own files with scope self-checks and the file's readings are declared unusable in a header — one of the two, recorded |
| 19 | `WA_AI_calculate_resource_need` is blind to a shortage of a resource you barely produce | MEASURED on `3d68a183` 1944.4: ENG runs unmet demand on 6 of 9 resources (aluminium −824, steel −1143, rubber −392, chromium −266, coal −2540, tungsten −121), holds `export_focus`, and **imports and re-exports the same scarce resources** (aluminium: imports 508, exports 160; chromium: imports 123, exports 96). Its `wa_ai_average_resource_need` reads **0.44945** against the 0.60 bar of `WA_AI_can_take_limited_exports`. Cause: `WA_AI_law_effects.txt:191` computes each term as `(consumed + deficit) / OWN production` and skips any resource produced at or below `constant:wa_ai_resource.trade_law.resource_floor` (75) — ENG produces rubber 29, chromium 27, tungsten 51, so the three worst shortages contribute nothing, and coal (11,084 produced) dilutes the rest. Independent of the R28 fix in `32c03c550`, which repairs different clauses | The need index counts a resource the country depends on but does not produce (or the floor's purpose is recorded), and a campaign shows a major with maxed `wa_ai_needs_*` counters crossing the 0.60 bar |


---

## DONE

| Date | Subject | Trace |
| --- | --- | --- |
| 2026-08-20 | **The convoy-arsenal harness anomaly: rehomed, recorded, and Fix 118 re-judged.** The harness read every country-valued trigger false when fired from `events/wa_events_test.txt` and correctly from a clean file — cause UNKNOWN (QUEUE 21 owns the audit of the other harnesses in that file). Done: harness rehomed to `events/wa_test_convoy_arsenal.txt` (`wa_ca.1/.2`) with its scope self-check kept and the hunt probes deleted; the `wa_iso` scope-isolation harness KEPT deliberately as a reusable clean call site (headers say so); lessons log entry "Two call sites, one effect"; Fix 118's false justification withdrawn at the code site, registry row 118 and checklist R79 corrected, the Fix 27 case restored as a guarded `is_subject` + `OVERLORD` fourth term (MEASURED-empty on `3d68a183`, reviewers OK/CONCERNS-addressed); R79 first VALID reading recorded (GER 1940.5, share 0.000, 0 of 706, ARSENAL 0 — land half of leg 1 as designed, maritime half still owed via `event wa_ca.2`); the ten probe commits of the hunt squashed out of the unpushed history | lessons-log "Two call sites, one effect", `events/wa_test_convoy_arsenal.txt`, `events/wa_test_scope_isolation.txt`, checklist R79, `tools/fix_registry.json` 117/118, QUEUE 21 |
| 2026-08-20 | **Lend-lease relief: all four closing criteria met, on campaign `3d68a183`.** The two fixes of QUEUE row 13 are confirmed at campaign scale, not just on a harness. Pathfinder: `WA_AI_supply_line_dbg_pf_ok` 444/444 on ENG and 11/11 on SOV, against 1 814 called / 0 ok before. Sends: `llr_send_failed_n = 0` on every donor read, 596 land transfers (GER 341, USA 169, SOV 86, ENG 0 - it is an island) and 272 convoy transfers carrying 9 342 hulls. Land-access rule: `path_refused_n = donor_selected_n = 243` on ENG, i.e. every land donor drawn for an island refused, while GER's continental clients pass 61 of 139. The fourth criterion needed a tool that did not exist and now does - `stock.py` reads the RECEIVING end by variant creator: **ENG holds 175 USA-built `convoy_1` and SOV 64**, and USA's own convoy stock ROSE 863 → 2 635 over the period it shipped 2 803 hulls, so nothing was sent out of a shortage. Scored into checklist R56 (FAILING → PASSING, 1 of 3) and R7b (UNSET → PASSING, 1 of 5). | `stock.py` + R56 / R7b history entries, campaign `3d68a183` |
| 2026-08-19 | **Les deux sens de « historique » sont desormais explicites et le cheat ITA/ETH suit le bon axe.** WA Historical AI Difficulty (`WA_AI_DIFFICULTY_is_historical`) preserve l'enchainement des evenements/campagnes; vanilla Historical AI Focuses (`is_historical_focus_on`) preserve le setup. Comme le vainqueur ITA/ETH affecte le setup, l'ajout et le `cancel` de l'aide utilisent le trigger vanilla. La regle et l'obligation de demander des exemples en cas d'ambiguite sont dans `AGENTS.md`, `wa-ai-systems` et le commentaire CONFIG. Skill, constantes, accolades, diff, revues et test a blanc: PASS. Pas de test en jeu. | `AGENTS.md`, `.claude/skills/wa-ai-systems/SKILL.md`, `common/scripted_triggers/WA_AI_CONFIG.txt`, `common/ideas/_WA_ai.txt`, `events/WA_AI_ITA.txt` |
| 2026-08-18 | **F5 re-measured: the front is not frozen, the war is just too slow.** GER's controller count peaks 1943.6-1944.6 (401/402) and halves to 210 by 1946.5; the puppets collapse with it (RUK 141 -> 38, RBL 109 -> 41 in the last quarter); Japan goes the other way, 347 -> 501 -> 484. The Allied bloc's own count stays nearly flat (95-158) and gains only USA +7 / SOV +6 while the Axis sheds 213, so the lost ground is not appearing as Allied gains. **Bounded honestly:** the census counts bare `controller=` lines and the denominator is unverified (a state parse returned 12,440 blocks for a ~1,300-state map), so it is a relative trajectory, not a state count - no rate quoted. Row recut into two: the slowness, and a verified control-counting probe | `checklist.md` F5 |
| 2026-08-18 | **Decision on the nine UNSYNCED-OK engine docs: hold one, leave eight loose, and say why.** Measured first - exactly **one of the nine is cited by anything**: `common/script_constants/documentation.md`, referenced by `wa-constants-registry/SKILL.md` and `wa-engine-reference/SKILL.md`, and it documents the `constant:` mechanism the whole constants registry rests on. Added to the manifest with its install sha (verified byte-identical first). The other eight - collections, doctrines/folders, map_modes, scientist_traits, three special_projects docs, strategic_locations - have **zero citations in the repo**; holding them would make the next game patch raise an ERROR demanding a re-read of documents nothing depends on. The rule is written at the `UNSYNCED-OK` row of the skill: add one the day something cites it | `tools/engine_docs_manifest.json`, `wa-engine-reference/SKILL.md` |
| 2026-08-18 | Two of the last three checker ERRORs cleared, the third recorded rather than faked. **ORPHAN-FIX:** R66 cited `a5501e28b`, a Fix 102 commit rebased off the branch - **its content is in the tree** (all 18 `coop_can_supply` lines verified present), so the item now cites the two live commits `ed29adbe5` / `2cf5f214f` and the orphan story moved OUT of the checked field into its own note, rather than weakening the rule. Registry row 102 records that it had **two claimants** and the generator picked the wrong one (`cb0be4b55` only mentions Fix 102 in its body). R53's ledger also stopped citing `340294482`, superseded the same day by `fe6d95612`. **NEVER-SCORED stands:** R53's probe calls a scratchpad tool that no longer exists | `checklist.md`, `tools/fix_registry.json` |
| 2026-08-18 | Campaign registry completed: `66636ff4`, `0edbc955` and `9d83084c` added - the last was scored **53 times** in item histories while absent from the table. `REGISTRY-MISSING` 3 -> 0, ERROR 5 -> 2. **And the fix was wrong the first time:** I appended all four new rows (these three plus `0bbc1f60`) at the TOP, but the table is read as the chronological campaign sequence, so the newest campaigns became the oldest and the checker reported R71 as **17 campaigns dormant on the day it was scored**. Caught by reading the tool's output instead of counting its errors. Rows moved to the bottom in analysed-date order, and the table now carries a note saying the order is load-bearing | `checklist.md` campaign registry |
| 2026-08-18 | **All 10 unresolvable fix numbers now say what they changed and where**, so a `Fix NN` in a comment resolves to something even when git cannot produce a commit. Harvested from the tree itself, not invented: six had `# Fix NN:` comments at their own code sites (11 port-upgrade, 21 ROOT-only rail networks, 22 coastal-access gate, 26 distance-limited port search across 14 sites, 69 T-43 hard-gate, 71 Canada joins the western front); three came from the railway system doc's own test cases (14 non-neighbour claims / TC-031, 19 peace clears the queue / TC-051, 23 pre-war pathfinding / TC-032); Fix 67 from the checklist item it is under test in (the 90-day invasion freeze stamped by `WA_AI_DIVISION_spawn_invasion`). They stay WARN `FIX-UNRESOLVED` on purpose - the commit really is gone | `tools/fix_registry.json` |
| 2026-08-18 | **Every checklist item now carries a status; `STATUS-UNSET` is 0.** The 11 were two different defects: nine had no `- **Status:**` line at all, and R28/R29 wrote `- **Status: DIAGNOSED, NOT FIXED.**` with the colon INSIDE the bold, which `field()` cannot read - so the two items that say plainly they are diagnosed-not-fixed were invisible to the checker. Statuses **transcribed from each item's own History**, naming the date and campaign of its last scored verdict, not derived from streak arithmetic (that was built and rejected on 2026-08-18 for mislabelling every item with a pre-fix baseline row). Five of the nine also record "not probed since", because their last one to three passes were NOT CHECKED | `checklist.md` (11 items) |
| 2026-08-18 | `tools/triage_error_log.py`: classifies a HOI4 error.log into ACTIONABLE / UNKNOWN / BENIGN, and an unrecognised shape is reported as UNKNOWN, never assumed benign. Two-sided controls: on the pre-fix 2.3 MB log it surfaces both defects found by hand today - `toolpack.txt:10` (3,531) and `common/characters/_documentation.txt` (117 lines collapsed to ONE row naming the file) - while parking 4,010 scope-validation lines as noise; on the live clean boot it reports zero of either. The UNKNOWN bucket earned its keep three times: a second parse-failure wording the rules missed, two more real families (missing history file, missing entity), and two Workshop descriptors being called ours until `other-mod` was moved above `parse-failure` | `tools/triage_error_log.py` |
| 2026-08-18 | `toolpack` log flood: the cause is a `visible = {{ ROOT = {{ ... }} }}` wrapper on the window container. **Positive control inside the same file:** 4 of toolpack.txt's 5 `visible` blocks are already bare and silent; the one the log names at line 10 is the only wrapped one. Across the mod, 11 other scripted GUIs (39 `visible` blocks, all `player_context`) write the triggers bare and none appears in the log. Removed the wrapper from all **8** window containers (9th was a second one in `toolpack_bst.txt`), brace balance and trigger text verified unchanged per file. **Not yet confirmed** - every boot since has had the tool off - so the confirmation is queued with its baseline | 8x `common/scripted_guis/toolpack*.txt` |
| 2026-08-18 | **Settled: nothing in a savegame can verify the faction-theatre remap**, so §15 now says so instead of carrying a falsifier that cannot run. Measured on `0bbc1f60`: the save's 98 `theatre` structures are army-side operational areas (median 31 provinces, max 140, each carrying its `orders_group`s) where a faction theatre spans hundreds; its 133 `theater_group` names are continents and "<Nationality> Theater N", none matching an `ai_faction_theater_id`; and the theatre ids themselves have zero occurrences. What is verifiable: the boot log is silent on `ai_faction_theaters` (the file parses, its region ids resolve), and correctness is a build-time property the generator already checks with connectivity + anchor + a positive control against vanilla's own 30 theatres | `WA_AI_MILITARY_SYSTEM.md` §15 |
| 2026-08-18 | **Decision: `naval_invasion_support_priority` is not adopted**, and "WA replaced it with nothing" was too harsh. WA sequences invasion theatres on a different axis: 76 `invade` entries in `FACTION_ALLIES_INVASION` across 12 blocks, 7 of them date-gated, as explicit hold/fire pairs (Norway, D-Day, no-Italy-early, North-Africa-first); mod-wide 351 `invade` / 46 `invasion_unit_request` / 6 `naval_invasion_focus`. The type keys on the **sea region a route crosses**, `invade` on the **target country** - route-keyed says "nothing in the Pacific until the Med is done" in one entry, target-keyed says it by naming every country, which is what WA's 50 `-2000` entries do. Enumeration works, so adopting would add a mechanism where nothing is broken (Fix 77 lesson). Recorded with the revisit condition | `WA_AI_MILITARY_TYPES_REFERENCE.md` |
| 2026-08-18 | `front_control` priority-vs-ownership collision **audited, and the fix refused**. Surface is 10 of 81 Country blocks, not 81. **Four of my ten rows were wrong** and both reviewers caught them: `CZE_help_france` carries `execute_order = yes`, two of the four CHI blocks are not holds, and SPR cannot co-apply at all (3-day 1936 flag vs an Axis-membership gate). Of the three real collisions, re-tiering to 400 was returned CONFLICT by both: it is the re-tiering §6.1 forbids - proposed by the session that wrote the prohibition - GER's gate is monotonic for a losing Germany and would become a permanent Eastern-Front veto (the `911bed3c` shape), and FRA's releases only when the civil war ends. Nothing shipped; §6.1.1 now records the corrected table, the refusal and its reasons | `WA_AI_MILITARY_SYSTEM.md` §6.1.1 |
| 2026-08-18 | `has_deployed_air_force_size` says **aircraft**, not wings. The engine's own wording is "checks for amount of aircrafts" (install `triggers_documentation.md`); the Reich bombing-ladder thresholds 299/450/700/900 were labelled "deployed strategic_bomber wings". The NUMBERS are unaffected - they were calibrated by reading this same trigger on campaign `973154a7`, so they were always plane counts - but the word is how two `*_MIN_EXCORT_WINGS` defines sat dead for years. Swept all 9 remaining `wing` comments near air thresholds: one more wrong (`WA_TEST_air_actors.txt`, "counts carrier wings too"), three correct and left alone (a real wing-to-plane conversion, a measured wings/planes ratio, a mission-carrying wing) | `WA_AI_MILITARY_triggers.txt`, `WA_TEST_air_actors.txt` |
| 2026-08-18 | Engine-doc refresh **read**, not just run. `check_engine_docs.py` is at 0 ERROR / 0 WARN. Swept the whole tree for every token the refresh reported renamed or deprecated: three live hits, and **the report itself was wrong on two of them** - the install marks exactly ONE token `[deprecated]` (`on_ruling_party_change_immediate`, which WA does not use), while `on_before_peace_conference_start` and `on_deployed_leader_defeated` are live tokens WA calls correctly. Skill corrected. Third hit real: the MIO stat catalogue carried `naval_supremacy_factor -DOESNT WORK`; the engine renamed it `naval_dominance_factor`, which vanilla's own organization files list | `wa-engine-reference/SKILL.md`, `common/military_industrial_organization/organizations/00_generic_organization.txt` |
| 2026-08-18 | `check_worklist.py --selftest`: a clean fixture tree that must report nothing, one mutation per rule that must make it fire, and the clause that a rule without a fixture fails the run. Written because three rules shipped inert the same day and none was caught by reading the diff. **It immediately found a fourth**: `SUPERSEDED`'s pattern ended in ``, so "superseded by R2" could never match and only the `merged into` half was working. **And a fifth, in itself**: it loaded the checker through `__pycache__`, so a stale .pyc had it testing an older copy and reporting a rule as firing after that rule was broken - it now compiles the file's text | `tools/check_worklist_selftest.py`, `tools/check_worklist.py`, `AGENTS.md` |
| 2026-08-18 | Fix numbering: `tools/fix_registry.json` created - **91 numbers, one row each**, derived from every commit message on every ref with range notation expanded (`Fixes 46-50`), plus `FIX-UNREGISTERED` / `FIX-ORPHAN` / `FIX-UNRESOLVED` in the checker. **Measured: zero fix numbers point at a commit HEAD cannot reach** - the rebase damage the row was opened for is already repaired. What is real is three checklist items that shipped with a literal `<FILL AT COMMIT>` placeholder (Fixes 67, 71, 75); 75 recovered, 67 and 71 marked UNRECOVERABLE. **The new check shipped dead** - a `` became a backspace byte, it matched nothing across 2,346 files and reported clean; an invented Fix 999 caught it | `tools/fix_registry.json`, `tools/check_worklist.py`, `checklist.md`, `AGENTS.md` |
| 2026-08-18 | **Decision: structure the ledger, keep the criteria in prose** - measured first: only **4 of 43** items have a probe a tool could run unaided, 28 need judgement or several tools, so structuring criteria buys ~9% and risks simplifying a leg to fit a schema. Every item now carries `- **Ledger:** class/threshold/streak/fix/status`, cross-checked against the prose (`LEDGER-MISMATCH`). Status is **copied, never derived** - deriving it was built, tested and rejected the same session. Two defects fell out: **R71 had been filed after the retirement ledger**, outside the RETIREABLE section and outside every check that split on it, and that split was positional twice over - now structural | `checklist.md` (52 items + R71 moved), `tools/check_worklist.py`, `wa-campaign-checklist/SKILL.md` |
| 2026-08-18 | Unmeasurable pass criteria. Swept all **128 pass legs** of the 43 live items: exactly one compares against a value that lives nowhere - R60 leg 4, "stay in the envelope of the previous campaign", which scored NOT TESTED on `7c7803a8` AND `9d83084c` and could never have scored anything else. Recut **self-seeding**: six named numbers recorded in the item's own History every scoring, PASS within ±30% of the last entry that carries them, first scoring NOT SCOREABLE by construction. New `CRITERION-UNSCOREABLE` check; its first draft asked "is there a digit in the leg", which `type-13` satisfies - it now requires the number within 200 chars of the comparison | `checklist.md` R60, `tools/check_worklist.py`, `wa-campaign-checklist/SKILL.md` |
| 2026-08-18 | Probes are now validated **at creation**: `PROBE-UNRUN` fails any item opened from 2026-08-19 that carries no fenced block holding the probe's actual output, and the skill says to run it against the pre-fix baseline - whose output is the FAILED baseline the item needs anyway. Older items grandfathered (their baseline saves are not all still on disk). Found and fixed on the way: `field()` required a bare `- **Probe:**`, so R57's qualified label read as **no probe at all** - a false NO-PROBE on the one item that documents why it has no WA_TLM metric | `tools/check_worklist.py`, `wa-campaign-checklist/SKILL.md`, `AGENTS.md` |
| 2026-08-18 | Telemetry debt cleared: `WA_TLM_r74_ally_rail_*` and `WA_TLM_r97_ea_*` deleted with their retired items R46 / R62 (write sites, init lines, registry rows; no version bump, §3.7). **The checker had been passing them on their own obituary** - the retirement ledger at the foot of the checklist names the item and its fix, so a whole-file search always found the token; and a bare `fix NN` anywhere in prose satisfied the fallback. Now: ledger cut off before searching (last heading occurrence - the table of contents names it too), token-only consumer test, and comments no longer count as write sites | `WA_TLM_core.txt`, `WA_AI_CONSTRUCTION_PRIORITY_railway_strategies.txt`, `WA_TLM_TELEMETRY_SYSTEM.md`, `tools/check_worklist.py`, `checklist.md`, `AGENTS.md`, `WA_AI_MILITARY_SYSTEM.md` |
| 2026-08-18 | error.log triage after the boot test. **One regression, ours, from the same day:** the engine-doc sync wrote markdown prose into `common/characters/_documentation.txt`, a `.txt` in a replace_path folder HOI4 parses - 105 `unexpected token` errors per boot, where the 13-line all-comment stub it replaced had produced none. Renamed to `.md`, guarded by a new `PARSED-DOC` check. Nothing in the log names anything else changed that day; the two runaway runtime signatures are queued | `common/characters/_documentation.md`, `tools/check_engine_docs.py`, `tools/engine_docs_manifest.json`, `wa-engine-reference/SKILL.md` |
| 2026-08-18 | `common/ai_faction_theaters` taken over by WA. Vanilla's file was live against WA's re-cut region map - 74 of the 223 ids its theatres name cover completely different ground (vanilla 239 = Alborz, WA 239 = Northern France), so 4 of WA's 6 `theatre_distribution_demand_increase` writers were inert or aimed at the wrong theatre. Generated from vanilla's theatres by province overlap, keeping `ai_will_do` / `cancel` verbatim; connectivity measured on the bitmap, positive-controlled against vanilla's own 30 theatres | `tools/gen_ai_faction_theaters.py`, `common/ai_faction_theaters/`, `descriptor.mod` (**gitignored - not committed**), `WA_AI_MILITARY_SYSTEM.md` §15, `AGENTS.md`, `wa-engine-reference/SKILL.md` |
| 2026-08-18 | The three "phantom" `ai_strategy` types: `force_ratio` and `infantry` removed from both docs and 35 file headers (every `type = infantry` is a `divisions_in_state` trigger hit); `naval_invasion_support_priority` **kept - the audit was wrong**, the token is in `hoi4.exe` and vanilla uses it 7x. Binary-grep-with-a-control added to the engine-reference skill | `WA_AI_MILITARY_SYSTEM.md`, `WA_AI_MILITARY_TYPES_REFERENCE.md`, 35x `common/ai_strategy/*_FRONT.txt` headers, `wa-engine-reference/SKILL.md`, `type-audit-2026-08-18.md` |
| 2026-08-18 | The `ai_strategy` type audit: 17 of 19 summary rows closed. §4's "Engine combination" column was inference for all but one pair - renamed and every cell tagged **E** / **I**. Real corrections: `theatre_distribution_demand_increase` is an absolute division count (doc said 0-500), `put_unit_buffers` `ratio` is a fraction of the whole army and same-`order_id` blocks SHARE it, `naval_dominance` sets rather than sums, `garrison`'s -5000 override is convention not engine text, `front_unit_request` has no documented base of 100, `dont_defend_ally_borders` "highest value wins" withdrawn. C6/C7 routed to their own row | `WA_AI_MILITARY_SYSTEM.md`, `WA_AI_MILITARY_TYPES_REFERENCE.md`, `type-audit-2026-08-18.md` |
| 2026-08-18 | `front_control` / Phase 5: the premise was wrong twice - Phase 5 shipped in `d149a204b` (50 slugs, 0 orphans) and the engine's `priority` covers only 7 of them; the other 43 are types with no `priority` field. `priority` is already in use on a brake > posture > routine ladder, not a layer ladder | `WA_AI_MILITARY_SYSTEM.md` §6 (rewritten, cites the engine section), `WA_AI_MILITARY_TYPES_REFERENCE.md`, `type-audit-2026-08-18.md` C2 + X1 |
| 2026-08-18 | 19 dead `05_defines.lua` writes: 17 deleted, 2 renamed to their live engine key with a new value, 1 given its missing `NDefines.NAir.` prefix. Logistics doc was wrong by 15x on `SUPPLY_FROM_DAMAGED_INFRA` | `common/defines/05_defines.lua`, `documentation/WA_AI_LOGISTICS_MODEL.md`, `documentation/WA_AI_MILITARY_SYSTEM.md`, `tools/constants_registry.json`, `lessons-log.md` |
| 2026-08-18 | `naval_dominance` out of scale: the 8 blocks at 1000 were a self-labelled probe (`289f1318f`) whose question R36 already answered - retired; region 356 moved to the Faction south corridor at 70 with its avoid pair | `WA_AI_NAVAL_COUNTRY_ENG.txt`, `WA_AI_NAVAL_FACTION_ALLIES.txt` |
| 2026-08-18 | Work queue + checklist decay checker (one ACTIVE, orphan fixes, stale statuses, dormancy) | `QUEUE.md`, `tools/check_worklist.py` |
| 2026-08-18 | Diagnosis protocol — six boxes; a missing script line makes the report `INCOMPLETE DIAGNOSIS` | `.claude/skills/wa-diagnosis/` |
| 2026-08-18 | Engine doc `ai_strategy` refreshed 2023-07 → 2024-11 (+291 lines), 20 citations moved to section names, drift checker | `tools/check_engine_docs.py` |
| 2026-08-18 | Engine reference: game install + Expert AI + wiki, with the "read before you assert" rule | `.claude/skills/wa-engine-reference/` |
| 2026-08-18 | `savegame.py`: 7 tooling defects that made wrong readings silent | `savegame.py`, `plans.py`, `airload.py`, `regions.py` |
| 2026-08-18 | Communication rule: answer first, MEASURED / DERIVED / ASSUMED labels | `AGENTS.md` § Talking to the user |
