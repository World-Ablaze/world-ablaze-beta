scripted_gui = {
    fra_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "fra_infantry_folder"

        visible = {
            OR = {
				original_tag = FRA
				original_tag = VIC
				original_tag = GRE
				has_country_flag = french_technologies_tree_flag
			}
			NOT = { original_tag = CZE }
			NOT = { original_tag = HUN }
			NOT = { has_country_flag = czechoslovakia_technologies_tree_flag }
			NOT = { has_country_flag = hungarian_technologies_tree_flag }
        }
    }
	jap_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "jap_infantry_folder"

        visible = {
            OR = {
				original_tag = JAP
				has_country_flag = japanese_technologies_tree_flag
			}
			NOT = { original_tag = CZE }
			NOT = { original_tag = HUN }
			NOT = { has_country_flag = czechoslovakia_technologies_tree_flag }
			NOT = { has_country_flag = hungarian_technologies_tree_flag }
        }
    }
    ita_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "ita_infantry_folder"

        visible = {
            OR = {
				original_tag = ITA
				original_tag = RIT
				has_country_flag = italian_technologies_tree_flag
			}
			NOT = { original_tag = CZE }
			NOT = { original_tag = HUN }
			NOT = { has_country_flag = czechoslovakia_technologies_tree_flag }
			NOT = { has_country_flag = hungarian_technologies_tree_flag }
        }
    }
    ger_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "ger_infantry_folder"

        visible = {
            OR = {
				original_tag = GER
				has_country_flag = german_technologies_tree_flag
			}
			NOT = { original_tag = CZE }
			NOT = { original_tag = HUN }
			NOT = { has_country_flag = czechoslovakia_technologies_tree_flag }
			NOT = { has_country_flag = hungarian_technologies_tree_flag }
        }
    }
    sov_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "sov_infantry_folder"

        visible = {
            OR = {
				original_tag = SOV
				original_tag = STA
				has_country_flag = soviet_technologies_tree_flag
			}
			NOT = { original_tag = CZE }
			NOT = { original_tag = HUN }
			NOT = { has_country_flag = czechoslovakia_technologies_tree_flag }
			NOT = { has_country_flag = hungarian_technologies_tree_flag }
        }
    }
    eng_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "eng_infantry_folder"

        visible = {
            OR = {
				original_tag = ENG
				has_country_flag = british_technologies_tree_flag
			}
			NOT = { original_tag = CZE }
			NOT = { original_tag = HUN }
			NOT = { has_country_flag = czechoslovakia_technologies_tree_flag }
			NOT = { has_country_flag = hungarian_technologies_tree_flag }
        }
    }
    usa_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "eng_infantry_folder"

        visible = {
            OR = {
				original_tag = USA
				original_tag = CSA
				original_tag = USB
				has_country_flag = unitedstates_technologies_tree_flag
			}
			NOT = { original_tag = CZE }
			NOT = { original_tag = HUN }
			NOT = { has_country_flag = czechoslovakia_technologies_tree_flag }
			NOT = { has_country_flag = hungarian_technologies_tree_flag }
        }
    }
    minor_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "minor_infantry_folder"

        visible = {
            OR = {
				original_tag = SWE
				original_tag = POL
				has_country_flag = polish_technologies_tree_flag
				original_tag = HUN
				has_country_flag = hungarian_technologies_tree_flag
				original_tag = FIN
				has_country_flag = finnish_technologies_tree_flag
			}
        }
    }
    cze_inf_folder_gui = {
		context_type = player_context
		parent_window_token  = technology_tab
        window_name = "cze_infantry_folder"

        visible = {
            OR = {
				original_tag = CZE
				has_country_flag = czechoslovakia_technologies_tree_flag
			}
        }
    }
}
