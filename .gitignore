.vscode/settings.json
descriptor.mod
Descriptor.mod

# Per-user Claude Code config stays local; .claude/skills/ is versioned.
.claude/settings.local.json

*.pyc
# Windows reserved device names (cannot exist as files)
nul
**/nul
# Python caches and tool run logs
__pycache__/
.pytest_cache/
tools/**/*.log

# Campaign report: retain the generator, never the cache or generated campaign data.
/.cache/campaign_report/
/tools/campaign_report/output/
