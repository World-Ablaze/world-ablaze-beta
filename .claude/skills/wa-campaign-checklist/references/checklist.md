# Campaign verification checklist - living data file

Scored at every analysed campaign (`wa-campaign-checklist` SKILL). Since 2026-08-23 this file
holds only the FUNDAMENTAL items (F2, F5, F6, F8, F9 since the 2026-08-27 pruning — F1/F3/F4/F7
retired, git holds them), the campaign registry, and the cosmetic-anomaly list. Per-fix verification lives in each subject's *Verification* lines in `WORK.md`;
the retired per-fix items are frozen in `documentation/archive/CHECKLIST_R_ARCHIVE.md`.

## Campaign registry (analysed to date)

| game_unique_id | Machine / DLC | Scope | Analysed |
| --- | --- | --- | --- |

> **The order of this table is chronological and load-bearing.** `check_worklist.py` reads it as the
> campaign sequence and computes every dormancy figure from the tail, so a row appended at the TOP makes
> a brand-new campaign the oldest one - on 2026-08-18 that briefly reported R71 as 17 campaigns dormant
> on the day it was scored. **Append new campaigns at the bottom.**

**Save-directory hazard (2026-08-11):** the cloud runs reuse the same filename scheme (`YYYY.M_Mon.hoi4`), so `911bed3c` **overwrote 123 of `31eaf7e6`'s files in place**. Only `1946.5_May.hoi4` and `1946.6_Jun.hoi4` still belong to `31eaf7e6`, and their in-game dates are *later* than `911bed3c`'s final save — globbing by filename/date silently splices two campaigns into a fake 125-save run. Always group by `game_unique_id`.

**DLC note (supersedes the old `dlcs=30` assumption):** the cloud box now ships `dlcs=257535` — La Résistance and Arms Against Tyranny both behaviourally confirmed in `66d6b53c` (LaR SCW flags, AAT MIO lines). Always read the save header's `dlcs=` per campaign instead of assuming by machine.

---
| `9d83084c` | cloud | BHU observer, **121 monthly saves 1936.2-1946.2**, unbranched (session strictly monotone with date). HOI4 1.19.2 `Operation Postern a729`, `save_version = 33`, ironman 0, difficulty normal, `wa_tlm_version = 26`; build through `e447c3e47` (Fix 106). The most-scored campaign in this file - **53 item mentions** | 2026-08-17 |
| `0bbc1f60` | cloud | BHU observer, 124 monthly saves 1936.2-1946.5, HOI4 1.19.2 (`Operation Postern v1.19.2.0.a729`); build = commit `442823e61`, i.e. the Fix 96-107 stack plus the 2026-08-18 `ai_faction_theaters` takeover. `wa_tlm_version = 27` in-save. | 2026-08-18 |
| `2f8cbd51` | cloud, `dlcs=257535` | BHU observer, **126** monthly saves 1936.2-1946.7, no missing/duplicate month, unbranched (125/125 session transitions strictly increasing). HOI4 1.19.2 `Operation Postern v1.19.2.0.a729 (2dd7)`, `save_version = 33`; single mod `world-ablaze-beta`; build best fingerprint `9a30aa6e9` (2m57 before first save), independently bounded at Fix 107 by `wa_tlm_version = 27` first and last. Scoped scoring: F9 and R56 only; every other item NOT CHECKED. | 2026-08-19 |
| `07270b64` | cloud, `dlcs=257535`, `historical` per header | BHU observer, **119** monthly saves 1936.2-**1945.12**, unbranched (sessions 31->4437 strictly monotone, one save/month written 09:17-10:31). HOI4 1.19.2 `Operation Postern v1.19.2.0.a729 (1137)`, `save_version = 33`; single mod `world-ablaze-beta`; **build = HEAD `f1e0b9086`** (Commonwealth handoff `2cdfce1e0` 09:07 + infantry-template `7b54880a8` 09:01 + heavy-garrison `a60d5a976` + convoy relief `62ea4ee49` + parse fix `f1e0b9086` 09:15, first save 09:17) — first post-change campaign for all of them. Scoped scoring driven by seven owner questions: **F1-F5, F8, F9, R56, R72 scored**; the run ENDS at 1945.12 with the European war still open (F5 truncated, not stalemate). | 2026-08-19 |
| `3d68a183` | cloud, BHU observer | 121 monthly saves 1936.1-1946.1, `wa_tlm_version = 28` | 2026-08-20 |
| `8f9b5653` | cloud, `dlcs=257535` (LaR+AAT) | BHU observer, **117** monthly saves 1936.2-**1945.10**, HOI4 1.19.2 `Operation Postern v1.19.2.0.a729 (500e)`, `save_version = 33`, ironman 0, difficulty normal, historical, unbranched (sessions 38->4383 monotone); single mod `world-ablaze-beta`; **build = HEAD `ed109de9d`** (HEAD committed 20:28, first save 20:32), `wa_tlm_version = 31` first and last save, `wa_tlm_resv_*` live. Scored: F1-F5, F7, F8 plus all four OPEN subjects. Run ENDS 1945.10 with the European war open (F5 truncated). | 2026-08-25 |
| `24933fb9` | cloud, `dlcs=257535` (LaR+AAT) | BHU observer, **118** monthly saves 1936.2-**1945.11**, HOI4 1.19.2 `Operation Postern v1.19.2.0.a729 (e644)`, `save_version=33`, ironman 0, unbranched (sessions 28->4358 monotone); single mod `world-ablaze-beta`; **build = HEAD `2f16ec583`** — DERIVED from push 03:46 / run 04:05-05:18 local, `wa_tlm_version = 31` (no bump in the batch), and two behavioural fingerprints MEASURED in-save: CAN deploys 10 `Reserve Divisíon` at 1939.11 (`eb7338da4`) and ITA's AOI buffer occupies the widened 5-state set {550,559,271,909,910} (`4044dea78`). Scored: F1-F9, the four owner symptoms (El Alamein/Torch, CAN, ITS/AOI, Tunisia), and every recently-shipped subject. Run ENDS 1945.11 with the European war open (F5 truncated, mid-collapse). | 2026-08-27 |
| `1ac7e4ea` | cloud, `dlcs=257535` (LaR+AAT) | BHU observer, **120** monthly saves 1936.2-**1946.1**, HOI4 1.19.2 `Operation Postern v1.19.2.0.a729 (03d9)`, ironman 0, unbranched (sessions 37→4357 monotone); single mod `world-ablaze-beta`; **build = HEAD `cd234cc51`** (commit 13:49:54, first save 13:52; `wa_tlm_version = 32` first and last save, `wa_tlm_llr_recv_*` arrays live = the v32 fingerprint). Scored: F1-F9, all four OPEN subjects, and the PARKED probes (aoi, minor-fitness, total-commitment, silos, trucks, prospecting, rk, aifc-closure-eth). Run ENDS 1946.1 with the European war open (F5 FAILED, no collapse trajectory). | 2026-08-27 |

## FUNDAMENTAL

> **Pruned 2026-08-27 (owner order).** F1 (WW2 start, streak 18), F3
> (Barbarossa, 16), F4 (Pearl Harbor, 17) and F7 (SCW, 13) retired — they always pass; git
> holds their sections and history. Registry rows before `9d83084c` and F-item history
> entries before 2026-08-19 pruned the same day — git is the archive.

### F2. France falls on time
- **Ledger:** `class=FUNDAMENTAL threshold=none streak=3 fix=none status=PASSING`

- **Pass:** France capitulates ~1940.6 (±3 months).
- **Probe:** global flags / FRA country block capitulation state; VP control of Paris.
- **Streak:** 3
- **History:**
  - 2026-08-27 · `1ac7e4ea` · **PASSED** — `fall_of_paris_ger` **1940.6.15**, `fall_of_france` = `the_fall_of_france_global_flag` **1940.6.23**, `FRA_already_surrendered` 1940.6.24 — historical to the week. **But France is never fully occupied afterwards**: FRA keeps 12–19 metropolitan states through the whole war (`control owner:FRA` 1941.6 GER 14/FRA 12 → 1943.6 FRA 39/GER 14), so a continental French front runs continuously. `liberation_of_paris` **1944.6.19.2**, then Paris is **RE-LOST**: state 16 reads GER 8/8 provinces at 1946.1, 19 months later. Fourth campaign where a liberated Paris does not stay French.
  - 2026-08-27 · `24933fb9` · **PASSED** — `fall_of_paris_ger` **1940.6.19.1**, `fall_of_france` = `achievement_france_surrender` **1940.6.30.1**, `FRA_already_surrendered` 1940.7.1 — historical to the week. **Paris is never liberated**: `liberation_of_paris` absent from the 85-flag global block, state 16 GER 8/8 provinces at 1945.11; FRA's 40 controlled states are largely colonial (267 provinces vs GER 134 on FRA-owned ground). See F5 — `d_day_happened` 1944.6.8.11 fired and produced no breakout in 17 months.
  - 2026-08-25 · `8f9b5653` · **PASSED** — `fall_of_paris_ger` **1940.6.18**, `fall_of_france` = `achievement_france_surrender` **1940.6.30**, `FRA_already_surrendered` 1940.7.1: historical to the week, and a recovery from `07270b64`'s 1941.10 failure. **Paris is never liberated**: `liberation_of_paris` is absent from the global-flag block, and at 1945.10 state 16 (Île-de-France) reads **GER on 8 of 8 provinces**; FRA's 39 controlled states are colonial (`control owner:FRA` — FRA 263 provinces / GER 172). See F5 — `d_day_happened` **1944.6.9.3** fired and produced no breakout in the 16 months that follow.
  - 2026-08-19 · `07270b64` · **FAILED — the worst F2 on record by 13 months.** `fall_of_france` = `achievement_france_surrender` = `the_fall_of_france_global_flag` all **1941.10.13.1–2**, `fall_of_paris_ger` + `FRA_already_surrendered` **1941.10.14.1**; Vichy (VIC) on the ground by 1941.11.1; FRA army collapses 105 → 12 in the month. Paris had been fully German since between 1941.9.1 and 1941.10.1. The Battle of France was a **13-month attritional front** (1940.9 → 1941.10): FRA's army *grew* 118 → 133 divisions through 1941.6, GER committed only piecemeal, and **GER–SOV stayed at peace until 1942.2.12** so Germany never stripped the east — while ENG held a 9–10-division static buffer in Nord-Pas-de-Calais through the German push (1940.3–1940.8), surged to 33–38 front divisions only when Italy declared (1940.9.3), and **pulled out between 1940.11.1 and 1940.12.1** (new East-Africa/Egypt army-group orders stamped 1940.11.25–27, ~35 divisions relocated at constant total 72), eleven months *before* the capitulation. The Commonwealth delegates contributed **zero** divisions to France all period (CAN fielded exactly 1 division worldwide — **its 13 other divisions were DISBANDED at 1939.11, not lent**: zero `expeditionary_owner="CAN"` in ENG's units at any of 1939.10/11/12 + 1940.3/6, `expeditionaries_sent` block absent, `expeditionary_force_data` absent, 4 militia in training ≠ 14; positive control finds 8 other expeditionaries in the same 1940.6 save, so the extraction path works). Owner hypothesis "the Commonwealth handoff freed ENG to reinforce France" is **refuted on this campaign's numbers**: the handoff cannot have moved anything in 1940 — its gates need CAN > 11 divisions, unmet until 1941.12.

### F5. Germany loses WW2 and the European war ends
- **Ledger:** `class=FUNDAMENTAL threshold=none streak=0 fix=none status=FAILING`

- **Pass:** Germany is defeated and the European war terminates (white-peace chain or capitulation). Historical target ~1945.5; ending at all is the invariant, ending on time is the aspiration.
- **Probe:** global flags for the German white-peace chain; GER country state in late saves.
- **Streak:** 0
- **History:**
  - 2026-08-27 · `1ac7e4ea` · **FAILED — the war does not end AND no collapse is under way: the previous campaign's trajectory did not reproduce.** At 1946.1 GER controls **51 of its own 53 states (410/436 provinces)**, nobody stands on German soil, GER holds Paris (state 16, 8/8), 16 FRA-owned states, 22 ENG-owned states, 5 SOV states + RBL 18 / RUK 9; divisions **241 (1941.6) → 307 (1946.1) monotone growth**; no white-peace/capitulation/berlin flag; Axis 20 members, 872 live `war_relation` records. The West is *partially* alive this run — `operation_torch` 1942.10.1.4, `d_day_happened` **1944.6.9.12**, Normandy front orders created 1944.6.12-13 (USA AG5 49 div), GER's share of FRA-owned states falls 15 → 9 over 1944.1→1944.7, North Africa is French ground at 1945.6 (32/36 provinces on 458/1061/459/460) — but the front never crosses the Rhine, and Paris is German again by 1946.1. Seventh campaign without a German defeat; unlike `24933fb9`, GER is stronger at the last save than on D-Day.
  - 2026-08-27 · `24933fb9` · **FAILED — truncated at 1945.11 with the war open, sixth campaign without a German defeat — but the FIRST of the six with a real collapse under way.** No white-peace / capitulation / `fall_of_berlin_sov` / `hitler_has_died` / `ww2_has_ended` flag anywhere; GER uncapitulated, Axis leader, 30 wars open, Berlin (64) 6/6 GER. The trajectory is new: GER controlled states **115 (1943.6) → 114 (1944.6) → 104 (1945.6) → 72 (1945.11)** (−31 % in the last five months), army 285 → 218, fill 0.991 → **0.710**, SOV on German-owned soil (5 states / 36 provinces, FRA on 11 more), **HUN and ROM flipped to Comintern as Soviet subjects**, BUL coup 1945.8.14. The West contributes almost nothing: `d_day_happened` 1944.6.8.11 fired, Paris still German 17 months later; USA flat at 100 controlled states since 1943.6. Mediterranean inverted: `fall_of_cairo_ita` 1943.4.17, `SUEZ_CANAL_BLOCKED` 1943.7.5, `fall_of_cairo` (guarded write, `news.109`) **1945.8.1 = GERMANY takes Cairo**. DERIVED: a 1946 termination is plausible on the 1945.6→11 rate — the same "ran out of saves" shape as `a8b29e09`, won from the East alone.
  - 2026-08-25 · `8f9b5653` · **FAILED — truncated at 1945.10 with the European war fully open; fifth campaign running without a German defeat, and the worst German end-state of the five.** No capitulation / white-peace / surrender flag exists anywhere: the 80-flag global block carries none, and a GER country-flag match on `capitul|white_peace|surrender|defeat` returns NO MATCH over 123 flags. GER at 1945.10: **53 owned states, 53/53 controlled, 436/436 provinces — the homeland is untouched, not merely unconquered**, 11 subjects alive, 37 wars open (ENG since 1939.9.1, SOV 1941.6.22, USA 1941.12.5). Its army is still **growing** at the truncation point: 283 (1944.6) → 320 (1945.4) → **321** (1945.10; `wa_tlm_comp_div_total` 322). What distinguishes this from `07270b64` is that **every upstream fundamental PASSED** — war on time, France on time, Barbarossa on the hard date, Pearl Harbor exact — and the arc still did not convert: `d_day_happened` **1944.6.9.3** fired on schedule and 16 months later Paris is German and no Allied soldier stands in Germany. `operation_torch` 1942.10.1 and `GER_case_anton` 1942.11.16 both fired normally, so the Mediterranean arc ran; it is the north-west European breakout that never happened. Cross-reads: ENG fields **34 → 35 → 44 → 41 divisions across the whole war** (F8 anomaly), the only Allied major that never scales; `fall_of_cairo` **1941.1.1.1** and `SUEZ_CANAL_BLOCKED` 1942.10.9 record an Axis conquest of Egypt (see `commonwealth-handoff` in WORK.md). **The run ends 1945.10, so this is TRUNCATED, not a measured stalemate** — but unlike `07270b64` there is no collapse trajectory to extrapolate: Germany is stronger at the last save than it was on D-Day.
  - 2026-08-19 · `07270b64` · **FAILED — truncated at 1945.12 with the European war fully open, fourth campaign running without a German defeat.** No `fall_of_berlin_sov`, no `hitler_has_died`, no `ww2_has_ended`, no white-peace chain flag anywhere in the final save. GER at 1945.12: alive, fascist, Axis leader, **239 divisions** (0.984 eq fill), owns 52 states / controls 50, **42 wars open** (SOV, USA, ENG included), only SOV 2 + FRA 11-province lodgements inside its bloc. D-Day landed on time (`d_day_happened` **1944.6.8.1**) and Paris was liberated by 1944.9 (FRA 314 prov / GER 79 inside France then) — but state 16 reads **ITA-held at 1945.6→12** and the run ends there. Distinct from the last three failures: this is not a two-year stalemate at end-of-saves, it is a **campaign that stopped existing 5 months early** while the war was mid-flight — the cloud run wrote 119 saves instead of the usual 124-126, last save 1945.12.1.2, real-world write 10:31 vs a 09:17 start (a normal full run reaches ~1946.5). Whether the run was cut by the harness or crashed is not determinable from the saves (**ASSUMED**; no local log). Barbarossa at **1942.2.12** (8 months past its hard date, see F3) means the whole eastern endgame was compressed; the war could not have finished by 1945.12 on any observed collapse rate. Controller census across six saves, same extractor each time: **GER 401 (1943.6) -> 402 (1944.6) -> 319 (1945.6) -> 274 (1945.12) -> 252 (1946.2) -> 210 (1946.5)** - it peaks in mid-1944 and then halves. The German puppets go with it in the last quarter: **RUK 141 -> 38, RBL 109 -> 41** between 1946.2 and 1946.5. Japan moves the other way, 347 (1943.6) -> **501** (1945.6) -> 484. The Allied bloc's own count is nearly flat throughout (95-158), and over that final quarter the biggest gainers are USA +7 and SOV +6 while the Axis sheds 213 - so the ground the Reich loses is **not** showing up as Allied gains. **CAVEAT, and it bounds every number above:** the census counts bare `controller=` lines, and I could not establish the denominator - a state-block parse returned 12,440 blocks for a ~1,300-state map, so the unit is unverified. Read the six figures as a consistent RELATIVE trajectory, not as state counts, and take no rate from them. **What this changes:** the row that called this a front that stops moving is wrong. Whatever is wrong here is a war being won too slowly to finish in ten years, which is a different question and needs a different probe.

### F6. Pacific war terminates — KNOWN GAP
- **Ledger:** `class=FUNDAMENTAL threshold=none streak=0 fix=none status=KNOWN_GAP`

- **Pass:** the Pacific war reaches a termination path (Japanese surrender chain).
- **Status:** **KNOWN GAP — no termination path exists in the mod.** Expected FAILED until the Japanese surrender chain is built (Tier 3 in `campaign-audit-fix-plan`); do not re-diagnose, do not count toward pathology findings.
- **Streak:** 0
- **History:**
  - 2026-08-27 · `1ac7e4ea` · FAILED (KNOWN GAP) — JAP at 1946.1: **357 divisions and rising**, `japanese_co_prosperity_sphere` intact (7 members); no surrender/nuke flag of any kind. Only Pacific-adjacent flags in the whole run: `second_sino_japanese_war_has_started` 1937.7.8, `JAP_khalkhin_gol` 1939.7.8. Not re-diagnosed.
  - 2026-08-27 · `24933fb9` · FAILED (KNOWN GAP) — JAP at 1945.11: **40 owned / 78 controlled** states (73→78 across 1943.6→1945.11, flat-to-rising), **349 divisions** (95→349 monotone), fill 0.990, no nuke flag of any kind. Last Pacific arc flag `fall_of_singapore` **1942.1.13** — **~46 months dark**. Not re-diagnosed.

### F8. No major runs the old pathologies
- **Ledger:** `class=FUNDAMENTAL threshold=none streak=4 fix=none status=PASSING`

- **Pass:** for every major: manpower not pinned at ceiling with falling division count; army-wide field fill ≥90%; no 100k+ equipment hoard sitting beside starving faction allies. **RECUT 2026-08-18 — the fill bar does not apply on its own.** A belligerent in year 4+ of a war it is losing runs a low fill *because it is losing*: that is the game working, not an allocation defect. Score the fill bar as a FAIL only when it is **paired** with the pathology — manpower at ceiling AND a falling division count — or when the country is not under sustained attrition (no contested home territory, war under ~3 years). A low fill on its own is **recorded, not failed**. This was cut after `0bbc1f60` scored GER 0.690 as a FAIL while the same reading showed no pathology at all; the reviewer's objection was "it is normal to lose the war as the Axis after 1943" and it is correct.
- **Probe:** division counts for every major via `savegame.py army <TAG> <saves...>` — it counts `division={` **inside `units` only** and prints `wa_tlm_comp_div_total` and `num_armies_for_training` beside it; treat the TLM figure as the authority and never hand-count. Then per-major `units` fill sampling + stockpile vs allies' deficits; `var TAG "^wa_ai_"` deployment/lend-lease state. Cross-reads R5 (GER), R7 (relief legs), R10 (USA).
- **Note:** GER late-war 65% fill under the low supply-reach defines was ruled **by design** (Phase 1 rejection in `campaign-audit-fix-plan`) — do not count it here.
- **Streak:** 4
- **History:**
  - 2026-08-27 · `1ac7e4ea` · **PASSED, with ENG recorded** — majors GER 307 / SOV 385 / JAP 357 / ITA 124 / USA 101 at 1946.1, all growing or flat; no ceiling-pinned-manpower + falling-count pair measured. **ENG recorded, not failed**: deployed **43 (1943.1) → 19 (1945.6/1945.10)**, a −56 % decline over 33 months of active war — but ENG is losing ground (22 ENG-owned states Axis-held at 1946.1) and the manpower half of the pathology pair is not measurable on this build (no manpower gauge in the v32 metric set), so per the 2026-08-18 recut this is *decline while losing*. Two ENG anomalies for follow-up: (a) **zero armour + zero mech the whole campaign** (`wa_tlm_comp_armor=0`, `comp_mech=0`, `armor_mech_pct_hist` 0 in 37/39 quarterly samples, `_last_t` live); (b) the unexplained **19 → 40 jump over 1945.10→1946.1** — NOT the reserve bank (it drained 1945.2 and its 40-division wave left 1 survivor by 1945.3, see `[reserve-quality]` in WORK.md).
  - 2026-08-27 · `24933fb9` · **PASSED under the recut — 2 of 3 legs measured, leg 1's manpower half not measurable.** Divisions (`wa_tlm_comp_div_total`, units-only count agreeing in all 28 cells) at 1940.6/1942.6/1944.6/1945.11: GER 144/261/285/**218**, SOV 199/375/412/**458**, USA 11/**4**/47/**115**, JAP 95/302/326/**349**, ITA 79/88/84/**114**, ENG 36/40/62/**75**, FRA 121/7/7/**5**. Leg 1: PASS on the division half — only GER (year-7 attrition, collapse measured in F5) and FRA (conquest) fall; free manpower pool serialises as ~0 everywhere (opposite of pinned), and `wa_tlm_version = 31` carries no manpower metric. Leg 2: **26/28 cells ≥ 0.90**; GER 1945.11 **0.710** paired with a falling count but inside the sustained-attrition exemption (contested home soil, 6-year war) — recorded, not failed, as `0bbc1f60`'s 0.690 was; ITA 1941.6 0.830 unpaired (army rising). Leg 3: **PASS** — stockpiles by own-builder at 1945.11 (SOV 1.18 M / JAP 864 k / USA 778 k / GER 421 k) but no faction ally starving: every major ≥ 0.93 fill except GER, the Axis donor itself. **Recorded, both worth a probe next run:** (a) **USA falls 11 → 4 deployed between 1940.6 and 1942.6** — six months *after* Pearl Harbor, at 1.000 fill — and reaches only 47 by D-Day, the weakest US mid-war curve since `a8b29e09` (not the `9be92c89` signature: fill perfect, curve then monotone to 115); (b) ENG 36/40/62/75 flat-to-modest = `reserve-quality` by design, not comparable across `ed109de9d`.
  - 2026-08-25 · `8f9b5653` · **PASSED under the recut — and the third leg is MEASURED here for the first time in seven campaigns.** Leg 1 (manpower pinned at ceiling + falling division count): **PASS**. Divisions at 1940.6 / 1942.6 / 1944.6 / 1945.10 (`wa_tlm_comp_div_total`, treated as authority): GER 134/264/283/**322**, SOV 207/371/406/**429**, USA 11/13/106/**212**, JAP 102/319/348/**384**, ITA 84/83/122/**146**, ENG 34/35/44/41, FRA 127/10/11/6. Only FRA falls, by conquest. **Probe correction worth keeping:** `countries/<TAG>/manpower={ current= }` is serialised only when non-zero and is **absent for all six live majors at all four saves** — the free pool is ~0 everywhere, which is the *opposite* of pinned-at-ceiling, not a missing reading. Leg 2 (fill ≥ 0.90): **26 of 28 cells pass**; the two exceptions are ENG 1942.6 **0.853** (division count *rising* 34→35, so unpaired — recorded, not failed) and FRA 1942.6 **0.788** (127→10 divisions by conquest, exile force, no manpower ceiling — attrition-explained). Leg 3 (no 100k+ hoard beside starving allies): **PASS with a WATCH** — own-built stockpiles at 1945.10 are USA 1 131 855 / JAP 924 144 / SOV 842 823 / GER 808 640, and the single hoard-beside-a-sub-0.90-ally instance in the whole campaign is **GER holding 174 735 `ger_inf_7` + 170 775 `ger_inf_6` while Axis ally ROM declines 0.759 (1944.6) → 0.669 (1945.10)**; ROM is not industry-poor (104 controlled arms factories, its own 10-factory truck line) and its decline is on the collapsing Balkan front, so this reads as attrition rather than donor-side famine — **DERIVED, not proven**, and it is the pair to re-probe next scoring. **Not an anomaly — recorded because it will look like one next campaign:** ENG's army is flat for the entire war (34/35/44/41) while every other Allied major grows 2–20×. **Owner, 2026-08-25: this is `reserve-quality` (`ed109de9d`) working as intended** — an overseas-only war no longer opens the reserve bank, so ENG fields fewer, better-equipped divisions instead of a wave of 30 %-fill reserve divisions. **Consequence for this item: a division COUNT is no longer comparable across the `ed109de9d` boundary.** F8's leg-1 pathology (manpower at ceiling AND falling division count) still reads correctly, since ENG's count is flat-to-rising, not falling — but any cross-campaign division-count comparison in this file that straddles 2026-08-24 20:28 is comparing two different army models and must be re-based on fill and frontage held.
  - 2026-08-19 · `07270b64` · **PASSED (recut criterion).** No major is manpower-pinned with a falling division count: every major carries 0.7–1.9 M free manpower at 1945.6; division curves 1939.9→1945.12 — GER 91/198/265/255/239 (the only faller, −26 over 30 months, combat attrition), SOV 156→359, USA 11→206, JAP 127→416, ITA 61→128, ENG 31→91. Field fill ≥0.90 everywhere measured (1945.6: GER .997, SOV .937, USA .985, JAP .990, ITA .979, ENG .980; 1945.12: all ≥ .907). Hoard leg NOT CHECKED (no cheap archetype-resolved stockpile probe — 6th of the last 7 campaigns).
  - **Number correction 2026-08-11:** the division figures in this entry and in R5/R10 were counted across the whole country block and are ~8–10% high. Units-only (authoritative, = `wa_tlm_comp_div_total`): SOV **182 → 379 → 373 → 388 → 368 → 356 → 394** (1940.6→1946.4), GER **132 → 249 → 264 → 269 → 315 → 312 → 337**, USA **17 → 19 → 53 → 64 → 122 → 136 → 143**, ENG 74 → 66 → 80 → 93 → 146 → 94 → 100, JAP 130 → 152 → 270 → 297 → 307 → 324 → 325, ITA 88 → 101 → 107 → 128 → 74 → 37 → 16. **None of the verdicts change** — no major collapses on either counting — but calibrate future thresholds on these.
  - **Probe-definition bug found 2026-08-11 (fix applied above):** `division={` blocks appear in **two** sections — `units` (deployed) and `experience_status`. A naive count of all `division={` inside a country block over-reports: USA at 1944.1 is 97 total but **80 deployed**. `31eaf7e6`'s "102 at 1944.1.1" R10 pass was almost certainly counting the inflated way. Count `division={` **inside `units` only**.
  - **Measurement correction 2026-08-12 (three passes disagreed; this is the adjudicated reading).** GER's 1945.6 rifle stockpile is **101 033**, not 117 370 and not 549. The 549 came from filtering to the vanilla `infantry_equipment_*` archetype — which for GER is literally **0** at 1945.6 — *and* from reading the wrong save; 117 370 does not reproduce under any consistent rule (rifles + heavy-infantry = 116 322). **The durable trap:** BUL and ROM have no national `bul_inf_*` / `rom_inf_*` family in this mod, so their stock appears as `infantry_equipment_N` with `creator="BUL"`/`"ROM"` — a vanilla-archetype filter therefore looks *plausible* on the recipients and silently returns near-zero on GER, whose rifles are all `ger_inf_*`. Resolve every id through the top-level `equipments={}` registry and select on `archetype = infantry_equipment`; keep `heavy_infantry_equipment` broken out separately (it is a distinct archetype, and merging it is what made the two passes look reconcilable). Added to the recurring-anomalies list below.

### F9. Game boots
- **Ledger:** `class=FUNDAMENTAL threshold=none streak=19 fix=none status=PASSING`

- **Pass:** the build the campaign runs on launches without CTD. Scored per build, before the campaign: run the launch harness after any commit touching `force_concentration` blocks in country ai_strategy files — the deterministic-CTD lesson is that `GER_fall_gelb` + `war_with_soviets` fc entries are load-bearing (see comments in `common/ai_strategy/GER.txt` and the `d69eef2fa` incident).
- **Streak:** 19
- **History:**
  - 2026-08-27 · `1ac7e4ea` · **PASSED (DERIVED)** — build **`cd234cc51`** (HEAD at run time, commit 13:49:54 / first save 13:52, `wa_tlm_version = 32` MEASURED first and last save) wrote 120 consecutive monthly saves over 10 game-years. Discharges in campaign conditions the boot debts owed by: the AIFC `min_pad` floor + `selection` constant group + `wa_test_aifc` event file, the capability expeditionary gate (`f8e6cc896`), the 3 theatre-distributor defines (`ec8737a6f`), `[reserve-quality]` v2 (`cd234cc51`), lend-lease-observability v32 telemetry, `prospecting-coop-solvency`, `rk-no-divisions`, and the CAN reserve-batch reward. The launch harness itself was not run.
  - 2026-08-27 · `24933fb9` · **PASSED (DERIVED)** — the build (`2f16ec583` by push/run timing + two in-save fingerprints) wrote 118 consecutive monthly saves 1936.2→1945.11 with the WA_AI init flags present at 1936.1.1.12; a CTD-on-load build cannot produce that. The launch harness itself was not run this session; the boot tests owed by `rk-no-divisions`, `prospecting-coop-solvency` and the CAN reserve batch remain owed as *launch* tests.
  - 2026-08-19 · `07270b64` · **PASSED with an asterisk** — build **`f1e0b9086`** (the 2026-08-19 morning stack: Commonwealth handoff `2cdfce1e0`, infantry-template `7b54880a8`, heavy-garrison `a60d5a976`, convoy relief `62ea4ee49`, convoy-trigger parse fix `f1e0b9086` at 09:15, first save 09:17) booted and ran **119 consecutive monthly saves / 9.9 game-years (1936.2 → 1945.12)** with no missing month and strictly monotone sessions. **Asterisk: every prior full run reached 1946.4–1946.8 (123–126 saves); this one stops at 1945.12 with the European war open.** No save-side evidence distinguishes "harness stopped early" from "late-game crash" — flagged, not diagnosed (no local log on the cloud box). Discharges the boot-test debt of the whole handoff/convoy/template stack in campaign conditions.
  - 2026-08-19 · `2f8cbd51` · **PASSED** — build fingerprint `9a30aa6e9` booted and ran 126 consecutive monthly saves, 1936.2 through 1946.7, with no missing or duplicate month; all 125 session transitions strictly increase. `wa_tlm_version = 27` first and last. This scores the campaign build only, not the later 2026-08-19 working-tree convoy recut.

---

## Recurring cosmetic anomalies — check here before reporting a "finding"

- **The global `spanish_civil_war` flag is never cleared on SCW resolution.** `scw_over` / `nationalist_victory` / `SPR_franco_won` are the authoritative end markers; the war flag still being set says nothing about the war still running (seen resolved-but-set in `31eaf7e6`).
- **`ww2_*` theatre flags carry last-seen dates, not first-set dates.** They are re-stamped on every on_action fire, so their set-dates are useless as a war timeline — use the war/peace flags instead. (In `66d6b53c` all three read 1945.3.1.)
- **Double Trotsky flags** appear in SOV flag blocks; harmless duplication, not a political-path bug.
- **`atomic_research_completed` firing ~1940 is a research-tree marker, not a bomb.** It marks the research step, not weapon availability — do not report early nukes off this flag.
- **`production` stores equipment by numeric variant id.** A name grep (`--grep "tank"`) on the `production` section only hits MIO idea names — resolve `equipment_variant_index` via the top-level `equipments={}` registry before concluding a line doesn't exist.
- **`lend_lease_to_allies_history` misses `send_equipment` transfers.** The WA relief system moves stock instantly without touching the diplomacy IC ledger; zero ledger IC between two countries is NOT evidence of no equipment flow — check `creator=` tags in the recipient's stockpile.
- **`units` line count absorbs the navy** — never use it as an army-size proxy; run `savegame.py army <TAG> <saves...>` instead.
- **`division={` blocks appear in TWO sections.** `units` holds deployed divisions; the `xp_by_template` blocks under `experience_status` are siblings of `units`, not nested inside it. A whole-country-block count over-reports — USA at 1944.1 in `911bed3c` is 97 total vs **80 deployed**; SOV 1942.6 is 409 total vs **373** deployed. **Use `savegame.py army`, which does the `units` scoping for you**; do not hand-count. It also prints the two cross-checks, which are not equivalent: `wa_tlm_comp_div_total` equals the units-only count (49/49 tag×date cells on `911bed3c`; one 1-division monthly-sample lag seen on GER 1946.4), while `num_armies_for_training` tracks it only while the army is stable and **overstates during losses** (ITA 1946.4: 28.0 vs 16 deployed). Use the TLM metric for analysis — but it is WA_TLM, so gameplay logic must never read it.
- **A state block omits `controller=` when the controller is the owner.** Taking the field literally makes a country look like it controls only its conquests (ENG 1946.4 reads 3 controlled states instead of 75). `savegame.py buildings` defaults controller to owner; any ad-hoc `states={}` parsing must do the same.
- **`decision_status` `days` and `count` are different clocks and different populations.** `active_timed_decision` exists only while live and carries `days` (remaining mission time when `state=active`; a re-arm cooldown, on an unrelated clock, when `state=failed` or `re_enable_cooldown`). `random_item` carries `count`, a **monotone cumulative fire counter** — that is the field for "how many times did this fire". Two entries were conflated twice in one session; `savegame.py decisions` labels the kinds apart.
- **`capitulated=yes` at depth 3 is a wargoal property, not capitulation.** It appears inside `<TAG>/diplomacy/wargoals` for countries that are perfectly healthy. Only a **depth-2** `capitulated=` is the country's own status.
- **`resource@X` reads `to_use[0] + to_use[2]` — net available MINUS unmet demand.** Not `produced`, and **not `net` alone**. `savegame.py resources` prints it as the **EFFECTIVE** column. ENG aluminium 1942.6: net `807.3`, deficit `−799.0`, effective **`+8.3`** — a `> 50` latch correctly stays shut on a row whose net column reads 16× the bar. Two mistakes stack here and both have cost a scoring session: reading `produced` alone (nearly a false FAIL on R25) and reading `net` alone (**a false accusation against Fix 39, see R32**). *This entry previously said "net available" and cited ENG **bauxite** in the same save, whose deficit is −1.0 so the two readings coincide — a rule induced from the one resource where they agree.* Verified 2026-08-13 on `02bd4445`: 36 discriminating latch cells, all siding with net+deficit, none with net.
- **`obsolete=yes` in the `equipments={}` registry is descriptive, never causal.** It does not mean only-one-per-archetype (288 (creator, archetype) groups on `02bd4445` hold 2+ non-obsolete members, up to six) and it does **not** gate production — 7.0% of lines and 9.6% of assigned factories run obsolete-flagged equipment in the final save, rising through the campaign, incl. USA's 68-factory `tank_usa_medium_chassis_3`. Also: the registry carries **no `archetype` field** (resolve it from `common/units/equipment/`), `obsolete=no` never occurs (absence = non-obsolete), and the entry's `parent_id` is a design-variant pointer unrelated to the def-level `parent =`. Never conclude "the AI cannot build X" from this flag.
- **`production_upgrade_desire_offset` is not serialized into a country's `ai` section.** No `ai_strategy` entry in a save carries a negative value at all, and no `id=` is ever an equipment token; three ungated controls (incl. `SOV_dont_build_shit_guns`) are equally invisible. A save can therefore never answer "did this strategy arm" — score it on the production lines instead. The `ai`-section reading used for R34's `invade` suppressions does not generalise to this type.
- **`date="1.1.1.1"` in `technology/technologies` is a tech being researched right now, not a duplicate.** It carries `research_points` and no `level=`, plus a twin under the sibling `technology/slots` block. Grepping names across the whole `technology` section reports a false duplicate for every in-progress tech (ENG 6, GER 6, USA 3 on `02bd4445`, matching `slots` 1:1). Start-granted techs carry a real date (`1936.1.1.12`).
- **`wa_ai_overext_dbg_active` is a monotone months-active counter, not a boolean.** Read it as "months the overextension flag has been set", not "currently overextended".
- **This mod sets `AIRBASE_CAPACITY_MULT = 100`, not vanilla's 200** (`common/defines/05_defines.lua:173`). `wa_ai_uk_air_dbg_capacity`'s ×100 is therefore the true engine figure — do NOT "correct" it to 200. (An earlier version of this file claimed the opposite.)
- **`*_dbg_started` counters over-count where the queue-growth guard is missing.** They increment after `WA_AI_PC_start_project`, which declines silently on `queue_max`/dedup. The theatre air-base strategy has the guard; the UK-hosting one does not. Never read such a counter as "projects built" — cross-check against actual building levels.
- **Absent `*_dbg_*` counters mean "never incremented", not "missing instrumentation".** The `WA_AI_supply_line_dbg_*` and `WA_AI_overext_dbg_mic_redirects_*` families are bare `add_to_variable` counters with no zero-init, so absence is a *result*, not a probe gap. Contrast the `WA_TLM_*` metrics, which ARE zero-initialised (`WA_TLM_core.txt:61-62`) — there, a present-and-0 value is a real zero reading. **Per-counter semantics — what each one counts, what it does not, and its known miscounts — now live in `documentation/WA_TLM_TELEMETRY_SYSTEM.md` §2.1; read the row before scoring any `*_dbg_*` value.**
- **Cloud campaigns reuse filenames and overwrite each other.** Saves are named `YYYY.M_Mon.hoi4`, so a new run silently replaces the previous campaign's files in place, leaving a few stragglers with *later* in-game dates than the current run's final save. Group by `game_unique_id`, never by filename or date.
- **Infantry-equipment stockpiles must be summed by ARCHETYPE, not by the vanilla family.** GER builds no `infantry_equipment_*` at all — its rifles are `ger_inf_*` — while BUL and ROM have **no** national family and *do* appear as `infantry_equipment_N` with `creator="BUL"`/`"ROM"`. A vanilla-only filter therefore looks plausible on the recipients and silently returns ~0 on the donor: it produced "GER holds 549 rifles" against a true **101 033** in one `9a4cd657` pass. Resolve ids through the top-level `equipments={}` registry, select on `archetype = infantry_equipment`, and keep `heavy_infantry_equipment` (the `*_hv_inf_*` families) broken out separately — merging the two is what made two contradictory passes look reconcilable.
- **The `strategic_air` walk must count `count=` only at the direct child depth of `air_wings={`.** A second `count=` family one level deeper (per-wing history/data sub-blocks) inflates plane totals by ~2.2× and dumps most of the fleet onto one arbitrary state — it produced ENG 27 638 instead of 6 144 on the first `9a4cd657` pass.
- **`WA_port_demolished` is not a Nero-only marker.** The generic `scorch_earth` decision (`common/decisions/_generic_decisions.txt:2927`) sets the same flag as the on_action (`100_wa_on_actions.txt:3336`), so it appears on SOV states from 1941.7, Sicily/Salerno 1943.7–9 and Libya 1944.4 — 19 states at 1944.6 and 26 at 1946.3. A whole-map census of the flag cannot detect a ROOT-gate leak (R21 criterion 3).
- **A campaign can pass every delivery probe and still lose the theatre.** `9a4cd657` placed the Mulberries on spec, capped Nero, landed the designed 33 divisions in Normandy — and the beachhead was German again by 1944.9.1. When an outcome item fails, check the *basing* and *reinforcement* items before re-opening the delivery ones.
- **Air wings are NOT nested inside `air_base` blocks.** The layout is `strategic_air → <TAG> = { air_wing_pool = { air_base = { id = N } air_wings = { count= tag= mission={ strategic_region= } } } }`, and the ~600 `air_base` blocks that carry `state=` are **siblings that appear FIRST**. Attributing wings to "the last `air_base` seen" silently dumps the whole world's air force onto one arbitrary state. Basing requires a **two-pass join**: build `air_base id → state`, then map `air_wing_pool.air_base.id → state`. Carrier decks are the `air_base` blocks with `carrier=` and no `state=`. Depths: `air_wings` at 3, `count=`/`tag=` at 4, `strategic_region=` at 5, equipment `amount=` at 6. This is **distinct from, and compounds with**, the note above about counting `count=` only at the direct child depth of `air_wings={`.
- **Idle carrier aircraft do NOT sit in the equipment stockpile.** The unassigned stockpile holds 1-258 carrier planes for every country at every date in `02bd4445` — essentially nothing — while **JAP holds 7,000 carrier aircraft off-deck, bound into air wings parked at land air bases**, against 1,380 deck slots. A "who over-produces carrier aircraft" query keyed on the stockpile alone returns a confident **false negative**; measure `stockpile + off-deck wings` against total deck capacity. Also note GER/ITA/SOV hold small static carrier-plane stocks (61 / 5 / 93) with zero carriers and zero carrier factories ever — inert holdings that pollute any stockpile-keyed query.
- **A decoy `countries={` block appears at depth 3 BEFORE the real top-level one** (seen at line ~1 101 690 of `af003548`'s `1944.8_Aug.hoi4`). A parser that seeks the first `countries={` and reads country blocks from there lands inside a nested structure and returns garbage or nothing; anchor on depth 1. (2026-08-15)
- **Naval-invasion orders are NOT under `countries/<TAG>`.** They live in the top-level `theatres → theatre → orders_group → order_instance` block. A per-country extraction sees no invasion plans at all and reads as "no live plan" — R34 leg 1's plan half and R44 legs 2/3 must be scored from that block. (2026-08-15)
- **A province omits `controller=` when its controller equals the state's controller** — the province-level twin of the state-block rule above. Any per-province controller read (R50's Narvik probe, corridor holdings) must default a missing `controller=` to the enclosing state's controller, which itself defaults to the owner. (2026-08-15)
- **A production line omits `active_factories=` when it is 0.** Summing `active_factories` over lines silently drops the zero-factory lines from the count of *lines*; a line with `requested_factories` and no `active_factories` is a live but unfunded line, not a parse gap. (2026-08-15)
- **`savegame.py tlm` mis-renders `wa_tlm_pc_built_by_type^N` as a ring-buffer series** — the indexed variable is a per-building-type array (index = building type id), not a time series, so the tool's history axis is wrong for it (33/40 vs 7 seen on `af003548`). Read it with `savegame.py var <TAG> "^wa_tlm_pc_built_by_type"` instead. (2026-08-15)
- **`savegame.py buildings` header `states owned=N controlled=M` cannot be read as "liberated".** On `a232d96c` FRA reads **58/58** at 1946.6 while GER holds 20 of the 24 metropolitan French states — the "controlled" count includes non-owned states (Maghreb, colonies, states FRA controls for someone else), so owned = controlled says nothing about the metropole. To score liberation, walk `states={}` for the metropolitan ids (`controller=` defaulting to `owner=`) — the direct walk read GER 20 / FRA 2 / RBE 1 / ITA 1. (2026-08-16)
- **`wa_tlm_nav_convoys` / `num_equipment@convoy` / `has_equipment = { convoy … }` read the FREE convoy pool, not the fleet.** Any country actually running trade sits at 0 free convoys because every hull is assigned; ENG read 0 for 46 consecutive months on `a232d96c` while its fleet never dropped below ~600. The fleet is `convoys={ equipment={ … creator= } }` at depth 1 of the country block (sum the `equipment` entries; `creator=` tells own-built from exile/lend-lease hulls). `convoys_destroyed=` at country depth 1 is kills **by** that country (= `wa_tlm_nav_conv_killed`), not its own losses. Neither figure is a script-visible fleet counter — which is the R56 gate defect. (2026-08-16)

