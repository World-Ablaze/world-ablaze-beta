# Lessons index

One line per entry of `lessons-log.md`, in log order. Grep THIS file first — the full log is 2,172 lines
and does not fit a working context. Each line ends with a verbatim fragment of the entry's heading:
`grep -F "<fragment>" lessons-log.md` jumps straight to the entry.

Line format: `- <CLASS> <MM-DD> <rule in <=15 words> · grep:"<verbatim heading fragment>"`

| Class | Meaning |
| --- | --- |
| A | Engine semantics misunderstood (a token/effect/define does not do what its name suggests) |
| B | PDXScript silent traps (scope, temps, NOT/NOR, absent variables, meta/loc, encoding) |
| C | Unverified hypothesis promoted to fact (conclusion shipped before the killing measurement) |
| D | Measurement / savegame / probe artefacts (the reading was wrong, not the game) |
| E | Process & discipline failures (partial idiom fixes, untracked copies, missing callers, doc drift) |
| F | AI design flaws (feedback loops, hysteresis gaps, gates without exits, historical-path gating) |
| G | Tooling & generators (evaluator, replacers, generated-output hazards) |

Update rule: every new log entry adds its index line in the same commit; the wa-lessons-reviewer
triages from this file first.

---

- A 08-08 Puppet land is controlled by the puppet; iterate every_subject_country too · grep:"does not include subject territory"
- A 08-08 build_railway is a map modification; works whoever controls the province · grep:"ignores the controller"
- A 08-08 Route refused? Check the province-type walkability parameter at the call site · grep:"Pathfinder province-type parameter"
- E 08-08 File-scoped @ constants: grep every declaration before changing one · grep:"duplicated on purpose"
- E 08-16 Shared numbers need a registry and checker, not a comment · grep:"is not a mechanism"
- F 08-08 Search ports per target, distance-limited; never cache per landmass · grep:"per-target and distance-limited"
- F 08-08 Same-landmass PARTIAL/FAIL routes must fall through to overseas supply · grep:"need an overseas fallback"
- D 08-08 WA Japan leads its own faction; never test via is_in_faction_with GER · grep:"not an Axis faction member"
- E 08-08 A harness test needs all seven registrations or fails silently · grep:"seven registrations per test"
- D 08-08 Before blaming the mod, re-read the test's dates and state IDs · grep:"the test's fault"
- A 08-08 replace_path folders: mod files are the WHOLE category; parse errors delete content · grep:"removes it from the game"
- A 08-14 A partially populated replace_path folder silently deletes vanilla's remainder · grep:"only partly populates"
- E 08-08 Docs hold intent; verify facts (versions, line refs) against source · grep:"Documentation drifts from source"
- A 08-08 AI renames templates; create_unit must create its template in the same block · grep:"reference a template by name"
- F 08-08 Focus tech gates need unconditional research triggers, or the AI deadlocks · grep:"AI research weight is conditional"
- A 08-08 Characters need recruit_character before advisor/leader effects work · grep:"character recruited first"
- A 08-08 Post-annexation template deletion by name must use donor-unique names · grep:"Never delete an inherited template"
- A 08-08 Check archetype availability before naming equipment in a scripted template · grep:"equipment the country cannot design"
- A 08-08 One support company per same_support_type per template; themed officers count · grep:"cannot coexist in one template"
- A 08-08 Filter blanket set_nationality transfers with is_advisor = no · grep:"already hold an advisor role"
- A 08-08 Icon-sheet frame 0 must stay generic and tintable · grep:"generic, tintable icon"
- D 08-09 Saves lowercase variable names; search case-insensitively · grep:"variable names lowercased"
- A 08-09 alliance_strength_ratio is global; use front-local measures for theatre decisions · grep:"worldwide - never gate theatre"
- A 08-09 Expeditionary powers control nothing; where-can-I-act scans need ally-soil fallback · grep:"blinds expeditionary powers"
- F 08-09 Peace-gated relief for a recurring wartime cost needs a scripted AI path · grep:"no relief valve except war bonds"
- F 08-09 Delegated defence keeps the delegator's floor plus a verified delegate bootstrap · grep:"needs a bootstrap path"
- F 08-09 Audit scripted PP sinks first; a write-only reserve is a shredder · grep:"write-only reserve"
- A 08-09 fighting_army_strength_ratio compares whole armies; coalition members fail vs majors · grep:"is pairwise - coalition members"
- F 08-09 Grounded planes: audit base capacity in range before touching air strategies · grep:"air basing is a construction problem"
- A 08-09 Enabling a new ai_template target re-runs decommission; nationals die mid-campaign · grep:"re-run the decommission pass"
- A 08-09 Template targets must be reachable by greedy edits without column restacking · grep:"deadlocks on regimental-support"
- B 08-09 Meta tokens spliced into unit names need every expansion to exist · grep:"naming scheme is asymmetric"
- B 08-09 defined_text returning a loc key yields its display name; return fragments · grep:"output is localised"
- C 08-09 order_id pooling inferred from a comment; save refuted it; set subtract_fronts_from_need · grep:"eat the buffer by default"
- D 08-09 Campaigns run on a cloud machine; verify builds via in-save fingerprints · grep:"dedicated cloud machine"
- B 08-09 Absent variables read 0; guard brake thresholds with has_variable · grep:"guard brake conditions with has_variable"
- E 08-08 Copy exact tab whitespace; never reformat untouched blocks · grep:"Match tabs exactly"
- A 08-09 Deleting force_concentration blocks can CTD at load; launch-test such deletions · grep:"can CTD the game"
- F 08-09 Reversed war outcomes: audit weekly FLOWS, not opening strength · grep:"lend-lease drip"
- B 08-10 THIS.id is engine-encoded; never compare against plain stored ids · grep:"engine-encoded reference"
- B 08-10 Give every loop an explicit break variable; reset at effect head · grep:"leaks across effects"
- B 08-10 An if/else ladder with no matching branch passes; add terminal else · grep:"vacuously TRUE"
- C 08-10 Reconstruct duplicated values at the authoring commit; the edited copy is intent · grep:"ONLY record of the design intent"
- A 08-10 on_startup fires on new game only; one-shot repairs are console migrations · grep:"does not re-fire"
- F 08-11 Liberation reverts states to the owner ally; selectors must permit faction builds · grep:"goes to the poorest ally"
- F 08-11 A ladder step must not destroy its own precondition; re-evaluate recurringly · grep:"completes at fatigue 0"
- A 08-11 Landing-time buildings predate on_state_control_changed; defer placement · grep:"predate the state-control flip"
- E 08-11 Enumerate the state a fix never reaches; parked saves need migrations · grep:"cannot re-arm state"
- E 08-11 surrender_progress measures VP loss, not capability; fix every copied gate · grep:"not a capability test"
- A 08-11 Available-factories reads ~0 when the queue saturates; use floored triggers everywhere · grep:"vanilla queue saturates"
- B 08-11 Multi-child NOT is NOR; write the OR or AND explicitly · grep:"it has now killed two systems"
- F 08-11 A controller reducing demand erases the scarcity signal it reads · grep:"erases its own demand signal"
- A 08-11 The trade AI imports to ~0 equilibrium; comfort thresholds are unreachable · grep:"never to build a reserve"
- A 08-16 resource@X is the producer's retained surplus; only importers park at zero · grep:"retained domestic surplus"
- C 08-17 Score a gate on its full population; read exported/to_export first · grep:"export share scales with extraction"
- E 08-11 Before copying a numeric idiom, verify the data property making it correct · grep:"correctness rests on the data"
- C 08-11 A resource nothing consumes may be a raw tier; ask what it enables · grep:"evidence of design, not absence"
- A 08-11 Grep both defines files before asserting an engine constant; the mod's file is a diff · grep:"before quoting any engine constant"
- E 08-11 A caveat on one twin says nothing; open the sibling's write site · grep:"says nothing about the twin"
- C 08-11 Grep scans yield candidates only; read the enclosing block first · grep:"structural or grep scan"
- F 08-11 Advance/retreat pairs must move equal steps for every variant · grep:"same distance both ways"
- G 08-12 Generated output used as input needs baselines; test analyse-apply-analyse-empty · grep:"idempotent analysis view"
- G 08-12 Derive equipment succession from the tech graph, never file order · grep:"ai_equipment file order"
- G 08-12 Below-threshold designs are fallbacks, not rejections; keep a production floor · grep:"only production floor"
- G 08-12 Validate slots, categories and count limits before scoring a redesign · grep:"redesign legality before scoring"
- G 08-12 Filter byte-identical patches before assigning operation IDs · grep:"byte-identical generator patches"
- D 08-13 Resolve region ids via the mod's id then name then loc, never filename or wiki · grep:"vanilla and the wiki give wrong names"
- B 08-13 num_ships_with_type takes hull types or four aggregates; categories read 0 · grep:"takes hull types"
- A 08-13 naval_mission_threshold is a bar: positive suppresses; duplicate ids sum · grep:"a bar, not a priority"
- C 08-13 obsolete=yes is descriptive, never causal; read the production lines · grep:"explains nothing"
- D 08-13 resource@X is net MINUS unmet demand; score the EFFECTIVE column · grep:"MINUS unmet demand"
- A 08-13 production_upgrade_desire_offset only waives upgrade stockpile checks on existing lines · grep:"waives a stockpile-surplus check"
- G 08-13 Bound generated numbers by the range the engine observably honours · grep:"observed to honour"
- E 08-13 Compute transitive downstream reach before suppressing a tech; ai_will_do is generated · grep:"technology LEADS TO"
- C 08-13 Before blaming the AI, check the equipment has a design at all · grep:"invisible everywhere"
- A 08-14 has_capitulated is not death; capitulated allies own the liberated soil · grep:"hides the ground"
- A 08-14 min_factories floors are need-blind and additive; flat cross-country counts betray them · grep:"need-blind AND additive"
- D 08-14 Aircraft lines live in military_lines; report which blocks a scan found · grep:"does not exist in the 1.19.2 save format"
- E 08-14 Script constants matching vanilla drift when the mod re-prices; register mirrors · grep:"matches VANILLA is still a bug"
- B 08-14 Country iterators inside scope loops need explicit ROOT wrappers · grep:"runs in the ITERATED scope"
- D 08-14 Dead tags keep last-written variables; prove the tag alive first · grep:"stale country variables"
- D 08-14 Unguarded set_global_flag refreshes its date; guard first-occurrence flags · grep:"records the LATEST nuke"
- D 08-14 Absent indexed variables read 0; initialise per-record fields at creation · grep:"indistinguishable from a zero one"
- D 08-14 Per-pulse gauges are snapshots; trends need counters, one sentinel per branch · grep:"not a multi-month state"
- B 08-15 A BOM desyncs the whole script file; check the first three bytes · grep:"kills the whole file"
- F 08-15 Situational suppressions need an exit reading the enemy's state, never own capability · grep:"needs an exit condition"
- A 08-15 Escort objectives come from a country's own convoy losses; scores only rank · grep:"OWN recent convoy losses"
- A 08-15 free_building_slots ignores state_max; track built+queued yourself at the add site · grep:"ignores a shared-slot building"
- D 08-16 Air-base load is nominal wing sizes, not planes present · grep:"no nominal wing size"
- D 08-16 A queued add is not a build; measure produced= or building levels · grep:"is not a build"
- E 08-16 Every gate, cap and shared temp names what it protects and assumes · grep:"outlives its reason"
- E 08-16 Before deleting an AI cheat, verify the honest path's gates end-to-end · grep:"check what the honest path is gated on"
- E 08-16 Shared numbers become script constants; @ stays legal only single-file · grep:"where `constant:` does not reach"
- D 08-16 A free pool of 0 IS shortage; Use/Need is not serialised · grep:"convoy free pool of 0"
- E 08-16 Re-adding an event means re-adding its caller; grep for the id · grep:"re-adding its call site"
- F 08-16 One relief leg per archetype; scripted transfers need the geography they imitate · grep:"complementary archetypes"
- F 08-16 Every posture verdict needs a consumer; prove gating flags are set somewhere · grep:"no live execute-order consumer"
- A 08-16 strategic_air_importance ranks requested planes; ground forces create air demand · grep:"does not create air demand"
- B 08-16 Meta renders lose variable context; invalid has_equipment tokens gate TRUE · grep:"scripted-localisation lookups"
- F 08-17 Classify Italy by owns-and-cores geography; tag, date and event gates die at the flip · grep:"owns and cores Italian soil"
- B 08-17 NOR misread again; never key a theatre on a puppet tag existing · grep:"East Africa was never a theatre"
- F 08-17 Aim AIFC at the states a scripted mission scores, on every rule option · grep:"aim at THOSE states"
- F 08-17 Overseas theatres need port buffers, sized pulls, landing-reading gates, never dates · grep:"needs a destination"
- F 08-19 Scale convoy reserves by naval-capacity bands plus separate major floors · grep:"function of network scale"
- A 08-17 The ai_area database caps at 72 on 1.19.2; count before adding, boot-test · grep:"73 crashes HOI4 1.19.2"
- D 08-17 A field constant across the whole sample is structure, not signal · grep:"are not signals"
- C 08-17 The token list defines what exists; no example does not mean no feature · grep:"TOKEN oracle"
- B 08-19 Unset temps read scope tokens, not 0; initialise loop guards at entry · grep:"does not read as 0"
- A 08-19 Move convoys with paired add_equipment_to_stockpile; send_equipment ignores them · grep:"does not move convoys"
- B 08-19 Temps live in one shared pool; bare names only, owner-prefixed · grep:"address them by BARE name"
- D 08-19 Probes print their scope context first, every run, or produce nonsense · grep:"does not print the context"
- C 08-19 Check a block's enable against the save before blaming its payload · grep:"silently OFF"
- B 08-20 Country-valued triggers can all fail per call site; rehome poisoned harnesses · grep:"Two call sites, one effect"
- A 08-20 front_armor_score id names the ENEMY; an ally's id scores nothing · grep:"ally's id is a silent no-op"
- D 08-24 Scope errors after hot reload or save load may be false; reproduce cold-boot · grep:"reproduce on a cold boot"
- A 08-27 Filter impassable states from rail paths; build edge-by-edge, whole path fails silently · grep:"refuses railways in"
- A 08-27 Negative garrison never empties existing areadef; catcher buffer moves, same-target entries sum · grep:"does NOT empty existing area-defence"
- F 08-27 Delegate availability measured in-theatre, never by total num_divisions · grep:"bar on TOTAL"
- F 08-27 Equipment-capability gates read MIL tiers, never stockpile; ENG closes by tag ruling · grep:"reads industry, not stockpile"
- A 08-29 Park an expandedWindow at y <= -height; a parked list is still drawn · grep:"park it above its own height"
- A 08-29 .gui z-order is declaration order; move the element after the container · grep:"z-order is declaration order"
